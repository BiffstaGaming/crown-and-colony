# Module: <Namespace or folder>

| | |
|---|---|
| **Last verified** | <YYYY-MM-DD> @ <short-commit> |
| **Location** | `game/src/<path>/` |
| **Layer** | GameLogic (engine-free) / Presentation (Godot) / Data |
| **Depends on** | <modules it uses> |
| **Used by** | <modules that use it> |

## Purpose

One paragraph: what this module is responsible for, and explicitly what it is **not** responsible for.

## Public API

The types/members other modules are allowed to use (everything else is internal):

| Type / member | What it does |
|---|---|
| `Foo.Bar(x)` | |

## Key design notes

- Invariants this module maintains (e.g. "goods quantities never negative").
- Error handling strategy (exceptions vs results, validation boundaries).
- Threading/turn-ordering assumptions, if any.
- Anything surprising a future maintainer must know before touching it.

## Tests

`game/tests/<path>/` — what aspects are covered; known gaps.

## Changelog

| Date | Change | Commit |
|---|---|---|
| <YYYY-MM-DD> | Initial documentation | <hash> |
