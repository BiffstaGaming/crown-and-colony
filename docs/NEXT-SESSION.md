# New-session prompt

Copy the block below into a fresh session. (CLAUDE.md auto-loads, so this focuses on *where we are* and *what to do next*.) Last updated 2026-06-14 — foreign-powers wave **FP-1→FP-4 shipped** (multi-player + active foreign-power AI); next is **FP-5 (AI economy)**.

---

You're continuing work on **Crown & Colony** (a Godot 4 / C# remake of Sid Meier's Colonization, FreeCol as reference spec). Read `CLAUDE.md` first — locked decisions, the non-standard toolchain paths, working rules. Then catch up via the ClickUp Space "Colonization": the **Session Log (doc 06, newest entry first)**, the **Architecture doc (02 — read ADR-019)**, the **Roadmap (doc 01)**, and the **kanban** (`clickup_filter_tasks` on list `901615382059`). `PostWorkSummary.md` (repo root) is the at-a-glance log.

**Where we are (2026-06-14):** Phases 0–4 complete. **Phase 5 (other powers)** — the *native* work is done, and the foreign-European-powers / multi-player wave (ADR-019) is **5 of 7 slices shipped**:
- **FP-1** extracted `Player` (player-scoped state incl. its own per-player `Market`; `Game` keeps the global world + `Players` + `HumanPlayer`); **FP-2** added the **owner-id seam** (`Unit.OwnerId`/`Colony.OwnerId`; enemy/fog/abilities resolve by owner + a stubbed stance hook `AreEnemies`); **FP-3a** parsed the **European nations** into the ruleset (`EuropeanNation`/`EuropeanNationType`, advantages, starting units, classic colony names); **FP-3b** made it **multi-player** (the human + 3 foreign powers + the natives as `Player` rows; ring-buffer `EndTurn` via `RunPlayerTurn`; reserved per-player RNG stream ids; multi-element `Players[]`); **FP-4** gave the foreign powers a **minimal AI** (`RunForeignPowerTurn`): they land on the map far from the human (`LandForeignPower`) and, each on **its own PCG stream** (`Player.Rng`, `RandomFor(player)`), found a colony then explore toward unexplored tiles; ships idle. Natives are still **inert**.
- **383 tests** (361 L1+L2 incl. 10 E2E + 2 soak + 16 L3 + 4 L4), CI green, **save format v20**, git clean. The human's stream 0 / RNG-resume / L4 goldens are byte-stable; the soak round-trips 25 seeds × 200 turns of active rivals byte-identically within the 2 ms turn budget.

**This session: FP-5 `86d3bex4w` — AI economy (trade + immigration + recruit).** Give the foreign powers an economy, **per-player and on their own RNG streams** so the human's stream 0 stays byte-stable. Scope (from the task):
- The foreign powers' **colonies produce/eat/grow** each turn — i.e. `RunPlayerTurn` runs `RunColonyTurn` for a foreign power's colonies and `AccumulateLibertyAndElectFathers`/`AccumulateImmigrationAndEmigrate` **for that player**, not just the human. (FP-4 only ran the human's economy.)
- The AI **sells its colony output to its OWN market**, and **buys/recruits in Europe** — minimal economic AI (e.g. sell surplus, recruit when affordable). Per-player market price movement is now exercised for real: add **independence tests** (one power's trades don't move another power's or the human's market) + the per-player-market **save round-trip**.
- **The determinism work (the crux):** the economy RNG helpers — `GenerateOffers`, `DrawRecruitType`, `Emigrate`, the emigrate draw in `AccumulateImmigrationAndEmigrate`, `RefreshDockForRecruitability` — currently draw from `_random` (stream 0). Parameterise them to draw from the **acting player's** stream (`RandomFor(player)`): the human keeps stream 0 (unchanged), foreign powers draw from `player.Rng`. This is what keeps the human byte-stable while the AI economy runs.
- Add an **L5 soak budget check**: over many turns the foreign economies must neither run away nor stall (invariants like FP-4's; no negative stores, colonies don't starve out, save still round-trips byte-identically).

**Remaining slices (kanban, in order — each lands all tests green + docs + CI):** **FP-6 `86d3bex51`** AI combat + diplomacy basics (stance/tension primitives — contact→peace, attack→war, tension→stance; replaces the stubbed `AreEnemies` stance hook; unblocks de Witt/Franklin; new `docs/systems/diplomacy.md`; this is where foreign/naval combat from `86d3bek5r` can land). **FP-7 `86d3bex56`** save-format v20 **consolidation + back-compat sweep** — drop the now-redundant legacy flat top-level player fields (kept additive through the wave), prove a lossless migration, freeze + verify the format. **Then:** the deferred Founding-Father effects (`86d3b7qxr`); Phase 6 (independence & REF — the REF nation is already parsed inert in FP-3a) → 7 (polish) → 8 (Australia variant = author a data set + register a `GameVariant`, no engine rewrite).

**How to work here (matters most):**
- **The byte-stability invariant (non-negotiable):** the human draws only from stream 0 (`Game._random`); every non-human player draws only from its own `Player.Rng` via `RandomFor(player)`. A stray `_random` draw on an AI path corrupts the human's game — the soak's byte-identical round-trip + `LoadedGame_ContinuesIdenticalRandomSequence` + the L4 goldens are the guards. Iterate players/units in stable by-id order.
- **Docs are part of the change** (no-drift): a slice isn't done until its `docs/systems/<x>.md` (both layers + verification + changelog — `players.md`, `market.md`, `turns.md`, `immigration.md` as relevant), `save-load.md`, `QA-REPORT.md` counts, the kanban status, the **Session Log**, and `PostWorkSummary.md` (with the **scheduled-next** item) are updated *in the same work* — **and the PostWorkSummary entry is pasted into the chat reply, not just the file.**
- **Determinism (ADR-009)** / **Transposability (ADR-018)**: per-player streams; no `Random`/unordered iteration in AI; economy/nation behaviour reads ruleset data.
- **Process (worked well this phase):** per slice → a research workflow (parallel readers over FreeCol `Player`/market/turn loop + our code + a test-blast-radius scan) → implement → an adversarial review (workflow or one thorough agent) → fix findings → commit & push to `main` (Chris has authorised per-slice push). Verify FreeCol numbers in `freecol/`.
- **Definition of done = tests green at every layer + docs synced + CI green.**

**Toolchain (this machine — gotchas):**
- Dot-source `scripts/dev-env.ps1` first (prepends the user-local .NET SDK 10 + sets `GODOT`/`GODOT_BIN`).
- **Use the PowerShell tool for all `dotnet`/Godot commands — `pwsh` is NOT installed (only Windows PowerShell 5.1); the Bash tool's `pwsh` fails.**
- Logic: `dotnet test game/tests/GameLogic.Tests/GameLogic.Tests.csproj` (add `--filter "Category!=Soak"` for L1+L2 only = 361; `--filter "Category=Soak"` for the 2 soak). Scene L3/L4: import once (`& $env:GODOT --headless --path game --import`) then `dotnet test game/CrownAndColony.csproj --settings game/gdunit.runsettings` — **the GdUnit4 runner flakes ~50% on a cold start (exit -1073741819 / timeout); just re-run, it passes (ADR-015).** Visual goldens are committed PNGs; regenerate only deliberately with `GOLDEN_UPDATE=1` (FP-4 needed none — rivals are fog-gated).

**Carry-overs flagged by the FP-4 adversarial review (latent, address when relevant):** owner-gate the rest of the presentation as rivals become discoverable; cache an exploration frontier (`StepTowardNearestUnexplored` is O(map)/unit/turn — fine at the 2 ms budget today, will matter as AI grows); a **minimum-distance-between-colonies** rule (the `CheckFoundColony` TODO, now reachable via the AI — relevant once AI founds more); synthesize native `Player` rows when loading a pre-FP-3b save; persist `_currentPlayerIndex` if turns ever save mid-ring; wire native units to their player id (FP-6/native-AI).

**Awaiting Chris's playtest (don't mark done):** the In Review kanban items (FreeCol art, colony screen, economy UI, Europe screen, native settlements). Combat + the foreign powers have **no UI yet** (logic + tests only; rivals are off-screen under the human's fog). Launch the game if asked (detached): `Start-Process "C:\Users\Chris\Tools\Godot_v4.6.3-stable_mono_win64\Godot_v4.6.3-stable_mono_win64.exe" -ArgumentList '--path','C:\Users\Chris\Code\Colonization\game'`.

Start by reading the latest Session Log + ADR-019 + the FP-5 task, confirm the build + tests are green, then implement **FP-5 (AI economy)**.
