# New-session prompt

Copy the block below into a fresh session. (CLAUDE.md auto-loads, so this focuses on *where we are* and *what to do next*.) Last updated 2026-06-14 — Phase 5 native work complete; the foreign-European-powers / multi-player wave **planned & decomposed (ADR-019)** and ready to build.

---

You're continuing work on **Crown & Colony** (a Godot 4 / C# remake of Sid Meier's Colonization, FreeCol as reference spec). Read `CLAUDE.md` first — locked decisions, the non-standard toolchain paths, working rules. Then catch up via the ClickUp Space "Colonization": the **Session Log (doc 06, newest entry first)**, the **Architecture doc (02 — read ADR-019)**, the **Roadmap (doc 01)**, and the **kanban** (`clickup_filter_tasks` on list `901615382059`). `PostWorkSummary.md` (repo root) is the at-a-glance log.

**Where we are (2026-06-14):** Phases 0–4 complete. **Phase 5 (other powers)** — all the *native* work is done (settlements, fog upgrade, interaction, trade, and combat 5a/5b/5c: the attack action + roles/equipment + brave defenders + **settlement assault/plunder/destroy** + Washington/Revere/Cortés). **366 tests** (344 L1+L2 incl. 10 E2E + 2 soak + 16 L3 + 4 L4), CI green, **save format v19**, git clean.

**This session: begin the foreign-European-powers / multi-player wave** (umbrella `86d3b7qwm`), the biggest architectural change in the project. It is **planned and decomposed — do not re-plan it.** Read **ADR-019** (Architecture doc `2kz0t3mf-736`) for the Player/Nation model. Key decisions already made (with Chris): a `Player` owns player-scoped state (identity, gold/tax, **its own per-player Market**, liberty/Congress, immigration/recruit-dock, explored fog, later stance/tension); `Game` keeps the global world (map, ruleset, turn, RNG, native settlements, **global** id counters) + a `Players` list + ring-buffer per-player turn iteration; units/colonies stay flat global lists referenced by an **owner id**; the human is found via `HumanPlayer`, **never by index**; **one PCG RNG stream per player** (human keeps stream 0 so existing seeded tests/goldens stay byte-stable, ADR-009); European nations are **variant data** (ADR-018). AI is minimal (a flat per-unit priority switch, **not** FreeCol missions); diplomacy = stance/tension primitives only. **Deferred:** REF / War of Independence (Phase 6), full AI missions, rich diplomacy/treaties, foreign naval combat (`86d3bek5r`).

**The 7 slices (kanban, do in order — each lands all tests green + docs + CI):**
1. **FP-1 `86d3bex4a` — Extract `Player` (single human, ZERO behaviour change).** ← *start here.* Move player-scoped state off `Game` onto a new `sealed class Player`; `Game` holds one human `Player` + `HumanPlayer`; keep the public `Game` surface via thin pass-through props (`Gold`/`Market`/`Congress`/`Explored`/… → `HumanPlayer.X`, marked human-only) so presentation + the 366 tests barely change; mutating seams get internal `Player`-taking overloads; collapse the 23-param `Restore` to a `SavedPlayer`-record list (one element). Save **v20** (optional `Players[]`; a v19 save folds the flat top-level fields into one human player; native-owned units stay native; keep `WhenWritingNull` so a lone-human save stays compact). New test `V19Save_LoadsAsSingleHumanPlayer`; `LoadedGame_ContinuesIdenticalRandomSequence` stays green. New doc `docs/systems/players.md` + `save-load.md`. **Pure refactor — diff is large but mechanical; behaviour provably unchanged.**
2. **FP-2 `86d3bex4f`** — owner-id seam: add `Unit.OwnerId`/`Colony.OwnerId`; convert the `IsNative != attacker.IsNative` enemy/fog tests to owner-inequality + a stubbed stance hook; resolve `AbilityForOwner`/`AutoEquipRoleScopes` via the owning player's congress. Still human+native only.
3. **FP-3a `86d3bex4j`** parse European nations into Ruleset (data only) · **FP-3b `86d3bex4p`** multi-player `Game` scaffolding + ring-buffer turn, spawning the 3 European powers **inert** (no AI) + natives as `Player` rows; save v20 persists real `Players[]` + reserves per-player RNG streams.
4. **FP-4 `86d3bex4u`** AI explore/move/found (flat priority switch, own RNG stream, stable by-id iteration; fixed-seed L2 test). 5. **FP-5 `86d3bex4w`** AI economy (trade/immigration/recruit; per-player market exercised). 6. **FP-6 `86d3bex51`** AI combat + diplomacy basics (stance/tension; unblocks de Witt/Franklin; new `docs/systems/diplomacy.md`). 7. **FP-7 `86d3bex56`** save-format v20 consolidation + back-compat sweep (provably lossless migration).

**Then:** the deferred Founding-Father effects (`86d3b7qxr`); Phase 6 (independence & REF) → 7 (polish) → 8 (Australia variant = author a data set + register a `GameVariant`, no engine rewrite — the point of ADR-018).

**How to work here (matters most):**
- **Slice discipline:** do FP-1 *first and alone* — the whole de-risking depends on the pure-refactor slice landing green before any nation/AI exists. Don't start FP-2+ until FP-1 is merged.
- **Docs are part of the change** (no-drift): a slice isn't done until its `docs/systems/<x>.md` (both layers + verification + changelog), `save-load.md`, the module docs, `QA-REPORT.md` counts, the kanban status, the Session Log, and `PostWorkSummary.md` (with the **scheduled-next** item) are updated *in the same work*.
- **Determinism (ADR-009):** all randomness via injected `IGameRandom`; **per-player streams** (human = stream 0); no `Random`/unordered-collection iteration in AI code.
- **Transposability (ADR-018):** European nations + colony names read ruleset data, not hard-coded as American-specific.
- **Verify FreeCol behaviour** against `freecol/` (`Player.java`, the server turn loop, `Stance`/`Tension`, the `<nation>`/`<european-nation-type>` spec) and pin numbers in tests.
- **Process used this phase (worked well):** per substantive slice, a research workflow (parallel readers over FreeCol + our code) → implement → an adversarial review workflow → fix → commit. (Skip the review for pure-data/parse slices already pinned by tests.)
- **Definition of done = tests green at every required layer + docs synced + CI green.** Logic: `dotnet test game/tests/GameLogic.Tests/GameLogic.Tests.csproj`. Scene (L3/L4): `dotnet test game/CrownAndColony.csproj --settings game/gdunit.runsettings` after a clean `godot --build-solutions` (+ `GODOT_BIN`). Toolchain: dot-source `scripts/dev-env.ps1`. Chris has authorised **commit & push to `main` per slice**.

**Awaiting Chris's playtest (don't mark done):** the In Review kanban items (FreeCol art, colony screen, economy UI, Europe screen, native settlements). Combat has **no UI yet** (logic + tests only). Launch the game if asked: `& "C:\Users\Chris\Tools\Godot_v4.6.3-stable_mono_win64\Godot_v4.6.3-stable_mono_win64.exe" --path "C:\Users\Chris\Code\Colonization\game"` (launch detached — `Start-Process` — or the harness reaps it).

Start by reading the latest Session Log + ADR-019 + the FP-1 task, confirm the build + tests are green, then implement **FP-1 (Extract `Player`)**.
