# Module: CrownAndColony.GameLogic

| | |
|---|---|
| **Last verified** | 2026-06-16 @ colony capture/pillage/plunder + defence bonus + La Salle + besiege + human defeat |
| **Location** | `game/src/GameLogic/` |
| **Layer** | GameLogic (engine-free) |
| **Depends on** | nothing (BCL only) |
| **Used by** | `CrownAndColony` (Godot presentation project), `GameLogic.Tests` |

## Purpose

The entire rules engine of Crown & Colony: every game rule, calculation, and state transition. It is explicitly **not** responsible for anything visual, audible, or input-related — that's the Godot presentation project.

**The defining constraint (ADR-006):** this project must never reference Godot (no `Godot.NET.Sdk`, no `GodotSharp`). That keeps the whole rules engine testable with plain xUnit, headless, in milliseconds. The csproj carries a comment to that effect; treat adding a Godot dependency here as a build-breaking offence.

## Public API

| Type / member | What it does |
|---|---|
| `Randomness.IGameRandom` | The only permitted randomness source (ADR-009) |
| `Randomness.Pcg32Random` | Deterministic PCG32 implementation; `FromState()` resumes saves |
| `Randomness.RandomState` | Serializable generator snapshot |
| `Specification.Ruleset` | Parsed rule data; `LoadClassic()` / `LoadEmbedded(resource)` / `Load(Stream)`; `Terrain(id)`, `Unit(id)` |
| `Specification.GameVariant` / `GameVariants` | Selectable game variant (id, name, ruleset loader) + registry (`ClassicAmerica`, `All`, `Default`, `ById`, `Resolve`) — transposability backbone (ADR-018) |
| `Specification.TerrainType` / `ProductionEntry` / `GoodsOutput` / `GenRanges` | Immutable terrain rule data incl. climate envelopes + combat `DefenceBonus` |
| `Specification.UnitType` | Unit rule data (movement, sight, `RecruitProbability`, `IsPerson`, `Space`/`SpaceTaken`/`IsCarrier`/`CarrySlots`, `Price`/`IsPurchasable`, `Offence`/`Defence`, `MaxHitPoints` (spec `hit-points`; drives ship repair time), combat-ability flags `DisposeOnCombatLoss`/`CanBeCaptured`/`CaptureUnits`/`CaptureEquipment`/`DisposeOnAllEquipmentLost`/`DemoteOnAllEquipmentLost`/`Bombard`) with `extends` inheritance resolved. **Data-driven `<ability>` map** (`Abilities` id→bool, resolved down `extends`) consulted via `HasAbility("model.ability.X")` (FreeCol `hasAbility`); `IsNaval` (`navalUnit`), `CanFoundColony` (`foundColony`), `CaptureGoods` (`captureGoods`, naval cargo loot), `Piracy` (`piracy`, attack without war + hidden flag), `CarryTreasure` (`carryTreasure`), `ExpertScout` (`expertScout`) are convenience reads off it — toggleable in variant data, classic values unchanged |
| `Specification.RoleType` / `RoleRequiredGoods` / `RoleChange` | Military role/equipment rule data: offence/defence, required-goods, downgrade, granted/required abilities, equipment-capture (`role-change`) rules; `Ruleset.Role(id)`/`Roles`/`CaptureRole(winner, loser, native)` |
| `Specification.UnitChange` / `UnitChangeTypeIds` | Unit-type transitions (promotion/demotion/capture: from→to + probability); `Ruleset.GetUnitChange(changeType, fromId)` |
| `Combat.CombatModel` / `AttackContext` / `DefenceContext` / `CombatResult` / `MovementPenalty` | Pure combat model: attack/defence power, win odds (`att/(att+def)`), graded resolution (`Resolve`) + naval resolution with an evade band (`ResolveNaval`); a `−12.5%/slot` cargo penalty (`GoodsCarried`). Roles fold into the scalar base power passed in. |
| `Combat.CombatNotice` | A transient record of a native raid on the human during `EndTurn` (attacker nation id, defender unit-type id, `CombatResult`, position) — collected on `Game` (`CombatNotices`), never saved; the presentation formats it (ADR-006) |
| `Combat.ColonyLossNotice` | A transient record of a human colony captured by a foreign power during `EndTurn` (attacker nation id, colony name, position) — collected on `Game` (`ColonyLossNotices`), never saved; the presentation formats it (ADR-006, 1c-3f) |
| `Combat.ColonyRaidNotice` | A transient record of a human colony pillaged by a native brave during `EndTurn` (attacker nation id, colony name, **goods id — or null for a gold raid**, amount, position) — collected on `Game` (`ColonyRaidNotices`), never saved; the presentation formats it (ADR-006, native pillage) |
| `Specification.GoodsType` | Goods rule data: `is-food`, `stored-as`, `made-from`, `is-storable` (warehouse-storable; gates native colony pillage to tradeable stacks — hammers/liberty aren't lootable), breeding number, market seed |
| `Specification.BuildingType` | Building rule data: conversions (with inputs), workplaces, upgrade chain, build cost, defence bonus (stockade/fort/fortress `model.modifier.defence`) |
| `Specification.ResourceType` / `ResourceModifier` | Bonus-resource yield modifiers (goods, type, index, unit-type scopes); finite-quantity range (`MinValue`/`MaxValue`/`HasQuantityRange`/`RollQuantity`) |
| `World.Improvements.TileImprovementType` / `ImprovementModifier` / `ImprovementProduction` / `ImprovementMovement` | Tile-improvement (river) rule data + pure production-delta (`YieldDelta`) and river follow-cost (`RiverMoveCost`/`ReducedCost`) rules; parsed via `Ruleset.Improvement(id)`/`RiverType`/`ImprovementTypes` |
| `Specification.NativeNationType` / `SettlementType` / `SettlementPlunder` / `NativeSkill` / `SettlementNumber` / `NativeAggression` | Native nation + settlement rule data (templates, counts, aggression, taught skills, defence + `<plunder>` ranges); `Ruleset.NativeNation(id)`, `Settlement(id)`, `SettlementType.PlunderRange` |
| `Natives.NativeSettlement` / `Natives.AlarmLevel` | A placed native settlement: nation type, settlement type, capital flag, position, size, taught skill, + interaction state (**per-player alarm** `AlarmFor(playerId)`/`AlarmChannels` keyed by `Player.PlayerId`, the human as id 0; `MostHated` (FreeCol `getMostHated`); the legacy `Alarm`/`AlarmLevel` = the human's channel; visited, skill-consumed) |
| `World.NativeSettlementGenerator` | Seeded native-settlement placement (capital-first, min-distance, suitability); `AssignWantedGoodsTo` also finishes imported-map settlements with wanted goods |
| `World.Position` | Grid coordinate; 8-way adjacency |
| `World.GameMap` | Terrain grid + bonus resources (`ResourceAt`) + finite resource quantities (`ResourceQuantityAt`) + natural improvements/rivers (`ImprovementAt`/`HasRiver`/`Improvements`) + regions |
| `World.MapGenerator` | Seeded climate-band map generation + mountain ranges + rivers (`MakeRivers`) + resource placement |
| `World.MapImporter` / `MapImportResult` | Deterministic, engine-free importer of a map definition (terrain grid + optional `[resources]`/`[improvements]`/`[rumours]`/`[settlements]` overlay sections) into a `GameMap` + initial `NativeSettlement`s (FreeCol `FreeColMapLoader` subset). `FixedMap` (America loader) delegates to it (`TryLoad`/`TryImport`); a terrain-only definition imports byte-identically. `Game.New` installs an import's `[settlements]` verbatim (skipping the procedural generator) when the map declares any |
| `GameSession.Player` / `PlayerType` / `RestoredPlayer` | A player and its player-scoped state (ADR-019): identity (`PlayerId`/`NationId`/`IsHuman`/`PlayerType` {Colonial, Native}), `Gold`/`TaxRate`, its **own** `Market`, liberty/`Congress`/`CurrentFather`/`OfferedFathers`, immigration/`RecruitDock`/`RecruitPrice`, `Explored` fog, and its own RNG stream (`RngStreamId`/`Rng`). `RestoredPlayer` is the load-time DTO. Mutated via `Game` |
| `Units.Unit` / `UnitLocation` | Unit state: map/sailing/Europe `Location`, `SailTurnsRemaining`, `Cargo`, `CarrierId`/`IsAboard`, `IsOnMap`, `OwnerNationId`/`IsNative` (native owner), `OwnerId` (colonial owner; the human is 0), `RoleId`/`RoleCount`/`HasDefaultRole` (equipment); mutated only via `Game` |
| `Colonies.Colony` | Colony state: `OwnerId` (colonial owner; the human is 0), population, stores, tile/building workers, buildings, build target |
| `Trade.Market` | A **per-player** European market: per-good bid/ask, supply-driven `Sell` with tax (FreeCol price model), `SaveDeltas`/`LoadDeltas` |
| `Specification.GoodsMarket` | Per-good market seed (initial amount/price/spread) |
| `Specification.FoundingFather` / `FatherType` / `FatherModifier` / `FatherAbility` / `ModifierType` / `ModifierMath` | Founding-father rule data: category, age weights, the modifiers (+ unit-type `ScopeUnitTypes`/`AppliesTo` — Drake's privateer scope) + abilities an election grants + `FreeBuildings` (La Salle's free-stockade `model.event.freeBuilding`) |
| `GameSession.Game` | The running game. Players (ADR-019): `Players`, `HumanPlayer`, `CurrentPlayer` (the ring pointer); player-scoped state is reached through `HumanPlayer` (the public no-arg `Gold`/`Market`/`Congress`/… pass through to the human, each mutating seam has an internal `Player`-taking overload). The human + 3 foreign colonial powers + native nations are `Player` rows; foreign powers run their own AI economy (sell/recruit/father) + unit AI in `EndTurn`, and native nations run a raid/wander AI (`RunNativeTurn`, slice 1b — braves attack the human's units when alarmed, resolved via the internal `Attack` overload), each on its own RNG stream/market so the human's stream 0 stays byte-stable. Map/units: `New`, `CheckMove`/`MoveUnit`, `EndTurn`, `SpawnUnit`, `CheckFoundColony`/`FoundColony`, `TileYield`. Colony work: `AssignWork`/`UnassignWork`, `TileWorkOptions` (a tile's producible goods + yields, for the colony screen's per-tile picker), `AssignBuildingWork`/`UnassignBuildingWork`, `SetBuild`/`Buildables`, `JoinColony`/`LeaveColony`. Trade: `Gold`, `TaxRate`, `Market`, `SellColonyGoods`/`SellShipCargo`/`BuyEuropeGoods`, `BuyUnit`/`CheckBuyUnit`. Europe/sailing: `SailToEurope`/`SailToNewWorld`, `UnitsInEurope`. Transport: `Board`/`Disembark`/`DisembarkToDock`, `Passengers`, `CargoCapacity`/`CargoSlotsUsed`/`CargoSlotsFree`. Fathers: `Liberty`, `Congress`, `ChooseFather`, `OfferedFathers`, `HasAbility`, `ApplyGoodsModifiers`. Immigration: `Immigration`/`ImmigrationRequired`, `RecruitDock`, `RecruitPrice`, `Recruit`/`CheckRecruit`. Natives: `NativeSettlements`, `NativeSettlementAt`, `ChangeNativeAlarm`, `Visit`/`CheckVisit`, `LearnSkill`/`CheckLearnSkill`, `SellToNatives`/`CheckSellToNatives`/`NativeSalePrice`, `PendingDemand`/`AcceptPendingDemand`/`RefusePendingDemand` (a native tribute demand the human pays or refuses — transient, never saved). Combat: `PlayerUnits`/`NativeUnits`, `CheckAttack`/`Attack`, `CheckAttackSettlement`/`AttackSettlement`, `CheckAttackColony`/`AttackColony`, `CheckPillageColony`/`PillageColony` (native colony pillage), `ColonyDefenceBonus` (a colony's stockade/fort/fortress defence %), `CheckEquipRole`/`EquipRole`, `EffectiveCombatRole`, `CombatNotices` (transient AI-raid notices) + `ColonyLossNotices` (AI colony-capture, 1c-3f) + `ColonyRaidNotices` (native colony-pillage), `IsHumanDefeated` (no human colonies + no human units). Fog: `Explored`/`IsExplored`, `CurrentlyVisible`/`IsVisible`. (All checks have a `Check…` oracle, ADR-006.) |
| `GameSession.MoveCheck` / `InvalidMoveException` | Move legality result / violation |
| `Persistence.SaveGame` / `SavedUnit` / `SavedColony` / `SavedResource` / `SavedResourceQuantity` / `SavedImprovement` / `SavedWorker` / `SavedNativeSettlement` / `SavedPlayer` | Complete JSON-serializable game snapshot (format **v47** — `SaveGame.CurrentVersion` is authoritative; `SavedImprovement` = a tile's river/improvement, v47; per-player state is the **sole** source in a `Players[]` array — gold/tax/per-player market/liberty/Congress/immigration/dock/RNG/explored/diplomacy stance+tension; per-unit + per-colony owner ids; per-unit `RepairTurns` (v21, ship repair — additive, omitted when 0); records its game variant, native interaction/trade state, per-unit role; a sacked/captured settlement is simply absent. The legacy flat top-level player fields are no longer written as of FP-7 — kept read-only so ≤v19 saves still fold into one human player) |
| `App.SettingsModel` / `App.WindowMode` | Pure **application** settings (video: window mode + vsync; audio: master/music/SFX volumes, linear 0–1) with defaults, `Clamp` (validate/normalise), and string-dictionary round-trip. App config — **not** game rules — kept here only so it gets L1 coverage; the Godot `SettingsService` autoload maps it to `user://settings.cfg` and the engine (see [settings.md](../systems/settings.md)) |

(Grows as systems land; keep this table current.)

## Key design notes

- `TreatWarningsAsErrors` and nullable reference types are on; keep them on.
- `GenerateDocumentationFile` is on — public members without XML doc comments fail the build, which mechanically enforces the documentation rule for APIs.
- Target: `net8.0` (matches Godot 4.6's .NET target). Tests roll forward to the installed runtime.

## Tests

`game/tests/GameLogic.Tests/` — xUnit, mirrors this project's folder structure. **527 tests** (523 L1+L2 incl. 10 E2E journeys + 4 nightly soak), all green as of 2026-06-16. (Scene/visual L3+L4 live in the Godot project — see [presentation.md](presentation.md); 558 across all five layers.)

## Changelog

| Date | Change | Commit |
|---|---|---|
| 2026-06-22 | **Per-player native tension** (`86d3e4bfb`, save **v55**): `NativeSettlement.Alarm` became a per-player map (`AlarmFor(playerId)`/internal `SetAlarm`/`AlarmChannels`, the human as channel 0; FreeCol `IndianSettlement.getAlarm(Player)`), + `MostHated` (`int?`, FreeCol `getMostHated`, derived). `Game.ChangeNativeAlarm` gains a `(settlement, playerId, delta)` overload (2-arg → human channel); all interaction/combat/land hooks key on the acting player. Decay cools every channel; ambient pressure + raid/demand AI + reset stay human-channel (faithful). Save `SavedNativeSettlement.AlarmChannels` (additive, omit-when-default); pre-v55 single scalar migrates into channel 0. One-power game byte-identical to the old scalar; multi-power soak twin-run + round-trip byte-identical (no new RNG). +11 L1/L2 (`NativeAlarmChannelsTests`). See [natives.md](../systems/natives.md). | Phase 5 (`86d3e4bfb`) |
| 2026-06-22 | **AI arms colonists + fortifies its garrison** (`86d3e49cm`): `RunForeignPowerTurn` wiring (no new mechanics) — a new arm step (before the found-colony branch) arms an idle plain colonist in an under-defended own colony to the strongest affordable military role via `BestAffordableMilitaryRole` (prefers `model.role.dragoon` over `model.role.soldier`) + the existing `EquipRole`, and the FP-5 garrison branch now `GarrisonFortify`s (existing `Fortify`/`CheckFortify`) a guard on its colony tile. `RunForeignPowerEconomy`'s sell loop gains `MilitaryReserveFor` (keeps the muskets/horses the arm step needs from the surplus sell, the counterpart of the pioneer-tools reserve). All new members are `private`/RNG-free → human stream 0 byte-identical (ADR-009, soak twin-determinism green); **no save change**. +8 L1 (`ForeignDefenceArmingTests`). See [players.md](../systems/players.md) | Phase A1 (`86d3e49cm`) |
| 2026-06-21 | **Data-driven unit abilities** (`86d3drpgg`, ABILITIES slice): `UnitType` gains an `Abilities` map (parsed from each type's `<ability>` tags, resolved down `extends` via new `Ruleset.CollectAbilitiesUpChain`) + `HasAbility(id)` consult (FreeCol `hasAbility`). Six capabilities migrated from hardcoded parsed-into-named-field booleans to `HasAbility` reads — `IsNaval`/`CanFoundColony`/`CaptureGoods`/`Piracy`/`CarryTreasure`/`ExpertScout` are now computed properties — so a variant can toggle them in data; classic resolved values byte-identical (no save bump). Other unit abilities (`person`, combat-outcome flags, `bombard`) + building abilities keep the per-ability `ResolveAbility` path for now. +2 L1 (`RulesetTests.UnitTypeAbilities_ParseFromSpecIntoHasAbilityMap`, `DataDrivenUnitAbilities_MatchClassicValues`). See [ruleset-data.md](../systems/ruleset-data.md) | EPIC (`86d3drpgg`) |
| 2026-06-20 | **Rivers** (`86d3b3qdx`): `Ruleset` parses `<tile-improvement-types>` (`RiverType`/`Improvement`/`ImprovementTypes`); `MapGenerator.MakeRivers` stamps rivers at game start; `GameMap` improvement layer (`ImprovementAt`/`HasRiver`/`Improvements`/`SetImprovement`); `Game.TileYieldPotential` + `Game.CheckMove` fold the river yield bonus + follow-cost; save **v47** (`SavedImprovement`/`Improvements`, omitted when none). Also wired the finite-resource-quantity roll (`86d3c9wbp`) into `Game.New`. 6 L4 goldens regenerated (stream-0 reorder); 21 version-guard tests retargeted 46→47. +11 L1 (`RiverTests`, `GameNew_RollsResourceQuantities`). See [rivers-tile-improvements.md](../systems/rivers-tile-improvements.md), [save-load.md](../systems/save-load.md) | P5 (`86d3b3qdx`/`86d3c9wbp`) |
| 2026-06-20 | **Emigration choice seam** (`86d3c9xft`, FreeCol `selectRecruit`): new `Game.Emigration.cs` partial — `PendingEmigrationChoice` record + `PendingEmigration` oracle + `ChooseEmigrant(slot)`. A tiny guard in `AccumulateImmigrationAndEmigrate` makes a **human with William Brewster** (`model.ability.selectRecruit`) **pause** on a due emigrant (set `_pendingEmigration`, no RNG drawn) instead of auto-emigrating a random slot; `ChooseEmigrant` then runs the same `Emigrate`+`ReduceImmigration`+target-raise and re-arms for a backlog. Every other player (no Brewster, all AI) is byte-identical (same RNG stream). **In-memory only** (no save bump); a reloaded game re-pauses next turn. +5 L1 (`EmigrationChoiceTests`). See [immigration.md](../systems/immigration.md) | P7 wave-2 D (`86d3c9xft`) |
| 2026-06-20 | **Player history log** (`86d3c9x53`, FreeCol player `HistoryEvent`): new `Game.History.cs` partial — an **in-memory** `Game.History` list of `HistoryEvent(HistoryEventKind, Turn, Description)` recording the human's notable events (colony founded / war declared / father elected). The engine appends from three tiny seams: `FoundColony` (human-owned colony), `SetStance`→War (human is a party; recorded once, on the transition into war), and the election point of `AccumulateLibertyAndElectFathers` (human). **Not persisted** (no save bump this wave — a saved log is a follow-up); a reloaded game's history starts empty, no determinism impact (no RNG, additive). Read by the presentation's History report tab. +4 L1 (`HistoryTests`). See [presentation.md](presentation.md) | P7 wave-2 D (`86d3c9x53`) |
| 2026-06-18 | **App settings model** (`App.SettingsModel`/`App.WindowMode`): pure video/audio app settings — defaults, clamp, string-dictionary round-trip — backing the title/settings front-end's `SettingsService` autoload (app config, not game rules; lives in GameLogic only for L1 coverage). +5 L1 `SettingsModelTests`. See [settings.md](../systems/settings.md) | a804549 |
| 2026-06-16 | **Sons of Liberty bell upkeep**: `AccumulateLibertyAndElectFathers` now banks **net** bells (gross − `max(0, pop − 2)`, FreeCol `unitsThatUseNoBells`=2) to both the colony's liberty and the player's founding-father pool (floored at 0) — a colony that outgrows its bell output loses liberty (SoL falls) and a growing colony must staff its town hall to keep electing fathers. +2 L1; Journey 4 re-derived. See [sons-of-liberty.md](../systems/sons-of-liberty.md) | Phase 5 (Sons of Liberty) |
| 2026-06-16 | **Simón Bolívar founding father**: +20 Sons-of-Liberty % to every one of his player's colonies (`model.modifier.SoL`). Implemented as a standing modifier — `Colony.SolModifierBonus` (derived from the owner's Congress, not persisted) folds into `Colony.SonsOfLiberty` after the liberty→% conversion; `Game.RefreshSonsOfLibertyModifiers` re-derives it on election/founding/load (so it survives population changes, unlike a one-time liberty bake). Zero regression (0 for non-Bolívar players). +5 L1 `FoundingFatherEffectsTests`. See [founding-fathers.md](../systems/founding-fathers.md) | Phase 5 (founding fathers) |
| 2026-06-16 | **Sons of Liberty (Slice B)**: `Colony.ProductionBonus` now adds to each attended worker's output — `RunColonyTurn` tile workers + `RunBuildingProduction` building workers, floored at 0 (the unattended colony square + town-hall bell excluded). Zero churn on the 579 existing tests + soak (bonus is 0 below goodGovernment, which every test colony is). +2 L1 application tests. Bell upkeep still deferred. See [sons-of-liberty.md](../systems/sons-of-liberty.md) | Phase 5 (Sons of Liberty) |
| 2026-06-16 | **Per-colony Sons of Liberty (Slice A)**: `Colony.Liberty` (stored, save v22) + computed `SonsOfLiberty`/`RebelCount`/`ToryCount`/`ProductionBonus` (FreeCol formulas; medium-difficulty limits 6/10; integer-only, RNG-free); `AddLiberty` (floor-0 + 100%-SoL cap). `Game.AccumulateLibertyAndElectFathers` now banks the same FF-modified bell figure to both the colony's liberty and the player's founding-father pool. **Bonus computed but not applied** to output (zero economy churn). +25 L1 `SonsOfLibertyTests`. See [sons-of-liberty.md](../systems/sons-of-liberty.md) | Phase 5 (Sons of Liberty) |
| 2026-06-13 | Project created with Randomness namespace | Phase 0 scaffold |
| 2026-06-13 | Phases 1–3: ruleset parsing, map/units/turns/save, fog, colonies + full economy (stores, tile/building work, construction, growth) | Phases 1–3 |
| 2026-06-13 | Phase 4: market+treasury, founding fathers + effects (modifier/ability system), Europe + high-seas sailing, immigration & recruitment, unit transport, bonus-resource yields, colonist join/leave; save v13 | Phase 4 |
| 2026-06-13 | Phase 5 slice 1: native nation + settlement rule data, `NativeSettlement` domain + seeded placement (`NativeSettlementGenerator`), save v14 | Phase 5 slice 1 |
| 2026-06-13 | Variant/game-mode selection layer (`GameVariant`/`GameVariants`, `Ruleset.LoadEmbedded`); variant-aware saves (v15) — transposability backbone (ADR-018) | Phase 5 (variant layer) |
| 2026-06-13 | Explored-vs-visible fog: `CurrentlyVisible`/`IsVisible` (units + colonies), colony reveal on founding | Phase 5 (fog upgrade) |
| 2026-06-14 | Native interaction: alarm model (`AlarmLevel`, `ChangeNativeAlarm`, turn decay), `Visit` (tales + gift), `LearnSkill` (unit upgrade); save v16 | Phase 5 slice 3 |
| 2026-06-14 | Native trade: sell cargo to a coastal settlement (`SellToNatives`/`NativeSalePrice`), wanted goods per settlement; save v17 | Phase 5 slice 4 |
| 2026-06-14 | Combat foundation: parse unit offence/defence + terrain defence bonus; pure `CombatModel` (power, odds, graded resolution) | Phase 5 slice 5a |
| 2026-06-14 | Combat 5b: unit ownership (`OwnerNationId`, `PlayerUnits`/`NativeUnits`) + roles/equipment (`RoleType`, `UnitChange`, `EquipRole`), brave defenders, attack action (`CheckAttack`/`Attack`) with FreeCol loser/winner outcome precedence + native alarm, Washington/Revere; save v18 | Phase 5 slice 5b |
| 2026-06-14 | Combat 5c: native settlement assault (`CheckAttackSettlement`/`AttackSettlement`, `ComputePlunder`, `SettlementPlunder`) — implicit-garrison defence, destroy + plunder gold, +500/+600 tension with sibling propagation, Cortés; can't move onto a settlement; save v19 | Phase 5 slice 5c |
| 2026-06-14 | Foreign-powers wave FP-1→FP-5 (ADR-019): extracted `Player` (player-scoped state incl. its own `Market`); owner-id seam (`Unit.OwnerId`/`Colony.OwnerId`; enemy/fog/abilities resolve by owner + a stance hook); parsed European nations as ruleset data; multi-player `Game` (human + 3 foreign powers + natives as `Player` rows; ring-buffer `EndTurn`); foreign-power AI — land/found/explore (FP-4) then a per-player economy — trade/immigration/recruit/father (FP-5), each on its own RNG stream; save **v20** (`Players[]`, per-player markets/RNG, owner ids; ≤v19 folds to one human) | FP-1…FP-5 |
| 2026-06-15 | FP-6a/6b: diplomacy — per-colonial-pair `Stance`/tension on `Player` (`StanceBetween`/`SetStance`/`ChangeTension`); contact→Peace, attack→War, decay, and the tension→stance machine (`StanceFromTension`/`UpdateColonialStances`: war→cease-fire→peace). Recorded only (no legality/AI-action change). See [diplomacy.md](../systems/diplomacy.md) | FP-6a/6b |
| 2026-06-15 | Native AI (slice 1b): `RunNativeTurn` (braves raid the human's units when their home settlement is Displeased+, else wander; one action/turn; nation's own RNG stream — human stream 0 byte-stable), `StepToward` pathing helper, `Combat.CombatNotice` + `Game.CombatNotices` transient raid feed, `CaptureUnit` capture-owner hardening. No save-format change. See [natives.md](../systems/natives.md) | Phase 5 slice 1b |
| 2026-06-15 | Slice 1c-1: `CheckMove` blocks moving onto a colony you don't own (`ColonyAt(target).OwnerId != unit.OwnerId`). Logic-only (the rival/own-unit rendering is presentation — see [presentation.md](presentation.md)); no save/RNG change. See [combat.md](../systems/combat.md), [units-movement.md](../systems/units-movement.md) | Phase 5 slice 1c-1 |
| 2026-06-15 | Slice 1c-2: `RunForeignPowerTurn` war branch + `AttackHumanUnit` — a power at `War` with the human sends its armed units after the human's nearest unit, combat via the internal `Attack(…, RandomFor(power))` overload (own stream; `CombatNotice` raised). Dormant at peace; no save/stream-0 surface. See [combat.md](../systems/combat.md), [diplomacy.md](../systems/diplomacy.md) | Phase 5 slice 1c-2 |
| 2026-06-15 | Pocahontas founding-father effect: `ResetAllNativeAlarm` on election (human-gated) + `ScaleNativeAlarmGain` (−50% spec-read `nativeAlarmModifier`) at `ApplyNativeCombatTension` (gains only). Rides on the persisted Congress; no save/RNG change. Franklin deferred (European-diplomacy father). See [founding-fathers.md](../systems/founding-fathers.md) | Phase 5 (#3 native fathers) |
| 2026-06-15 | Ferdinand Magellan: `InitialMovement` folds the owner's Congress `movementBonus` for naval units (+3); `SailTurnsFor(owner)` applies `sailHighSeas` (−1, floored at 1) on departure. Spec-read, naval-gated, rides on the Congress (no save/RNG change). See [founding-fathers.md](../systems/founding-fathers.md), [units-movement.md](../systems/units-movement.md) | Phase 5 (#3 fathers) |
| 2026-06-15 | Naval combat (1c-3a, sink-only): `CombatModel.ResolveNaval` (evade band) + cargo penalty (`GoodsCarried`/`CargoSlotsUsed`); `Game.Attack` naval branch (no terrain on water); `SinkShip` (carrier cascade — fixes a latent passenger orphan); foreign warships attack at war (`NearestHumanUnit` domain-matched; the naval skip narrowed). No save-format change (v20). See [combat.md](../systems/combat.md) | Phase 5 slice 1c-3a |
| 2026-06-15 | Naval cargo loot (1c-3c): `UnitType.CaptureGoods` (spec `model.ability.captureGoods`); `Game.LootShip(winner, loser)` plunders the beaten ship's hold (up to free space, 100/slot) at the top of the naval loser branch, before sink/damage. Deterministic; no save change (rides `Cargo`). See [combat.md](../systems/combat.md) | Phase 5 slice 1c-3c |
| 2026-06-15 | Privateer piracy (1c-3d-i): `UnitType.Piracy` (spec `model.ability.piracy`); `Game.Attack` suppresses the colonial War/tension hook when attacker or defender has piracy; `Game.UnknownEnemyNationId` sentinel hides a foreign privateer's flag in its `CombatNotice`. Deterministic; no save change. See [combat.md](../systems/combat.md), [diplomacy.md](../systems/diplomacy.md) | Phase 5 slice 1c-3d-i |
| 2026-06-15 | Francis Drake (1c-3d-ii): `FatherModifier.ScopeUnitTypes`/`AppliesTo` (`ParseModifier` reads `<scope>`); `Game.FatherCombatFactor` folds the owner's scoped `model.modifier.offence`/`defence` into `OffenceBase`/`DefenceBase` (Drake = +50%/+50% for privateers). Per-owner, deterministic, no save change. See [founding-fathers.md](../systems/founding-fathers.md) | Phase 5 slice 1c-3d-ii |
| 2026-06-15 | Colonial-colony capture (1c-3e): `Colony.OwnerId` made settable; `Game.CheckAttackColony`/`AttackColony`/`CaptureColony` — a land unit assaults an ungarrisoned rival colony (transient unarmed-colonist defender), a win flips `OwnerId` (people/buildings/stores transfer), a loss disarms the attacker; act of war recorded first. Deterministic; no save change (OwnerId already serialized). See [combat.md](../systems/combat.md) | Phase 5 slice 1c-3e |
| 2026-06-15 | Foreign-AI colony capture (1c-3f): `RunForeignPowerTurn` war branch + `AdjacentCapturableHumanColony`/`CapturePlayerColony` — a power at War captures an adjacent undefended human colony via the internal `AttackColony(…, RandomFor(power))` (priority over the unit-hunt); new transient `Combat.ColonyLossNotice` + `Game.ColonyLossNotices` feed (cleared each `EndTurn`, never saved). Deterministic (power's own stream); no save change. See [combat.md](../systems/combat.md) | Phase 5 slice 1c-3f |
| 2026-06-15 | Native colony pillage: `Game.CheckPillageColony`/`PillageColony`/`PillageableGoods`/`AdjacentPillageableHumanColony` — a hostile brave beside an undefended pillageable human colony steals `min(amount/2, 50)` of one uniformly-picked goods stack (`Colony.AddGoods`); `RunNativeTurn` pillages before the unit-hunt (nation's own stream); new transient `Combat.ColonyRaidNotice` + `Game.ColonyRaidNotices` feed. Deterministic; no save change. Goods-loot subset (gold/building/ship/destroy deferred). See [combat.md](../systems/combat.md), [natives.md](../systems/natives.md) | Phase 5 native pillage |
| 2026-06-16 | Colony fortification defence bonus: `BuildingType.DefenceBonus` (parses `model.modifier.defence`, skips `delete="true"`); `Game.ColonyDefenceBonus`/`ColonyDefenceBonusAt` shield a colony's defender (garrison or transient last colonist) in `Attack`/`AttackColony`/`PillageColony` via `DefenceContext.SettlementDefenceBonus` (stockade +100 / fort +150 / fortress +200). Deterministic; no save change. See [combat.md](../systems/combat.md) | Phase 5 colony defence bonus |
| 2026-06-16 | Native tribute demands: `GoodsType.IsMilitary`/`IsTradeGoods` + derived `Ruleset.BuildingMaterials`; `Natives.NativeDemand` record; `Game.PendingDemand`/`SelectDemand`(internal)/`CreateNativeDemand`(internal)/`AcceptPendingDemand`/`RefusePendingDemand`/`AdjacentDemandableHumanColony`; `RunNativeTurn` raises a demand (after pillage) against a colony it can't storm; `EndTurn` auto-refuses an ignored demand. Transient `PendingDemand` — **no save change** (v21); RNG-free (stream 0 byte-stable). See [natives.md](../systems/natives.md) | Phase 5 native tribute |
| 2026-06-16 | Native-pillage gold-steal: `Game.PillageColony`'s win branch picks over the goods stacks **+ a gold option** (when `HumanPlayer.Gold > 0`) — steals `min(max(1, ColonyPlunderAmount/5), gold)` off the human; `Combat.ColonyRaidNotice.GoodsId` now nullable (null = gold). Gold-plunder draw on the nation's stream; no save change. See [combat.md](../systems/combat.md) | Phase 5 native pillage |
| 2026-06-16 | `Game.TileWorkOptions(tile)` — a tile's attended producible goods + yields (sorted desc), a pure rules query feeding the colony screen's per-tile work picker (assign a colonist to lumber/ore/cotton/… not just food). No save/RNG change. See [colonies.md](../systems/colonies.md) | Phase 5 colony UI |
| 2026-06-16 | La Salle free building: `FoundingFather.FreeBuildings` (parses `model.event.freeBuilding`); `Game.ApplyFreeBuildings(player)` (each turn after election + growth) grants every elected father's free buildings to each owned colony meeting the required population — La Salle → a free stockade at pop 3. RNG-free, idempotent; no save-format change (rides the colony building list). See [founding-fathers.md](../systems/founding-fathers.md) | Phase 5 (La Salle) |
| 2026-06-16 | Colony capture plunder: `Game.PlunderColony`/`ColonyPlunderAmount` in the `AttackColony` win branch transfer `rnd[0,upper]+1` gold (FreeCol `getPlunderRange`, capped at the victim's purse) from the former owner to the captor, before the handover. Drawn on the captor's stream (human stream 0 / foreign power's own — stream-0-safe). No save change (gold already serialized). See [combat.md](../systems/combat.md) | Phase 5 capture plunder |
| 2026-06-16 | Besiege fallback (`86d3bx03d`): `Game.NearestHumanColony` + a `RunForeignPowerTurn` war-branch step — a war-power land unit with no field prey marches on the nearest human colony (`StepToward`) instead of exploring. Additive (only when `NearestHumanUnit` is null); power's own stream; no save change. See [players.md](../systems/players.md) | Phase 5 (besiege) |
| 2026-06-16 | Human defeat (`86d3bx04e`): `Game.IsHumanDefeated` (computed — no human colonies and no human units anywhere; conservative subset of FreeCol `checkForDeath`); `GameController.OnEndTurnPressed` surfaces it as a HUD defeat message. Pure/computed, no save-format change. See [players.md](../systems/players.md) | Phase 5 (human defeat) |
| 2026-06-15 | Ambient native alarm (slice 5): `Game.ApplyAmbientNativeAlarm` (in `EndTurn` before decay) raises each settlement's alarm from the human's nearby colonies (`2 + population`) + offensive land units (offence) within `radius + 2`; Pocahontas's −50% (`ScaleNativeAlarmGain`) relocated off `ApplyNativeCombatTension` (now raw) onto this ambient gain. RNG-free; no save change. See [natives.md](../systems/natives.md) | Phase 5 slice 5 |
| 2026-06-15 | Ship damage + repair (1c-3b, save **v21**): `ResolveLoserOutcome(…, greatLoss)` branches `DamageShip` vs `SinkShip` (great loss / no repair location sinks, else damage); `DamageShip` drops cargo+passengers and limps the hull to Europe with `RepairTurnsRemaining = max(1, MaxHitPoints−1)`; `AdvanceRepairs` (in `EndTurn`) heals one/turn; `IsUnderRepair` ships pinned to 0 moves and barred from sail/buy/board (`SailToNewWorld`/`CheckBuyEuropeGoods`/`CheckBoard`). `UnitType.MaxHitPoints` parses spec `hit-points`. Fully deterministic (no RNG); `SavedUnit.RepairTurns` additive (omitted when 0). See [combat.md](../systems/combat.md) | Phase 5 slice 1c-3b |
| 2026-06-15 | FP-7: save-format v20 consolidation — the legacy flat top-level player fields are no longer written (`Players[]` is the sole source); format version unchanged; flat properties kept read-only for ≤v19 fold + pre-FP-7 v20 back-compat | FP-7 |
