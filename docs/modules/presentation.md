# Module: CrownAndColony (Presentation)

| | |
|---|---|
| **Last verified** | 2026-06-22 @ `NegotiationPanel` (`86d3c9xpt`/`86d3c9ubw`) — the human diplomacy / negotiation dialog + the scout-enters-rival-colony mission menu (Spy / Negotiate) + a HUD **Diplomacy** button, all over the shipped diplomacy backend (`PendingHumanProposals`/`TakePendingHumanProposals`, `EvaluateTrade`, `SettleTrade`, `ForeignNationStances`, `StanceBetween`, `CheckSpyOnColony`/`SpyOnColony`). Presentation-only (ADR-006) — no rules, no RNG, **no save bump**. +3 L3 (`NegotiationPanelTests`) green; build clean. Prior: `DeclarationPanel` (`86d3c9xht`) — the Declaration-of-Independence signing screen (eligible-gated **Independence** HUD action → confirm-consequences → forwards `Game.DeclareIndependence` + shows the signed declaration; reads `CheckDeclareIndependence`/`NationalSonsOfLiberty`/`UnitsInEurope`; +2 L3 green, no save bump) **and** the `NewGameDialog` **game-options section** (`86d3drn64`) — a **Victory conditions** group (defeat the REF / last European / last human, `gameOptions.victoryConditions`), pre-ticked to parsed spec defaults, forwarded via `GameController.PendingVictoryConditions` → the new config seam `Ruleset.WithVictoryConditions` so `Game.Winner` honours them (fog-of-war + other `gameOptions.map` toggles NOT surfaced — engine doesn't read them yet; session-only, no save bump; +1 L1). Plus the `SoundService` autoload + 8 license-clear FreeCol SFX (`86d3c9xrp`). Prior: `HighScoresPanel`/`HighScoresService` (`86d3c9xb1`) — persisted top-10 leaderboard to `user://highscores.json` (separate from the save → **v52 unchanged**); +3 L3, +23 L1. |
| **Location** | `game/presentation/`, `game/scenes/` (project: `game/CrownAndColony.csproj`) |
| **Layer** | Presentation (Godot) |
| **Depends on** | `GameLogic`, Godot 4.6 |
| **Used by** | — (top of the stack) |

## Purpose

Everything the player sees and touches: scene tree, drawing, camera, input, UI. It forwards commands to `GameLogic` and reflects its state. It must contain **no game rules** (ADR-006) — if a conditional encodes "what's allowed in the game", it belongs in `GameLogic` and this layer calls `CheckMove`-style oracles instead.

## Key parts

| Part | What it does |
|---|---|
| `GameController` (root of `scenes/main.tscn`) | Owns the `Game` + the selected `GameVariant` (new game loads its ruleset; saves record it; loads restore under the save's variant — ADR-018); input → commands (click select/move, **click an adjacent enemy/settlement to attack** via `AttackUnitAt`/`AttackSettlementAt`, B found — which raises a **buy/steal/abandon land-claim dialog** (`PromptLandClaim`→`FoundColonyWithClaim`) when founding on native-owned land, `86d3e4bj7`, E Europe, F5/F9 save/load); **all keyboard shortcuts now flow through one authoritative key table** (`KeyBindings`/`BuildKeyBindings`, `86d3f0vjg`) that drives both `_UnhandledInput` dispatch and the **F1 keys legend** so they can't drift — Enter end-turn, Space skip-unit, D disband, Ctrl+C centre, Ctrl+S/Ctrl+O save/load (the PauseMenu dialog path), plus the existing W/G/B/E/L/F/C/N; a hotkey is suppressed while a `LineEdit`/`TextEdit` owns focus (`IsTextInputFocused`); a **right-click tile menu** (`HandleRightClick`) activates a stacked unit / centres / arms goto; opens the colony + Europe panels; shows the date in a dedicated `UI/CalendarPanel/CalendarLabel` (reads `Game.CalendarLabel`) near the turn controls; a **Reports** button opens the empire colony report (`OpenColonyReportPanel`); a **Colopedia** button / **C** key opens the in-game ruleset reference (`OpenColopediaPanel`); after End Turn, surfaces what the human suffered or received during the AI phase by formatting `Game.CombatNotices` (raids, via `FormatCombatNotice`), **`Game.ColonyRaidNotices`** (colonies pillaged by a brave, via `FormatColonyRaidNotice` → "⚔ The X raided Y and carried off N goods!" — or "…made off with N gold!" when the notice's `GoodsId` is null), **`Game.ColonyLossNotices`** (colonies seized by a power at war, via `FormatColonyLossNotice` → "⚑ The X captured your colony Y!") **and `Game.CustomHouseSaleNotices`** (custom-house auto-sales, via `FormatCustomHouseSaleNotice` → "💰 Your custom house in X sold N goods for G gold.") — each formatted to a player-facing row and routed into the dismissible **`TurnMessagePanel`** (`_turnMessagePanel.Open(messages)`, FreeCol `ReportTurnPanel`), **not** the status bar; presentation-only string-building from raw ids (`NationLabel` shared; slices 1b / native pillage / 1c-3f); exported `Seed` for deterministic test runs. **Owner-gated for multi-player (FP-4):** `SyncColonyMarkers` fog-gates colony markers (a foreign colony stays hidden until a human unit discovers its tile), and tile-click, the HUD subject line, and the camera focus resolve the human **by owner** — so the human can't manage or centre on a foreign power's colony |
| `ColonyTheme` | The colony screen's cohesive parchment/wood `Theme` (cached, ADR-006 presentation-only), assigned to `ColonyPanel` in `Open` so it cascades to every child — wood-toned buttons/dropdowns, a bordered `BuildingCell` `PanelContainer` variation, dark-ink labels with a light halo, engraved separators, and a `ColonyTitle`/`SectionHeader` size/colour hierarchy. Leaves the `panel` stylebox to the parchment override; bundles **Cardo** (SIL OFL — `assets/fonts/`) as the default font so every screen renders in a period serif (Slice D) |
| `ColonyArt` | Loads + caches the FreeCol art the colony screen draws (terrain diamonds, the settlement sprite, colonist sprites, **building images** via a camelCase-id→file map, **goods icons** for the production/warehouse bars, and the **`PanelParchment` skin** `ui/bg_paper_brown.png`) from `res://assets/freecol/{terrain,settlements,units,buildings,goods,ui}` — kept separate from `MapView` so the map's golden-tested rendering is untouched (terrain base/overlay mapping mirrors it; unify later) |
| `SelectedUnitPanel` (`UI/SelectedUnitPanel`) | A small top-left HUD readout of the **selected unit** (`86d3dmej6`, a sub-slice of the map-controls panel `86d3c9x6y`): type / `moves left/max` / role / orders / "building &lt;improvement&gt; (N)" / "going to", set by `GameController.RefreshView` (`DescribeSelectedUnit`) from `Unit` reads. Below the line, **standing-orders buttons** (`86d3dmh6b`) — **Fortify** / **Sentry** / **Wake** — **pioneer build buttons** (`86d3dqr62`) — **Road** / **Plow** / **Clear** — a **Sail to Europe** button (bug fix), a **Cash in treasure** button (`86d3f62q1`), a **Skip** button (Space, `86d3f0vuy`) and a **Disband** button (D, `86d3f0vgd`) call `Game.Fortify`/`Sentry`/`ClearOrders` / `BuildImprovement` / `SailToEurope` via `ApplyUnitOrder`, plus `CashInSelectedTreasureTrain` (a `ConfirmationDialog` framed as the **King's offer** at a colony — "the King will carry … and take {`Game.TaxRate`}% ({`Game.TreasureKingsCut`}g); you keep {`CashInValue`}g. (Carry it home yourself to keep all …)" — or the full fee-free, tax-free amount in Europe via `Game.TreasureCashInIsFeeFree`, then `Game.CashInTreasureTrain`), `SkipSelectedUnit` (flags the session-only skip set + cycles on) and `DisbandSelectedUnit` (a `ConfirmationDialog` → `Game.Disband`), each gated/disabled on its `Check…` oracle (Wake disabled when active and not building; the build buttons on `CheckBuildImprovement`; **Sail to Europe** visible only for a naval unit and enabled only on a high-seas tile, `CheckSailToEurope` — the only UI surface for *departing* to Europe, see [europe.md](../systems/europe.md); **Cash in treasure** visible only for a treasure-carrying unit and enabled only when `CheckCashInTreasureTrain` allows it where the train stands — at an owned colony or aboard a galleon docked in Europe — the only UI surface for cashing in, see [treasure-train.md](../systems/treasure-train.md), a refusal lands in the status bar; **Disband** on `CheckDisband`). Skip + Disband also have keyboard paths — see [hud-input](../systems/hud-input.md). Shown only while a unit is selected; the panel + label are `mouse_filter = Ignore` so only the buttons take clicks (no map-click steal). Presentation-only (ADR-006) |
| `TileInfoPanel` (`UI/TileInfoPanel`) | A small top-left HUD **tile-info readout** (FreeCol's `InfoPanel` tile-info, `86d3c9x6y`): describes the **last-clicked map tile** — terrain short name, bonus resource (if any), and the occupant ("Your colony: …", a discovered "<nation> settlement", or an on-map unit's type), built by `GameController.DescribeTile(Position)` from `Game` oracles (`IsExplored`, `Map.TerrainAt`/`ResourceAt`, `ColonyAt`, `NativeSettlementAt`, on-map unit lookup). Fog-gated: an unexplored tile reads only "Unexplored". `HandleTileClick` records the clicked tile (`_inspectedTile`); the readout is **empty/hidden until a tile is clicked** (so the camera-centred visual goldens are unaffected) and cleared on new/loaded game. Panel + label are `mouse_filter = Ignore` (no map-click steal). Presentation-only (ADR-006) |
| `MapView` | Isometric tile drawing with FreeCol terrain art (ADR-014, 128×64 diamonds); `ShowState(map, explored, visible)` — unexplored black, explored-but-unseen dimmed, visible full bright; tile↔pixel conversions: `TileCentre(p)` (forward projection) and the inverse pick `TileAt(local)` (a **true point-in-diamond** test — inverse-isometric rounding, not a bounding box) plus `TileAtScreen(viewportPosition)` (maps an input event's screen position through `GetGlobalTransformWithCanvas().AffineInverse()` — camera zoom/pan-correct — then `TileAt`; the accurate picking seam the click handlers use, `86d3f675b`). Child layers (drawn in order over the terrain `_Draw`): `RiverLayer` (`RiverOverlay`), `ImprovementLayer` (`ImprovementOverlay`), `NativeLayer`, `ColonyLayer`, `RumourLayer`, `UnitLayer`, `GotoMarker` |
| `RiverOverlay` (`MapView/RiverLayer`) | The **river map overlay** (`86d3b3qdx`): draws `GameMap`'s river improvement layer as connected blue courses over the terrain, no imported art. `ShowState(map, explored, visible)` (called from `GameController.RefreshView` beside `MapView.ShowState`) → each explored river tile draws a spoke from its diamond centre to the midpoint toward every adjacent **explored** river tile (the neighbour draws the matching half → a continuous course), a dark rim under a water fill (two passes), and a small pool for a lone river tile. **Fog-gated** like the terrain (skips unexplored, dims remembered). Drawn small/large **style is derived at render time** from connected-component length (≥4 tiles ⇒ large/thick), never a stored field (ADR-006, no save bump). A `Node2D` (no input). First under `MapView`, so above terrain and below settlements/units. See [rivers-tile-improvements.md](../systems/rivers-tile-improvements.md) |
| `ImprovementOverlay` (`MapView/ImprovementLayer`) | The **pioneer-built improvement overlay** (`86d3dqr62`), sibling of `RiverOverlay` for the man-made layer: draws `GameMap`'s **road** and **plowed-field** improvements over the terrain, no imported art. `ShowState(map, explored, visible)` (called from `GameController.RefreshView` beside `MapView`/`RiverOverlay`) reads `GameMap.HasImprovement(p, RoadId/PlowId)`. **Roads** draw like rivers — each road tile spokes to the midpoint toward every adjacent **road** tile (a connecting earthen-brown track, dark rim under the surface; a hub disc for a lone segment; roads connect only to roads in the drawing — the road↔river *movement* interconnect is a rules concern, not visual). **Plowed fields** draw as a few parallel diagonal furrows inset in the diamond (no connectivity). **Fog-gated** like the terrain (skips unexplored, dims remembered); render-time only, **no stored style/save bump** (ADR-006). A `Node2D` (no input). **Second** under `MapView`, so above the river course and below settlements/units/markers. A fresh game has no pioneer improvements → draws nothing on an untouched map (**zero golden regen**). See [rivers-tile-improvements.md](../systems/rivers-tile-improvements.md) |
| `UnitMarker` | **Role-aware** FreeCol unit sprite (`SetUnit(type, role)` loads the first existing of the candidate paths from the engine-free `UnitSpriteCatalog.CandidatePaths` — `units/{role}/{type}.png` → `units/{role}/{role}.png` → `units/{type}.png` → a red-disc fallback — so a colonist in the soldier/pioneer/scout/… role draws as that role, and a **native `brave`** draws its FreeCol art: bare `brave`/`indianConvert`/`nativeColonist` for the unarmed/convert/captured forms, and `armedBrave`/`mountedBrave`/`nativeDragoon` role folders for the equipped braves — never the red disc) + selection ring + an **owner ground-ring** (`OwnerColor`) marking a non-yours unit; one per drawn unit, reconciled into `UnitLayer` by `GameController.SyncUnitMarkers` — the human's own on-map units always, every non-human unit (a foreign power's or a native brave) only while its tile is in live sight (`Game.IsVisible`). The human's own units pass `default` (no ring); foreign units use their `EuropeanNation.Color`, natives a constant (1c-1). The path-priority rule itself lives engine-free in `GameLogic` (`Specification/UnitSpriteCatalog`) so it's L1-unit-tested (`UnitSpriteCatalogTests`) without booting Godot |
| `ColonyMarker` | FreeCol settlement sprite + name plate, one per colony |
| `RumourMarker` | A small **drawn glyph** (an earthy mound + a gold glint — no imported art) marking an explored Lost City Rumour tile (`86d3dm9tq`). `GameController.SyncRumourMarkers` places one per explored rumour tile (`GameMap.Rumours` fog-gated by `Game.IsExplored`, the `SyncNativeMarkers` pattern) into `MapView/RumourLayer`. Presentation-only (ADR-006); a `Node2D` so it never consumes input. See [lost-city-rumours.md](../systems/lost-city-rumours.md) |
| `NativeSettlementMarker` | FreeCol indian-settlement art (camp/village/Inca/Aztec) + nation plate (capitals starred); one per discovered settlement (`GameController.SyncNativeMarkers`, fog-gated) |
| `CameraController` | Drag pan (right/middle mouse) + wheel zoom + **arrow-key pan** (continuous in `_Process`, scaled by zoom; WASD deliberately avoided — letter hotkeys) (`86d3f0vqf`). On a **right** press it starts a possible drag-pan and tracks whether the motion passed a 4 px threshold; on release it consumes the event (`SetInputAsHandled`) only if it actually dragged, so a genuine pan never reaches `GameController` — a drag-free right-click falls through so the tile context menu opens (`86d3f0vrz`). See [hud-input](../systems/hud-input.md) |
| `GotoMarker` (`MapView/GotoMarker`) | A hollow gold diamond outlining the selected unit's standing **go to** destination tile (`86d3c9xjy`). `GameController` arms goto-target mode on the **G** key (`EnterGotoMode`); the next map click forwards to `SetSelectedDestination` → `Game.CheckSetDestination`/`SetDestination`, and `RefreshView` positions + shows this marker when the selected unit has a `Destination`. The per-turn walk (`ProcessGotos`) is GameLogic (ADR-006). |
| `MiniMap` (`UI/MiniMap` in `scenes/main.tscn`) | A corner overview map (FreeCol `MiniMap`, `86d3c9x64`): draws the explored world as scaled flat-colour terrain cells (a `ShortName`→`Color` palette; remembered tiles dimmed; unexplored stays dark) with colony / native-settlement / unit **dots**, **fog-respecting** (a cell or dot only shows once its tile is explored/visible). `ShowState(Game)` reads the fog sets + entity positions (ADR-006 — never mutates). A left-click recenters the main camera (`TileSelected` → `GameController.CenterCameraOnTile` → `Camera.Position`); the **+/-** buttons (`ZoomIn`/`ZoomOut`, `PixelsPerTile` steps) zoom. Bottom-left corner, clear of the top-left status line and bottom-right control stack. The scaled map is centred in the panel (zoom clips when it overflows) |
| `ColonyPanel` | The colony screen, laid out after FreeCol's in a near-fullscreen panel with FreeCol's **brown parchment skin** (`EnsureOpaqueBackground` → a cached tiled `StyleBoxTexture`, solid-fill fallback — opaque so the map never bleeds through), built programmatically per open/refresh. Bands top-to-bottom: a title + summary line (population / idle / food→growth / defence); a full-width **production bar** of net-production goods icons (`NetProduction`: tile yields + colony-centre auto-yield − food eaten); a compact **middle row** of `[LeftColumn | BuildingsColumn]` — the left a fixed-width band with the **Sons-of-Liberty bar** (`SonsOfLibertyBar` — Rebels/Population/Royalists + SoL% + production bonus + a membership meter), the **isometric tile view** (`TilesView`, 160×80 FreeCol diamonds — settlement centre, a colonist + yield on each worked tile, **drawn as its real unit type** via `WorkerSprite(Colony.WorkerTypeAt(tile))` so an expert looks like one, `86d3f674f`; **click-to-move** worker management via a per-tile `Tile_x_y` hit button, plus a *✕ Release* and a *Work…* good picker via `Game.TileWorkOptions` + `AssignWorkWithClaim` — which raises the **buy/steal/abandon land-claim dialog** when the tile is native-owned, `86d3e4bj7`), and the **construction** panel (the full **build queue** — each item's image + `BuildCost` progress + per-row ▲/▼ reorder + ✕ remove — an **Add to queue** `OptionButton` listing both buildings *and* buildable units, and a *Clear queue*; each queued id resolved via `Game.DescribeBuildable` so a queued unit renders correctly); the right an `ExpandFill` **buildings grid** (`BuildingsGrid`, 4-wide, `ShrinkCenter` — each cell a `ColonyArt.BuildingImage` + `workers/workplaces` + **per-slot worker portraits** over `Colony.BuildingOccupants` (one sprite per occupant drawn as its real type, each a click-to-remove `Worker_{building}_{slot}` button; empty workplaces faintly marked, `86d3f6754`) + `+`/`−` staff/unstaff); then an **outside-the-colony** row (one per on/adjacent unit: *Join colony* → `JoinColony`; *Arm soldier/dragoon/scout/pioneer* + *Disarm* → `EquipRole`; *Send a colonist out* → `LeaveColony`); a full-width **warehouse bar** of stored-goods icons + counts; and a **`ProductionOverview`** — a per-good `Production_{good}` table (icon · name · +produced · −consumed · net) over the tested `Game.ColonyProductionSummary` oracle, FreeCol's colony-panel production summary showing the full produce/consume/net breakdown incl. building inputs/outputs + bells/crosses/hammers (`86d3f674t`). `WorkerSprite(typeId)` resolves `units/<type>.png` via `UnitMarker.ResolveTexture` (default role) with a `ColonyArt.UnitIcon` fallback. Sons-of-Liberty / Rebels / Royalists deliberately omitted (no per-colony liberty data; ADR-006). All actions via Game oracles (ADR-006). **Drag-and-drop worker assignment** (`86d3f6dnd`, additive over the clicks): an **Idle colonists** row (`IdleColonists`) of draggable portrait chips (`IdleColonist_{index}`, each a `WorkerSprite` portrait wrapped in `EuropeDragSource` carrying a `ColonyDrag.Worker(index)` payload — see `ColonyDragDrop.cs`); each tile's `Tile_x_y` click button is wrapped in an `EuropeDropTarget` (`TileDrop_x_y`, gated on `CheckAssignWork` of the tile's best good, dropping through the **shared** `AssignWorkWithClaim` so native land raises the same buy/steal/abandon modal); each building cell's content sits in an `EuropeDropTarget` (`BuildingDrop_{building}`, gated on `CheckAssignBuildingWork` → `AssignBuildingWork`). Both re-check inside `_DropData` (a worked tile / full building / dockless sea tile is a silent no-op). The drag is additive — every click button stays the accessibility/L3 fallback; no engine/save change. Named controls (`Tile_x_y`/`TileDrop_x_y`/`Release_x_y`/`Work_x_y`/`Staff_*`/`Unstaff_*`/`Worker_*_*`/`BuildingDrop_*`/`IdleColonist_*`/`Join_*`/`Equip_*`/`Disarm_*`/`LeaveColony`/`BuildOptions`/`StopBuild`/`Up_i`/`Down_i`/`RemoveBuild_i`/`ProductionOverview`/`Production_*`) preserved for the L3 tests |
| `EuropePanel` | The Europe screen, rebuilt from a flat text list into a **zoned harbour** layout (`86d3f6…` Phase 1 click/button + **Phase 2 drag-and-drop & ship picker**), in a near-fullscreen scrolling panel with the shared **brown parchment skin** (`EnsureOpaqueBackground`, the `ColonyPanel` pattern) and `ColonyTheme`. A centred content card holds, top-to-bottom: a **header** (treasury + next-recruit price in the static label) and an **immigration clock** drawn as a `ProgressBar` (`ImmigrationBar`, `Immigration`/`ImmigrationRequired`); a `[recruit/train/purchase | goods-market]` row; a **ships-in-port** zone; a **sail-to-the-New-World** zone; and an **on-the-docks** zone. **Recruit/train/purchase** (`RecruitTrainPurchaseZone`): the 3 dock slots (`Recruit_{slot}`); a **Train a specialist** list (`Game.UnitTypesTrainedInEurope()` → `Train_{shortName}` → `Game.TrainUnit(string)`, flat price); a **Purchase** list (`Game.UnitTypesPurchasedInEurope()` → `Purchase_{shortName}` → `Game.BuyUnit(string)`, artillery escalating) — this is the **train/purchase routing split** (specialists train, ships/artillery buy), each button labelled with `Game.EuropeUnitPrice(string)` and **greyed** (`GatedButton`) when its `Check*` oracle refuses. **Goods market** (`GoodsMarketZone`): every tradeable good with live **ask** + a **boycott** flag (`Market.CanTrade`/`Arrears`), trading through the first non-repairing in-port ship — a `BuyGood_{shortName}` **`GatedButton`** (100-lot at `Market.BuyCost`, `Game.BuyEuropeGoods`; greyed with a reason tooltip when `CheckBuyEuropeGoods` refuses rather than vanishing — findings #5/#7) and a `Sell_{shipId}_{shortName}` button per stack aboard, **labelled + gated from `Game.CheckSellShipCargo(...).Cost` — the after-tax proceeds**, never the old raw `BidPrice × amount` (findings #1/#8, the press re-checks); a boycotted good shows its `boycotted (arrears N)` state, not a bogus sell price (finding #4, the dead identical-arms ternary removed); read-only (prices only) when no ship is in port. **Ships in port** (`ShipsInPortZone`): each ship a card with its hold's **slots drawn out** (`HoldSlots` — an `HBox` of `CargoCapacity` boxes, filled slots labelled with the good ×amount / passenger, empty slots blank — replacing the old "hold 1/2" text; the per-stack slot count from `Game.GoodsSlotsFor(amount)`, not a reimplemented `(amount+99)/100` literal — finding #3), passengers with **Put on dock** (`Unload_{id}` → `DisembarkToDock`) and the fee-free `CashIn_{id}` (treasure aboard a galleon docked in Europe); a ship **under repair** shows its repair countdown and no sail/trade controls. **Sail** (`SailZone`): a `Sail_{id}` button per ready ship (`SailToNewWorld`). **On the docks** (`DocksZone`): waiting colonists with a `Board_{person}_{ship}` button per ship with room (`Game.Board`), and carried-home **treasure trains** with the fee-free `CashIn_{id}`. All actions guarded (the press re-checks the oracle so a stale button no-ops) and routed through Game oracles (ADR-006). **Phase 2 (`86d3f6…`) — drag-and-drop & ship picker (additive over the buttons):** each zone wraps its draggables in `EuropeDragSource` and its drop zones in `EuropeDropTarget` (see below). Five drops: dock colonist `DockColonist_{id}` → ship `ShipDrop_{shipId}` (Board), ship `ShipDrag_{shipId}` → `SailDrop` (Sail), market good `MarketGood_{short}` → ship card (Buy a 100-lot), hold lead-slot `Cargo_{shipId}_{short}` → `GoodsMarketDrop` (Sell the stack), aboard `Passenger_{id}` → `DocksDrop` (Disembark). Sail/Disembark re-check inline state-only guards (`CanSailToNewWorld`/`CanDisembarkToDock`, mirroring the engine command's own throws — no `Check*` oracle exists for them); the other three re-check `CheckBoard`/`CheckBuyEuropeGoods`/`CheckSellShipCargo`. A **ship picker** (`Select_{id}` → `EuropePanel.SelectTradeShip`, public for the L3) chooses which in-port ship the goods market trades through (`_selectedShipId`, marked ▶/"Selected", cleared when the ship leaves), defaulting to the first non-repairing ship; **UI-only session state, no save change**. Named controls preserved for the L3 tests |
| `ColonyPanel` | The colony screen, laid out after FreeCol's in a near-fullscreen panel with FreeCol's **brown parchment skin** (`EnsureOpaqueBackground` → a cached tiled `StyleBoxTexture`, solid-fill fallback — opaque so the map never bleeds through), built programmatically per open/refresh. Bands top-to-bottom: a title + summary line (population / idle / food→growth / defence); a full-width **production bar** of net-production goods icons (`NetProduction`: tile yields + colony-centre auto-yield − food eaten); a compact **middle row** of `[LeftColumn | BuildingsColumn]` — the left a fixed-width band with the **Sons-of-Liberty bar** (`SonsOfLibertyBar` — Rebels/Population/Royalists + SoL% + production bonus + a membership meter), the **isometric tile view** (`TilesView`, 160×80 FreeCol diamonds — settlement centre, a colonist + yield on each worked tile, **drawn as its real unit type** via `WorkerSprite(Colony.WorkerTypeAt(tile))` so an expert looks like one, `86d3f674f`; **click-to-move** worker management via a per-tile `Tile_x_y` hit button, plus a *✕ Release* and a *Work…* good picker via `Game.TileWorkOptions` + `AssignWorkWithClaim` — which raises the **buy/steal/abandon land-claim dialog** when the tile is native-owned, `86d3e4bj7`), and the **construction** panel (the full **build queue** — each item's image + `BuildCost` progress + per-row ▲/▼ reorder + ✕ remove — an **Add to queue** `OptionButton` listing both buildings *and* buildable units, and a *Clear queue*; each queued id resolved via `Game.DescribeBuildable` so a queued unit renders correctly); the right an `ExpandFill` **buildings grid** (`BuildingsGrid`, 4-wide, `ShrinkCenter` — each cell a `ColonyArt.BuildingImage` + `workers/workplaces` + **per-slot worker portraits** over `Colony.BuildingOccupants` (one sprite per occupant drawn as its real type, each a click-to-remove `Worker_{building}_{slot}` button; empty workplaces faintly marked, `86d3f6754`) + `+`/`−` staff/unstaff); then an **outside-the-colony** row (one per on/adjacent unit: *Join colony* → `JoinColony`; *Arm soldier/dragoon/scout/pioneer* + *Disarm* → `EquipRole`; *Send a colonist out* → `LeaveColony`); a full-width **warehouse bar** of stored-goods icons + counts; and a **`ProductionOverview`** — a per-good `Production_{good}` table (icon · name · +produced · −consumed · net) over the tested `Game.ColonyProductionSummary` oracle, FreeCol's colony-panel production summary showing the full produce/consume/net breakdown incl. building inputs/outputs + bells/crosses/hammers (`86d3f674t`). `WorkerSprite(typeId)` resolves `units/<type>.png` via `UnitMarker.ResolveTexture` (default role) with a `ColonyArt.UnitIcon` fallback. Sons-of-Liberty / Rebels / Royalists deliberately omitted (no per-colony liberty data; ADR-006). All actions via Game oracles (ADR-006). Named controls (`Tile_x_y`/`Release_x_y`/`Work_x_y`/`Staff_*`/`Unstaff_*`/`Worker_*_*`/`Join_*`/`Equip_*`/`Disarm_*`/`LeaveColony`/`BuildOptions`/`StopBuild`/`Up_i`/`Down_i`/`RemoveBuild_i`/`ProductionOverview`/`Production_*`) preserved for the L3 tests |
| `EuropePanel` | The Europe screen, rebuilt from a flat text list into a **spatial harbour** layout (`86d3f6…` Phase 1 click/button + **Phase 2 drag-and-drop & ship picker** + the **spatial two-column sprite/icon re-layout**), in a near-fullscreen scrolling panel with the shared **brown parchment skin** (`EnsureOpaqueBackground`, the `ColonyPanel` pattern) and `ColonyTheme`. The whole panel sits over the FreeCol **harbour backdrop** (`ColonyArt.HarbourBackdrop()` = `ui/colonydocks.png`, sky+sea, set stretched by `BuildPanelBackground` with a parchment→solid fallback); the content sits on an **opaque parchment backing** (`CardBackingStyle` on a `ContentBacking` `PanelContainer`) so text stays readable over the scene. A centred content card holds, top-to-bottom: a **header** ("Europe — the harbour" + treasury/next-recruit label) and an **immigration clock** drawn as a `ProgressBar` (`ImmigrationBar`, `Immigration`/`ImmigrationRequired`); a two-column `TopRow` of **bordered titled cards** (`Card`/`CardStyle`) — `[recruit/train/purchase | goods-market]`; a **ships-in-port** zone; an **in-transit** zone (`InTransitZone`, two `Card`s: *Expected soon* = `Game.ShipsSailingToEurope`, *Bound for the New World* = `Game.ShipsSailingToNewWorld`, each a ship sprite + "arrives in `{SailTurnsRemaining}` turn(s)" — read-only, no buttons); a **sail-to-the-New-World** zone; and an **on-the-docks** zone. The zones draw **real FreeCol art** (sprites/icons), not text rows: `UnitSprite` (= `UnitMarker.ResolveTexture(type, "default")` ?? `ColonyArt.UnitIcon`, the colony screen's portrait lookup), `ColonyArt.GoodsIcon`, and `IconRect`/`UnitPortrait`/`Display(short)` helpers. Every cell is sized to its content with padding so nothing clips in the 17px-bold in-game theme. **Recruit/train/purchase** (`RecruitTrainPurchaseZone`): the 3 dock slots as **portrait cards** (colonist sprite + name + `Recruit_{slot}`); a **Train a specialist** + a **Purchase** **icon grid** each (`UnitButtonGrid` — a 3-col `GridContainer` of `Button`s, each a sprite + price with the full name in the tooltip; `Game.UnitTypesTrainedInEurope()` → `Train_{shortName}` → `Game.TrainUnit(string)` flat price, `Game.UnitTypesPurchasedInEurope()` → `Purchase_{shortName}` → `Game.BuyUnit(string)` escalating) — the **train/purchase routing split**, each `GatedButton` greyed when its `Check*` oracle refuses (replaces the old long vertical text lists). **Goods market** (`GoodsMarketZone`): each tradeable good a **row with its goods icon** (`ColonyArt.GoodsIcon`) + name + `BidPrice/AskPrice` + a **boycott** flag (`Market.CanTrade`/`Arrears`), trading through the first non-repairing in-port ship — a `BuyGood_{shortName}` **`GatedButton`** (100-lot at `Market.BuyCost`, `Game.BuyEuropeGoods`; greyed with a reason tooltip when `CheckBuyEuropeGoods` refuses) and a `Sell_{shipId}_{shortName}` button per stack aboard, **labelled + gated from `Game.CheckSellShipCargo(...).Cost` — the after-tax proceeds**; a boycotted good shows its `boycotted (arrears N)` state; read-only when no ship is in port. **Ships in port** (`ShipsInPortZone`): each ship a bordered **card** (`CardStyle`, selected highlighted) = the ship **sprite** + name + `hold X/Y` + its hold's **slots drawn out** (`HoldSlots` — an `HBox` of `CargoCapacity` boxes: a filled goods slot shows the **goods icon + amount**, a passenger slot a **portrait**, an **empty slot a faint parchment outline** (`SlotStyle(filled:false)`, not the old near-black box); the per-stack slot count from `Game.GoodsSlotsFor(amount)`, not a reimplemented literal), passengers as `Passenger_{id}` chips with **Put on dock** (`Unload_{id}`) + fee-free `CashIn_{id}`; a ship **under repair** shows its countdown, no sail/trade. **A single CLICK** on a ship card selects it as the trade ship and a **drag** of the card sails it — both on the card's `ShipDrag_{id}` source (`EuropeDragSource.OnClick` → `SelectTradeShip`), the old `Select_{id}` button removed. **Sail** (`SailZone`): a visually distinct **bordered drop panel** (`SailZoneStyle`, arrow/label) with a `Sail_{id}` fallback button per ready ship (`SailToNewWorld`). **On the docks** (`DocksZone`): a wrapping `HFlowContainer` of colonist **portrait chips** — each the `DockColonist_{id}` drag source (portrait + name) + a `Board_{person}_{ship}` button per ship with room (`Game.Board`); carried-home **treasure trains** as chips + fee-free `CashIn_{id}`. All actions guarded (the press re-checks the oracle so a stale button no-ops) and routed through Game oracles (ADR-006). **Phase 2 — drag-and-drop & ship picker (additive over the buttons):** each zone wraps its draggables in `EuropeDragSource` and its drop zones in `EuropeDropTarget` (see below); the standalone drop zones (`ShipDrop_*`/`SailDrop`/`DocksDrop`) are laid out via a `DropZone(drop, body)` helper that makes the drop target the **PARENT** of its body (`EuropeDropTarget.SetContent` anchors the body FullRect + adopts its combined min size) so the body's buttons are the Pass target's **descendants** — reachable by real mouse clicks — while a drag-hover over the card's non-button decorations (made `MouseFilter.Ignore`) still reaches the target (a `MouseFilter.Pass` later-sibling overlay would forward the click to the parent, NOT down to the button beneath it, leaving every button dead to real clicks — the BLOCKER `86d3f6ddfix` fixed; the goods market's clean `GoodsMarketDrop` was always a parent-of-buttons, which is why it worked). Five drops: dock colonist `DockColonist_{id}` → ship `ShipDrop_{shipId}` (Board), ship `ShipDrag_{shipId}` → `SailDrop` (Sail), market good `MarketGood_{short}` → ship card (Buy a 100-lot), hold lead-slot `Cargo_{shipId}_{short}` → `GoodsMarketDrop` (Sell the stack), aboard `Passenger_{id}` → `DocksDrop` (Disembark). Sail/Disembark re-check inline state-only guards (`CanSailToNewWorld`/`CanDisembarkToDock`); the other three re-check `CheckBoard`/`CheckBuyEuropeGoods`/`CheckSellShipCargo`. A **ship picker** (a **single click** on the ship card's `ShipDrag_{id}` source → `EuropePanel.SelectTradeShip`, public for the L3; the old `Select_{id}` button removed) chooses which in-port ship the goods market trades through (`_selectedShipId`, marked ▶/"(trading)", cleared when the ship leaves); **UI-only session state, no save change**. **Readability fix (`86d3f6…`):** the content cards are a **light parchment** fill with a wood/parchment edge (palette `CardParchment` `#E8D9B0` / `CardParchmentDark` `#D9C290` / `CardEdge` `#C2A86A` / `CardEdgeWood` `#7A4F30`, matching `ColonyTheme`'s `BuildingCell`) so the dark theme `Ink` reads — `CardBackingStyle`/`SlotStyle(filled)`/`CardStyle`/`SailZoneStyle` were dark brown/blue ⇒ dark-on-dark unreadable (Chris's playtest); empty hold-slots keep the faint outline, `SailZoneStyle` a pale slate-blue `#D7E0EA` + blue border so the departure gate stays distinct, the harbour backdrop showing in the margins/gaps. The audit also lightened the drag preview (`EuropeDrag.Preview` → light box + explicit dark `font_color`) and darkened `Muted` `(0.42,0.32,0.18)` / `Negative` `(0.7,0.18,0.12)` so they read on the light cards (ColonyPanel/ColonyTheme untouched — already light). The **Train/Purchase `UnitButtonGrid` cells now show the unit name** (`Display(short)`, word-wrapped) under the sprite above the price (cell `124×148`, was `118×96`, full name still in the tooltip), and each **goods-market row keeps its readable `Display` name** with a `92px` min width so it can't collapse beside a Buy+Sell pair. Presentation-only (ADR-006), no engine/API/RNG/save change; every control name + the drop-target-as-parent structure preserved (descendant/min-height L3 guards stay green). Named controls preserved for the L3 tests |
| `EuropeDragSource` / `EuropeDropTarget` (`game/presentation/EuropeDragDrop.cs`) | The Europe-screen drag-and-drop transport Controls (`86d3f6…` Phase 2), the project's first use of Godot 4.6's three-override drag API. **`EuropeDragSource`** wraps a draggable child and overrides `_GetDragData` to build a `Godot.Collections.Dictionary` payload (via a configured factory) + a floating `SetDragPreview` label, returning `default` for "nothing draggable" (Godot issue 78507 — the C# override is non-nullable). It also offers an optional **`OnClick(Action)`** — a left-button release that didn't become a drag fires the action (Godot dispatches a drag through `_GetDragData` on press-and-move and suppresses the release click), so a ship card both **selects on a single click** and **sails on a drag** from the one control. **`EuropeDropTarget`** wraps a drop zone and overrides `_CanDropData` (validate the payload kind **and** the `Game.Check*` oracle Allows — so the cursor shows can/can't-drop) and `_DropData` (re-check the oracle, forward the single `Game` command, refresh). It is a **sizing PARENT, not an overlay** (`86d3f6ddfix`): it is meant to *wrap* the zone's content as that content's parent so the content's interactive buttons are this control's **descendants** (rendered on top of the `MouseFilter.Pass` target → reachable by real mouse clicks), while a drag-hover over the non-button area still reaches the target. Because a bare `Control` does **not** aggregate its children's minimum sizes the way a `Container` does (the default `_GetMinimumSize` returns only its own `CustomMinimumSize`), a content child placed inside one would let the parent collapse to nothing — so `EuropeDropTarget` adds **`SetContent(Control)`** (adds the child, anchors it `FullRect`, connects its `MinimumSizeChanged` → `UpdateMinimumSize`) and overrides **`_GetMinimumSize()`** (the per-axis max of its visible `Control` children's combined min sizes), making it size like a single-child container. Both are **rule-free transport** (ADR-006) — every accept/drop decision reads a `Game` oracle or an inline state-only guard, encoding no new rule; a refused drop is a graceful no-op, never thrown. The payload tags/keys live in the `EuropeDrag` static (`KindColonist`/`KindShip`/`KindGoods`, `unitId`/`shipId`/`goodsId`/`amount`/`fromShipId`); `EuropeDrag.KindOf(Variant)`/`Preview(text)` are shared helpers (the `Preview` floating label is a **light parchment box with an explicit dark `font_color`** since it is a transient, briefly-parented control that may not inherit `ColonyTheme` — the readability fix `86d3f6…`, was a dark box ⇒ dark-on-dark). Both expose a fluent `Configure(...)` (+ `EuropeDragSource.WithChild`) so `EuropePanel`/`ColonyPanel` wire them inline during their programmatic rebuild. The L3 tests drive the callbacks directly (`_GetDragData`/`_CanDropData`/`_DropData`) since Godot can't synthesise a real mouse-drag headlessly; a structural-ancestor + geometry L3 guard (the zone's `EuropeDropTarget` is an *ancestor* of each key button, and the card/cell renders un-collapsed) protects the parent-not-overlay invariant |
| `ColonyDrag` (`game/presentation/ColonyDragDrop.cs`) | The colony-screen drag-and-drop **payload schema** (`86d3f6dnd` Phase 2), reusing the generic `EuropeDragSource`/`EuropeDropTarget` transport Controls (they're rule-free, not Europe-specific). A single payload shape travels — `worker : { "kind":"worker", "unitId":int }` — built by the `ColonyDrag.Worker(unitId)` factory; `ColonyDrag.KindOf(Variant)`/`IsWorker(Variant)`/`Preview(text)` (the last delegating to `EuropeDrag.Preview`) are the shared helpers. Because a colony's idle colonists are a **count + a non-free-type overlay, not individual map units**, and the engine's assign-work commands auto-pick an idle colonist, `unitId` is the dragged chip's own identity (its idle-row index) — used for the preview/round-trip, never to choose the worker. `ColonyPanel` wires the idle chips (`EuropeDragSource`), the tile targets and the building targets (`EuropeDropTarget`) with this schema during its programmatic rebuild |
| `TradeRoutePanel` | The trade-route management screen (`86d3c9rrd`): lists the human player's routes (each with its stops, an **Assign carrier** dropdown of eligible ships/wagons, a **Delete** button) and a **New route** quick-create (from-colony + to-colony + an optional good to load at the first stop → **Create route**). Built programmatically into the `VBox/Dynamic` shell like `EuropePanel`; reads `HumanPlayer.TradeRoutes` and forwards to `Game.CreateTradeRoute`/`AssignTradeRoute`/`RemoveTradeRoute` only (ADR-006). Opened by the **Trade Routes** button via `GameController.OpenTradeRoutePanel`. See [trade-routes.md](../systems/trade-routes.md) |
| `ColonyReportPanel` | The empire **reports** screen (FreeCol Report* panels) with switchable **tabs** (`Tab_Colonies`/`Tab_Units`/`Tab_Education`/`Tab_Production`/`Tab_Labour`/…): **Colonies** (`86d3c9wz6` — one `Colony_{id}` row per human colony: population, SoL %, production bonus, net food/turn, current **build** + materials-required (`stored/cost`, FreeCol "Requirements"), and net per-turn production via the shared `Game.ColonyNetProduction` oracle); **Units** (`86d3c9x15` — the human's units grouped into `UnitSection_Military`/`Naval`/`Cargo`/`Labour` using `Game.IsMilitaryUnit` + `UnitType.IsNaval`/`IsCarrier`/`CarryTreasure`/`IsPerson`, each row showing type+role+location, carriers showing hold contents). Built into `VBox/Dynamic`; reads Game oracles only (ADR-006). Opened by the **Reports** button via `GameController.OpenColonyReportPanel`. **Education** (`86d3drn6f` — FreeCol `ReportEducationPanel`: per human colony with a teaching building, a `School_{id}` header then each `Teaches` building's eligible expert teachers (`Colony.BuildingOccupants` filtered to the `[minimum-skill, maximum-skill]` window) and a `Student_{id}_{building}` row for the colonist each teacher will raise — derived from `Colony.TileWorkers`/`BuildingOccupants`/`IdleWorkerTypes`/`IdleColonists` via `Ruleset.GetTeachingType`, least-skill-first to echo the engine's automatic pick — with its training progress `done/needed` from `Colony.SchoolTrainingTurnsAt` and `Ruleset.NeededTurnsOfTraining − ProductionBonus`. Read-only — the schooling runs in GameLogic's colony turn); **Production** (`86d3drn6g` — FreeCol `ReportProductionPanel`: a `ProductionGood` `OptionButton` over the non-farmed goods; picking one rebuilds with a `Production_{id}` row per human colony showing its net output (`Game.ColonyNetProduction`, keyed by the good's `StoredAs`) and the producers behind it — each worked tile yielding it (`Colony.TileWorkers` + `Game.TileYield`) and each building whose `BuildingType.Productions` make it, with its `workers/workplaces`); **Labour** (`86d3drn6m` — FreeCol `ReportLabourPanel`: every human colonist grouped by unit type into a `Labour_{type}` header then one row each with location + job — colony residents from the worker overlays (tile good / building / idle, schools shown as "teaching") plus on-map `PlayerUnits` persons). **Requirements** (`86d3e4buh` — FreeCol `ReportRequirementsPanel`: per human colony a `Requirements_{id}` header then warning rows — a worked tile / manned building whose good wants an expert (`Ruleset.ExpertForProducing`) that isn't doing the job (bad assignment) or that no colony expert produces (missing expert); a `RequirementsMet_{id}` line when none); **Military** (`86d3e4buh` — FreeCol `ReportMilitaryPanel`: a `MilitaryHuman` row with the human's land/naval unit counts + combined land attack power (`Game.HumanMilitaryStrength`) and a `MilitaryRef` row with the Royal Expeditionary Force's land/naval unit counts (`Game.ExpeditionaryForceStrength`) — the strength comparison ahead of independence); **Naval** (`86d3fq0ee` — FreeCol `ReportNavalPanel`: a `NavalHeader` fleet-count line then a `Ship_{id}` row per naval unit with type+role+location and hold contents — the standalone fleet breakdown the Military tab only counts); **Cargo** (`86d3fq0e1` — FreeCol `ReportCargoPanel`: a `CargoHeader` then a `Cargo_{id}` row per carrier (ships + treasure wagons via `UnitType.IsCarrier`/`CarryTreasure`) with location + hold contents (or `[empty]`) — where goods are loaded across the fleet); **REF / Expeditionary Force** (`86d3fq0d8` — the REF intelligence report: a `RefHeader` land/naval-total line then `— Land —` / `— Naval —` sections of `Ref_{type}_{role}` "N × type (role)" rows from the **new** `Game.ExpeditionaryForceComposition()` oracle — the King's army by unit type as it grows via ADD_TO_REF, where the Military tab shows only the totals); **Foreign** (`86d3c9x3c`/`86d3fq0e6` — affairs: each rival European power's stance + **tension/attitude band** (the **new** `Game.TensionLevelBetween` oracle, FreeCol `Tension.getLevel`) + #colonies + #units + military/naval strength (`Game.ColonialStrength`) + gold, faithful to FreeCol's `NationSummary` which hides only SoL%/fathers/tax without De Witt); **Natives** (discovered native settlements grouped by nation: alarm level, learnable skill, mission, wanted goods, fog-gated on `IsExplored`); **Religion** (`86d3e4buh` — FreeCol `ReportReligiousPanel`: the `ReligionImmigration` crosses bar — banked vs required + recruit price — then a `Church_{id}` row per colony producing crosses with its `+N crosses/turn` (`Game.ColonyNetProduction`) and its church/cathedral buildings' `workers/workplaces`); **Trade** (`86d3e4buh` — FreeCol `ReportTradePanel`: a `TradeTotal` net-after-tax summary then a `Trade_{good}` row per tradeable good with its sell (bid) / buy (ask) price **plus its cumulative net units sold + income before/after tax** from B4's market counters (`Game.Market.SalesOf`/`IncomeBeforeTaxesOf`/`IncomeAfterTaxesOf`) + a boycott flag); **Score** (`86d3fq0fb` — the in-game score / nation report: a `ScoreTotal` line with the human's score + honorific (`Game.ScoreBreakdown(human)` + `ScoreLevels.ForScore`) over its itemised summands (units / liberty / fathers / gold / history, + an independence-bonus line when applicable), then a `ScoreRankingHeader` and a `Standing_{id}` rank row per colonial-or-independent power (the **new** `Game.NationRanking()` oracle — score-descending, "(you)" on the human), reusing `ScoreComponents`/`PlayerScore` from the victory/high-score screens); **Exploration** (`86d3e4buh` — FreeCol `ReportExplorationPanel`: the regions the human discovered (`Game.Map.Regions` filtered to `DiscoveredBy == human`), newest-first, one `Region_{id}` row each with type / discovery turn / score, or an `ExplorationEmpty` line); **Congress** (`86d3c9x53` — the Continental Congress election state: a `CongressProgress` header showing the father being recruited (`Game.CurrentFather` → ShortName, or "(none)") + the banked-liberty progress (`Game.Liberty` / `Game.TotalFoundingFatherCost()`), then one `Father_{shortName}` row per offered father (`Game.OfferedFathers`) with its category (`FoundingFather.Type`), marking the in-progress pick. **Read-only** — election lives in `FoundingFatherPanel`); **History** (`86d3c9x53` — FreeCol's `ReportHistoryPanel`: the human's notable past events from `Game.History` in turn order — a `History_{i}` "Turn N: …" row each for colonies founded / wars entered / Founding Fathers elected, under a `HistoryNote` header (and a `HistoryEmpty` row when nothing has happened). The event log is **in-memory only this wave** — not persisted, so a reloaded game's history starts empty (a saved log is a follow-up, deferred to the save-hotspot stream)). Built into `VBox/Dynamic` (the tab row is an `HFlowContainer` that wraps onto a second line now that there are 18 tabs); reads Game oracles only (ADR-006). Opened by the **Reports** button via `GameController.OpenColonyReportPanel`. See [colonies.md](../systems/colonies.md), [units-movement.md](../systems/units-movement.md), [market.md](../systems/market.md), [independence.md](../systems/independence.md), [founding-fathers.md](../systems/founding-fathers.md) |
| `VictoryPanel` (`UI/VictoryPanel`) | The **victory / end-of-game statistics** screen (`86d3c9xc6`, FreeCol's victory `ModelMessage` + `ReportEndTurnPanel`): shown once `Game.Winner` is set — a nation won by defeating the REF (independence) or by an alt-victory condition (last European / last human standing). Names the winner in `VBox/VictoryTitle`, then into `VBox/Dynamic`: a `VictoryReason` line (which condition fired, from the winner's `PlayerType`), the **final score broken down** from the new `Game.ScoreBreakdown(winner)` oracle — `ScoreHeader` (total) then `ScoreUnits`/`ScoreLiberty`/`ScoreFathers`/`ScoreGold`/`ScoreHistory` lines and a `ScoreBonus` row when an independence bonus applies — and the **end-game stats** `StatColonies`/`StatPopulation`/`StatTurns`/`StatYear` (the winner's colonies, summed population, `Game.Turn`, `Game.CalendarLabel`). Built into `VBox/Dynamic` with the signal-safe rebuild idiom (`RemoveChild` then `QueueFree`); reads Game oracles only (ADR-006), never mutates. Auto-opened once when `Game.Winner` first appears (a `_victoryShown` one-shot in `GameController.UpdateVictoryUi`, re-armed in `StartGame`); also `GameController.OpenVictoryPanel` for tests. Hidden by default → no golden churn. See [scoring.md](../systems/scoring.md), [independence.md](../systems/independence.md), [game-modes.md](../systems/game-modes.md) |
| `HighScoresPanel` (`UI/HighScoresPanel`) | The **High Scores** leaderboard screen (`86d3c9xb1`, FreeCol's `ReportHighScoresPanel`): the persisted top-10 of completed games, opened from the in-game **High Scores** button (`GameController.OpenHighScoresPanel`) and automatically when a game ends. Handed an already-ranked `IReadOnlyList<HighScore>` via `Open` (the ranking/store rules live in GameLogic — ADR-006), it renders into `VBox/Scroll/Dynamic` a header plus a `Score{rank}` row per entry best-first: rank, the `ScoreLevel` honorific (e.g. *Mountain Range*), player/nation, score, Won/Lost, difficulty, turns, date; an empty leaderboard shows an `Empty` placeholder. Built with the signal-safe `RemoveChild`+`QueueFree` idiom; does **no file I/O** itself — `GameController` loads the store via `HighScoresService` and passes it in. Hidden by default → no golden churn. See [scoring.md](../systems/scoring.md) |
| `HighScoresService` (static, presentation) | The Godot host I/O bridge for the leaderboard (`86d3c9xb1`): reads/writes `user://highscores.json` with `FileAccess`, delegating all ranking/serialization to GameLogic's pure `HighScoreStore` — the same engine/host split as `GameController` ↔ `SaveGame`. `Load()` (missing/unreadable → empty), `Record(candidate)` (offer to the saved board, persist if it qualifies — FreeCol `newHighScore`), `Save(scores)`. The file is **separate from the game save** → no save-version bump (v52). See [scoring.md](../systems/scoring.md) |
| `FoundingFatherPanel` | The **Founding Father choice** dialog (`86d3c9xet`, key **F**): shows the fathers currently offered to the human's Continental Congress (one per category, `ShortName (Type)`), the father being recruited, and the liberty to elect the next; choosing an offered father (a `Choose_{shortName}` button) calls `Game.ChooseFather`. Reads `Game.OfferedFathers`/`CurrentFather` (ADR-006); the election/age-weighting/effects are all in GameLogic. Opened by `GameController.OpenFoundingFatherPanel`. See [founding-fathers.md](../systems/founding-fathers.md) |
| `DeclarationPanel` (`UI/DeclarationPanel`) | The **Declaration-of-Independence signing screen** (`86d3c9xht`, FreeCol's `InGameController.declareIndependence` + `ConfirmDeclarationDialog` + `showDeclarationPanel`): the human's ceremonial seam for breaking from the Crown. A **gated `IndependenceButton`** HUD action — shown by `GameController.RefreshView` only while `Game.CheckDeclareIndependence(human).Allowed` (a colonial power with ≥ 50% national SoL, a connected port, in the colonial era), hidden the rest of the game / once rebelled — opens it via `GameController.OpenDeclarationPanel`. Two phases built into `VBox/Dynamic` with the signal-safe `RemoveChild`+`QueueFree` rebuild idiom (mirrors `FoundingFatherPanel`): **confirm** — echoes rebel sentiment (`Game.NationalSonsOfLiberty`) and lists the consequences (`ConsequenceEurope` reading `Game.UnitsInEurope` for the seized-units count, `ConsequenceRef`, `ConsequenceMuster`) with **"Liberty or Death!"** (`ConfirmButton`) / **"Maybe Later"** (`CancelButton`); **signed** — on confirm it forwards `Game.DeclareIndependence(human)` and shows the verbatim FreeCol `declareIndependence.resolution` flavour text (`Resolution`) + a `SignedCloseButton`. Opened while ineligible it shows the oracle's gate reason (`GateReason`) + a `DismissButton` instead — so it can be driven unconditionally. Reads oracles + forwards the one command only (ADR-006); no rules, no save bump. Hidden by default → no golden churn. See [independence.md](../systems/independence.md) |
| `ColopediaPanel` (`UI/ColopediaPanel`) | The in-game **Colopedia** reference panel (`86d3c9x9t`, FreeCol's `Colopedia`), with switchable **category tabs** (button **Colopedia** / key **C**): a `Cat_{Category}` tab row above the list switches between **Goods** / **Terrain** / **Units** / **Buildings** / **Fathers** / **Nations** / **Resources** / **Concepts**, each a read-only list over a different `Game.Ruleset` collection (named `<Category>_{shortName}` rows for the L3 tests). **Goods** — kind (food / raw / New World / refined-from-X / manufactured) + market sell/buy price (`Game.Market`) for tradeable goods; **Terrain** — kind + move cost + defence bonus + worked-for goods; **Units** — kind (ship/colonist/land) + movement + attack/defence + how obtained (built/trained/purchased); **Buildings** — required population + workplaces + build cost; **Fathers** — category + a plain effect summary (free units/buildings, lifts boycotts, reveals colonies, or bonus/ability counts); **Nations** — playable flag + advantage type (REF nations skipped); **Resources** — the unscoped yield bonuses it grants; **Concepts** (`86d3drn70`, FreeCol's `ConceptDetailPanel`) — free-text **help topics** as a master-detail view: a left list of topic-title buttons (`ConceptsRow/ConceptList/Concept_{slug}`, the active one disabled) over a right detail pane (`ConceptsRow/ConceptDetail/ConceptTitle` + word-wrapped `ConceptText`). Since there are no ruleset help-strings yet, the topics are a small **curated stub set** (Founding a colony, Working tiles & production, Trade & Europe, Sons of Liberty & bells, Founding Fathers, Combat basics, Native relations, Declaring Independence) — the seed of an in-game help/tutorial surface, presentation-only (no rules). Built programmatically into `VBox/Scroll/Dynamic`; reads `Game.Ruleset`/`Game.Market` only (ADR-006), never mutates. Opened by `GameController.OpenColopediaPanel`. Hidden by default → no golden churn. See [market.md](../systems/market.md), [founding-fathers.md](../systems/founding-fathers.md) |
| `FindSettlementPanel` | The **Find Settlement** / locate-colony dialog (`86d3c9xkx`, key **L**): lists the human player's colonies (a `Find_{id}` button each, with population + position); choosing one recenters the main camera via `GameController.CenterCameraOnTile` and closes. Reads `Game.Colonies` filtered to the human (ADR-006); no GameLogic change. Opened by `GameController.OpenFindSettlementPanel` |
| `NativeSettlementPanel` | The on-map native-settlement interaction panel: opened by clicking a discovered settlement, offers **speak with chief / learn skill / establish or denounce mission / sell goods / buy goods / attack**, each shown only when its `Check…` allows the acting unit; re-resolves the acting unit by id each rebuild (a learned colonist is swapped), hides itself if the settlement is sacked from it. **Two-way trade** (`86d3f62qh`): when the acting unit is a carrier at/adjacent to a coastal settlement, *Sell goods* / *Buy goods* open an in-panel goods+price picker (over `GoodsToSell`/`CheckSellToNatives`/`CheckBuyFromNatives`) then a haggle screen surfacing `TryHaggleSell`/`TryHaggleBuy`'s offer/counter — Accept commits via `SellToNatives`/`BuyFromNatives`, Haggle pushes one round, a refused trade shows its reason (never throws). The haggle is advisory; the panel reports the gold the oracle actually returns. **Missionary actions** (`86d3f62qr`): when the selected unit is a missionary at/adjacent to the settlement, an **Establish mission** button (`CheckEstablishMission`) — or, over a denounceable rival European mission, a **Denounce mission** button (`CheckDenounceMission`) — forwards to the thin guarded `GameController.EstablishMission`/`DenounceMission` commands (over the public `Game.EstablishMission`/`DenounceMission` oracles; `try/catch (InvalidMoveException)` → status notice) and surfaces the outcome (established / missionary killed / rival ousted / denunciation failed) via the result text + `onAction`; the denounce roll runs on the engine's injected RNG stream (ADR-009). Reads state + forwards to Game oracles only (ADR-006) |
| `NegotiationPanel` (`UI/NegotiationPanel`) | The **full negotiation table** (`86d3c9xpt`/`86d3c9ubw`/`86d3e4bbv`, FreeCol's incoming-treaty dialog + `getScoutForeignColonyChoice`/`moveScoutColony` + `NegotiationDialog`): the human's seat at the treaty table over the shipped backend. Opened by the HUD **`DiplomacyButton`** (→ `GameController.OpenNegotiationPanel`), auto-opened by `RefreshView` when `Game.PendingHumanProposals.Count > 0`, or pinned to a rival by a **scout at its colony gate** (`TryOpenScoutColonyMissions` intercepts the rival-colony click when `Game.CheckSpyOnColony` passes → a Spy/Negotiate mission menu; non-scouts still assault). Surfaces queued AI offers (**Accept** → `Game.SettleTrade`, **Decline** → drop); lets the human pick a contacted rival (`Game.ForeignNationStances`) and **assemble a multi-clause treaty** — gold give/demand, goods, colony cede/demand, unit hand-over/demand, incite, stance (reading `Game.Colonies`/`Units`/`Ruleset.GoodsTypes`/`Player.Gold`/`Colony.StoreOf` for eligible items) — then **Submit** through the rival's own `Game.EvaluateTrade` (accept → `SettleTrade`; reject → `Game.CounterOffer` surfaced for Accept/Reject counter). Spy runs `Game.SpyOnColony` (the `ColonyInteriorSnapshot` glimpse — always succeeds, FreeCol-exact). Reads oracles + forwards commands only (ADR-006); signal-safe `RemoveChild`+`QueueFree` rebuild; **no rules, no save bump**; the only RNG is inside `Game.CounterOffer` (the AI's own stream). Faithful subset: a single AI accept/counter/reject round (no human-side multi-round haggle). See [diplomacy.md](../systems/diplomacy.md) |
| `MoundsDecisionPanel` | The strange-mounds decision modal (`86d3cqqu5`): when a human explorer steps onto a strange-mounds Lost-City rumour (`Game.PendingMounds`), `GameController.RefreshView` opens this panel to ask **Investigate** or **Leave them be**; the buttons call `Game.ResolvePendingMounds(bool)` and report the one-line outcome in the status bar. Presentation-only (ADR-006) — the outcome roll/effects live in GameLogic. Mirrors `NativeDemandPanel` |
| `PreCombatPanel` | The **pre-combat odds modal** (`86d3c9xmw`): clicking an adjacent enemy unit opens it via `GameController.AttackUnitAt`, showing your strength / the enemy's strength / your chance to win from the side-effect-free `Game.CombatOddsAgainst` oracle, with **Attack** (→ `ResolveAttackOn` rolls the real `Game.Attack`) and **Cancel** (no-op — nothing committed: no RNG, no war, no turn spent). Presentation-only (ADR-006); the powers/odds + resolution live in GameLogic. Mirrors `MoundsDecisionPanel`. See [combat.md](../systems/combat.md) |
| `NativeDemandPanel` | The native tribute-demand modal: when a brave demands tribute during the AI phase (`Game.PendingDemand`), `OnEndTurnPressed` opens it (gated on `!IsHumanDefeated`) to ask **pay** (`Game.AcceptPendingDemand`) or **refuse** (`Game.RefusePendingDemand`), then reports a one-line outcome and hides. Renders the demand + forwards the choice only (ADR-006); the engine auto-refuses an unanswered demand next turn |
| `MonarchDialog` (`UI/MonarchDialog`) | The **Monarch action** modal (`86d3c9xdb`): when the home-nation King makes an *answerable* demand — a **tax rise** (RAISE_TAX) or a **mercenary offer** (MONARCH/HESSIAN_MERCENARIES) sets `Game.PendingMonarchDemand` during the turn tick — `GameController.RefreshView` opens it (gated on `!IsHumanDefeated`, once via `!Visible`) to ask **Accept** / **Decline**, forwarding the choice to `Game.RespondToMonarch(bool)` and reporting a one-line outcome in the status bar. A tax demand shows the proposed rate + the eyed goods/colony (*Pay the tax* / *Refuse (tea party)*); a mercenary offer shows the force size + gold price (*Hire them* / *Decline*). Renders the demand + forwards the boolean only (ADR-006); the proposed rate, tea-party fallout, mercenary force/price + the King's displeasure all live in GameLogic (`Game.Monarch.cs`). The King's *immediate* actions (lower/waive tax, declare war/peace, free support, REF build-up) have no pending seam, so they never open this dialog. Hidden by default → no golden churn. Mirrors `NativeDemandPanel`/`MoundsDecisionPanel`. See [monarchy.md](../systems/monarchy.md) |
| `EmigrationChoicePanel` (`UI/EmigrationChoicePanel`) | The **emigration choice** modal (`86d3c9xft`, FreeCol's `selectRecruit`): when a human who has elected William Brewster is due an emigrant, `Game.PendingEmigration` is set and `GameController.RefreshView` opens this modal (gated on `!IsHumanDefeated`, once — the panel re-opens itself for a backlog and clears when the engine clears `PendingEmigration`). One `Choose_{i}` button per dock recruit (the unit type, title-cased), built into `VBox/Dynamic`; choosing one calls `Game.ChooseEmigrant(slot)` and reports the outcome via `OpenEmigrationChoicePanel`'s callback. The same panel also serves the **Fountain-of-Youth** burst (`86d3c9ujx`): when `PendingEmigration.IsFountainOfYouth` it shows a "A Fountain of Youth!" title + "(N to choose)" info and re-opens itself for each of the `dx` picks. Presentation only (ADR-006) — the pause seam + the per-emigrant bookkeeping live in `GameLogic` (`Game.Emigration.cs`). Hidden by default → no golden churn. Mirrors `MoundsDecisionPanel`. See [immigration.md](../systems/immigration.md) |
| `TurnMessagePanel` (`UI/TurnMessagePanel`) | The **end-of-turn message panel** (`86d3c9x53`, FreeCol `ReportTurnPanel`): after the AI phase, `OnEndTurnPressed` formats every notice channel (`Game.CombatNotices`/`ColonyRaidNotices`/`ColonyLossNotices`/**`WarehouseOverflowNotices`**/**`ColonyFamineNotices`**/**`ColonyStarvedNotices`**/**`MonarchDecreeNotices`**/`CustomHouseSaleNotices` + the defeat line) into player-facing strings and hands the list to `Open(messages)`, which lists one named `Message_{i}` row per message in a scrollable column and shows the panel; an empty list is a no-op (stays hidden). Dismissed by the **OK** button. Pure presentation (ADR-006) — receives already-formatted strings (the formatting helpers `FormatCombatNotice`/`FormatColonyRaidNotice`/`FormatColonyLossNotice`/**`FormatWarehouseOverflowNotice`**/**`FormatColonyFamineNotice`**/**`FormatColonyStarvedNotice`**/**`FormatMonarchDecreeNotice`**/`FormatCustomHouseSaleNotice` stay in `GameController`); owns no game state. Hidden by default → no golden churn. Replaces the old one-line status-bar concatenation of the AI-phase notices |
| `MessageLogPanel` (`UI/MessageLogPanel`) | The re-openable **message log** (`86d3e4buh`/`86d3f0wdv`/`86d3e959t`): the per-turn notices the `TurnMessagePanel` flashes and forgets are appended to `GameController._messageLog` (a `List<MessageLogPanel.Entry(Turn, Messages)>`, each `Message` a `LogMessage(MessageCategory, Text)`), and the **Messages** HUD button (`OpenMessageLogPanel`) re-opens them — `Open(history, hidden, onToggle)` renders newest-turn-first, one `LogTurn_{turn}` section per logged turn (or a `LogEmpty` line). A **category filter row** (`VBox/FilterBar`, one `Filter_{Category}` `CheckBox` per `GameLogic.App.MessageCategory` — combat/diplomacy/economy/natives/monarch/colony) hides every notice of an un-ticked category and drops any turn header that empties out; the hidden set is the live **`SettingsModel.HiddenMessageCategories`** client option, so un-ticking persists to `settings.cfg` (FreeCol's per-type `guiShow…` toggles + `MESSAGES_GROUP_BY`). The controller tags each notice in `OnEndTurnPressed` (`Logged(category, text)`) by its notice type. **Persisted across save/load (save v59)**: the controller round-trips `_messageLog` through `SaveGame.MessageLog` (omit-when-empty — a fresh game stays byte-identical to v58), so a reloaded game keeps its history; the log feeds no game evolution, so the soak twin-run stays byte-identical (ADR-009). Pure presentation (ADR-006) — receives already-formatted strings; the hidden set is a client preference, not game state. Hidden by default → no golden churn |
| `MainMenu` (root of `scenes/MainMenu.tscn`, **the boot scene**) | The title screen shown before any game (replaces booting straight into a running game): FreeCol's antique-map backdrop + a carved-wood/parchment panel with **New Game** (→ the world-options dialog → the game scene), **Load Game** (→ the save-slot dialog), **Settings** (→ the settings overlay), **Help** (→ the Help overlay, `86d3e98db`), **About** (→ the About overlay, `86d3f0w2x`), **Quit**. **Quit** exits immediately — no confirmation, since no game is in progress at the title screen (the in-game quit paths in `PauseMenu` do confirm). `project.godot` `run/main_scene` now points here. Presentation-only (ADR-006). See [main-menu.md](../systems/main-menu.md), [help.md](../systems/help.md), [about.md](../systems/about.md) |
| `AboutPanel` (`scenes/AboutPanel.tscn`) | The **About / version / license** screen (`86d3f0w2x`, FreeCol `AboutPanel.java`): a reusable overlay (emits `Closed`) reachable from **both** the main menu and the pause menu, same parchment/wood skin as `SettingsScreen`. Shows the game name + `AppInfo.Version`, a short GPL v2 disclaimer pointing to `LICENSE` + `CREDITS.md`, a copyright line, the repo link (`AppInfo.RepositoryUrl`), and a pointer to the player manual (`docs/MANUAL.md`). Purely informational — reads `GameLogic.App.AppInfo` only (ADR-006); **Back** dismisses it. See [about.md](../systems/about.md) |
| `HelpPanel` (`scenes/HelpPanel.tscn`) | The **in-game help / tutorial** screen (`86d3e98db`, FreeCol's tutorial + contextual help): a reusable overlay (emits `Closed`) reachable from a **Help** button on **both** the main menu and the pause menu, same parchment/wood skin as `AboutPanel`. A single scrollable page covering the **goal** of the game, the **core loops** (explore → found/run colonies → economy/Europe → liberty/independence → natives → combat), and a **controls / keybindings reference** whose key list restates the authoritative key table in `GameController.BuildKeyBindings` (and the F1 legend), ending with a `docs/MANUAL.md` pointer. Original wording derived from `docs/MANUAL.md`; purely informational (ADR-006) — owns no game state, binds no hotkey (F1 stays the in-game legend). **Back** dismisses it. See [help.md](../systems/help.md) |
| `NewGameDialog` | The new-game world-options overlay (`86d3c9w9c` + America scenario map + nation pick `86d3drn5x` + the **game-options section** `86d3drn64` + the **fog-of-war toggle** `86d3dzdw3`): the **New Game** button opens it to pick the **map** (Random New World / America (fixed)), **world size** (Small/Standard/Large/Huge `OptionButton`), **land mass** (Sparse/Normal/Dense), **landmass style** (Continent/Archipelago/Islands), **difficulty**, **nation** (the ruleset's selectable powers, or "No nation"), the three alternative **victory conditions** (a "Victory conditions" group of `VictoryRefCheck`/`VictoryEuropeansCheck`/`VictoryHumansCheck` `CheckBox`es — FreeCol's `gameOptions.victoryConditions`: defeat the REF / be the last European / be the last human standing, each pre-ticked to the ruleset's parsed spec default), and the **fog of war** toggle (a `FogOfWarCheck` `CheckBox` — FreeCol's `model.option.fogOfWar`, pre-ticked to the ruleset's spec default, classic **on**; unticking it keeps every explored tile permanently visible), then **Start**. Choosing America disables the size/land/style dropdowns (a fixed map sets its own dimensions). Built programmatically (no `.tscn`), parchment-skinned; collects the choices and forwards them to `GameController.PendingWorldSize`/`PendingLandMass`/`PendingDifficulty`/`PendingMapSource`/`PendingNation`/`PendingLandStyle`/**`PendingVictoryConditions`**/**`PendingFogOfWar`** (statics surviving the scene change) → `StartNewGame`/`Game.New`. **Faithful subset:** only base game options the engine actually honours are surfaced — the victory conditions (read by `Game.Winner`) and fog of war (read by `Game.CurrentlyVisible`/`IsVisible`) are in; the other `gameOptions.map` toggles are still omitted because the engine doesn't consult them yet. Presentation-only (ADR-006); the presets + generation + the fixed `FixedMap` loader + the victory checks + the fog derivation live in GameLogic (`WorldSizeOptions`/`MapGenerator`/`FixedMap`/`Game.Winner`/`Game.CurrentlyVisible`), the victory + fog overrides applied to the loaded ruleset via the config seams `Ruleset.WithVictoryConditions`/`Ruleset.WithFogOfWar` (**session-only — not persisted, no save bump**). See [map-terrain.md](../systems/map-terrain.md), [main-menu.md](../systems/main-menu.md), [independence.md](../systems/independence.md), [fog-of-war.md](../systems/fog-of-war.md) |
| `SettingsScreen` + `SettingsService` (autoload `/root/Settings`) | The options screen — a reusable overlay (emits `Closed`) editing the live application settings the autoload owns: Video (window mode, vsync) + Audio (master/music/SFX via audio buses) + **Accessibility** (**UI scale** 75–200% via the root viewport `ContentScaleFactor`, whole-UI live resize; **colourblind palette** toggle) + **Game** (**Autosave every N turns** SpinBox, 0–100, 0 = off, read at turn end by `GameController.MaybeAutosave`) + a **Key Bindings…** button opening `KeyBindingsScreen`, applied live and persisted to `user://settings.cfg` (`ConfigFile`). The model is the engine-free `GameLogic.App.SettingsModel`. `SettingsService._Ready` also calls `KeyBindingsService.LoadAndApply()` so saved hotkey overrides reach the `InputMap` on boot. App settings, deliberately separate from per-game rule options. See [settings.md](../systems/settings.md) |
| `KeyBindingsScreen` (`scenes/KeyBindingsScreen.tscn`) + `KeyBindingsService` (static) | The **key-rebinding** surface (`86d3f0wjj`). `KeyBindingsScreen` is a reusable overlay (emits `Closed`) opened from `SettingsScreen`'s *Key Bindings…* button: one row per rebindable action showing its current key, a **Rebind** (captures the next key press in `_Input`; Esc cancels, lone modifiers ignored), a per-row **Reset** and a **Reset all to defaults**; **Back** persists. `KeyBindingsService` (static) bridges the engine-free `GameLogic.App.KeyBindingsModel` (action list + overrides) to Godot's `InputMap` (defaults in `project.godot` `[input]`) and to `user://settings.cfg`'s `[keybindings]` section — `Apply`/`Rebind`/`Load`/`Save`/`Describe`/`EventFor`. `GameController._UnhandledInput` dispatches hotkeys via `@event.IsActionPressed(actionId, exactMatch: true)` and regenerates the F1 legend from the live `InputMap`, so rebinds take effect immediately and the legend can't drift. Presentation-only (ADR-006); **settings.cfg only — no save bump (ADR-009)**. See [hud-input.md](../systems/hud-input.md) |
| `AccessibilityPalette` (static) | The single player-colour source for the map's identification markers, gated by the colourblind setting (`SettingsService.Apply()` sets its `ColorblindMode` flag). Pass-through accessors (`RivalNation`/`Native`/`RivalFallback`/`Own`/`Rival`) return the supplied default colour when off; when on they return the **Okabe–Ito colour-universal palette** (<https://jfly.uni-koeln.de/color/>, free reuse — a foreign nation is keyed onto the cycle by a deterministic FNV-1a hash of its short name). Consumed by `GameController.OwnerColorOf` (unit owner rings) and `MiniMap` (colony/unit/settlement dots). Presentation-only (ADR-006); no save impact (settings.cfg only). See [settings.md](../systems/settings.md) |
| `SoundService` (autoload `/root/Sound`) | The sound-effects player (`86d3c9xrp`). Game code calls `Play(SoundEvent)`; the service resolves the logical event to a clip via the **engine-free** `GameLogic.Audio.SoundEventCatalog` (the event→`res://` path map, unit-tested) and plays it on the **`SFX`** audio bus, so the settings SFX-volume slider applies. Clips are FreeCol's own GPL-v2 SFX, copied into `assets/freecol/sound/` (see that folder's `PROVENANCE.md`); preloaded once at startup into a small round-robin pool of `AudioStreamPlayer` voices so overlapping cues don't cut each other off. A missing clip/service is a logged no-op, never a crash (ADR-006: logic decides *what* to cue, this layer decides *how* it sounds). First wired events: **founding a colony** (`ColonyFounded`) and **resolving an attack** (`Combat`, or `ShipSunk` on a decisive naval win), both in `GameController`. See [settings.md](../systems/settings.md) |
| `MusicService` (autoload `/root/Music`) | The background-music + national-anthem player (`86d3c9xu1`). On startup it begins a **looping, shuffled background playlist** (`PlayBackground(MusicContext.Background)`) on the **`Music`** audio bus, so menu *and* gameplay share one ambient bed (the settings Music-volume slider applies). It resolves contexts/anthems via the **engine-free** `GameLogic.Audio.MusicTrackCatalog` (the context→`res://`-path-list map + a `model.nation.<x>`→anthem map, unit-tested). `PlayAnthem(nationId)` plays a nation's anthem **once** then resumes the playlist (a no-op for natives/REF/unknown ids); `GameController.StartGame` cues the human player's anthem. One `AudioStreamPlayer`; the `Finished` signal advances the playlist (and falls back from an anthem). Tracks are FreeCol's music copied into `assets/freecol/music/` (background = **CC BY 4.0**, anthems = **GPL v2** — see that folder's `PROVENANCE.md`). A missing clip/service is a logged no-op, never a crash (ADR-006). The shuffle uses Godot's RNG (cosmetic order only — outside the seeded game RNG, no simulation effect; ADR-009 unaffected). See [settings.md](../systems/settings.md) |
| `CombatAnimation` (transient, parented to `MapView/UnitLayer`) | The **procedural attack animation** (`86d3drn72`). When an attack resolves, `GameController.PlayCombatAnimation` spawns one via `CombatAnimation.Play(unitLayer, from, defenderTile, kind, texture)`: a transient `Sprite2D` (the attacker's resolved sprite via `UnitMarker.ResolveTexture`, or a coloured-disc stand-in) **lunges** from the attacker's tile toward the defender and snaps back over ~0.36 s, with a flash — a **hit** lunges in warm, a **repelled** loss recoils away in red, a naval **evade** is a cool feint. The visual flavour comes from the engine-free `GameLogic.Combat.CombatAnimationMap` (`CombatResult`→`CombatAnimationKind`, unit-tested). **Non-blocking** (a `Tween` that `QueueFree`s itself — the turn proceeds immediately) and **no new art** (faithful subset of FreeCol's `animation.Animations.unitAttack` move-toward-defender tween; FreeCol's per-type `.sza` sprite-sequence frames cover only soldier/dragoon, so the lunge is the common denominator). Shares the isometric tile space via `MapView.TileCentre`; never touches the shared `UnitMarker` draw code. A null unit layer (headless) is a silent no-op. See [combat.md](../systems/combat.md) |
| `PauseMenu` (in `scenes/main.tscn` `UI`, `ProcessMode.Always`) | The in-game **Esc** pause overlay over a paused tree (`GetTree().Paused`): Resume / Save Game / Load Game / Settings / **About** (`86d3f0w2x`) / Quit to Main Menu / Quit to Desktop. Sub-overlays (settings, save/load, about) are tracked as one `_overlay` (a `Control`) added to the UI layer with `Always` process mode so they work while paused. **Both quit paths confirm first** (`86d3f0vf5`): Quit to Main Menu and Quit to Desktop each raise a **"Quit without saving?"** `ConfirmationDialog` (a `Window`, tracked separately in `_quitConfirm` so Esc is parked while it is up); only **Quit** runs the action, **Cancel** is a no-op (the game stays paused with the pause menu up). Additive to `main.tscn`. See [pause-menu.md](../systems/pause-menu.md), [about.md](../systems/about.md) |
| `InfoPopup` | A reusable modal (dim + parchment panel: title + wrapped message + OK); `InfoPopup.Show(host, title, message)` one-liner, emits `Closed`, frees itself. First consumer: save/load confirmations. See [info-popup.md](../systems/info-popup.md) |
| `SaveLoadDialog` | A five-slot save/load dialog (`user://saves/slotN.json`) **plus a read-only Autosave entry** (`autosave.json`). Each slot row (an `HBoxContainer`) shows "empty" or "Turn N (yyyy-MM-dd HH:mm)" — the **timestamp from `FileAccess.GetModifiedTime`** (no save-header change) — and, when filled+deletable, a **Delete** button (confirm → `DirAccess.RemoveAbsolute` + rebuild). Saving over a filled manual slot pops an **overwrite confirm** (cancel = no-op). The Autosave entry is **loadable but never a manual-save target** and not deletable here. Choosing a slot delegates to the host and closes. Hosts: the main menu (Load → `GameController.PendingLoadPath` + boots) and the pause menu (Save/Load → `GameController.SaveTo`/`LoadFrom`, confirmed by an `InfoPopup`). `GameController` also gained `static AutosavePath` + `MaybeAutosave` (end of `OnEndTurnPressed`: writes the autosave when `SettingsModel.AutosavePeriod > 0 && Turn % N == 0`, via the existing `SaveTo` — no format change). See [save-load-ui.md](../systems/save-load-ui.md) |
| `ColonyArt.ParchmentSkin()` | The shared parchment panel skin (tiled `bg_paper_brown` + 26px content inset, solid-fill fallback), hoisted from `ColonyPanel` and used by every menu-style screen (menu/settings/pause/popup/dialog) |
| `presentation/tests/GoldenAssert` | Shared L4 golden-image comparison helper (per-channel + fraction tolerance, `GOLDEN_UPDATE=1` regen, artifacts on mismatch) — used by both the map goldens (`VisualGoldenTests`) and the UI goldens (`MenuGoldenTests`) |
| `presentation/tests/` | **L3 GdUnit4 + L4 visual tests — live inside this project** because the GdUnit4 adapter requires the test assembly's project to BE the Godot project (official gdUnit4Net layout; ADR-011/015). Run: `dotnet test game/CrownAndColony.csproj` with `gdunit.runsettings` + `GODOT_BIN` set, after a clean `godot --build-solutions` |

## Key design notes

- The csproj **excludes `src/**` and `tests/**`** from its compile glob — Godot's SDK otherwise globs every `.cs` under the project root (build errors guaranteed). New presentation code goes under `presentation/`.
- `RollForward=LatestMajor` so the VSTest host can run on a newer installed runtime.
- Test-framework packages (GdUnit4, Test SDK) are referenced by the game project per the official pattern; revisit before shipping builds (strip via condition if it bloats exports).
- Renderer is `gl_compatibility` (works headless/software in CI).

## Tests

**107 scene tests** (97 L3 interaction + 10 L4 visual goldens), green on the real Godot runtime: `ColonyReportPanelTests` (the 18 report tabs incl. Trade volume/income, Exploration, Requirements, Military, **Naval/Cargo/REF-composition/Score**, and the Foreign attitude band), `MessageLogPanelTests` (the message log opens empty by default, renders a section per logged turn, and un-ticking a category hides its notices + drops the emptied turn header), `MonarchDialogTests`, `MainSceneTests`, `NegotiationPanelTests` (the full negotiation table: a queued AI offer auto-surfaces + Accept settles the alliance both ways, Decline drops it untouched, the offer builder adds a peace stance clause + Submits through the backend, an unacceptable gold-demand offer surfaces an AI counter that Accept-counter settles — the offer→counter→accept journey — and a queued AI cease-fire proposal stays answerable alongside the builder), `DeclarationPanelTests` (the Declaration-of-Independence screen: eligible → consequences → confirm declares + signed text + closes; ineligible → gate reason, the Independence button hidden, nothing declared), `InputTests` (click/move, click-to-attack, native-raid status-bar notice, **multiple own units render, a non-human unit renders only when in sight, a foreign unit renders in its nation colour**, the foreign-colony-capture loss notice, the **game-over overlay + disabled End Turn** on defeat, the **native tribute-demand modal** (pay transfers the goods), hotkeys, F5/F9), `ColonyPanelTests` (incl. **drag-and-drop worker assignment** `86d3f6dnd`: an idle chip → a free tile / a building assigns the worker, refused on a worked tile / a full building — driven via the drag callbacks directly), `ColopediaPanelTests` (Goods reference + category-tab switch incl. the **Concepts** help tab + a Concepts list→detail case), `EuropePanelTests` (recruit/board/sail + sell/buy goods + train/purchase routing — the train case **discriminates** `TrainUnit` from `BuyUnit` by zone — + a `GatedButton` **disabled** when unaffordable + carried-home treasure cash-in; **Phase 2 drag-and-drop**: the 5 drops via the callbacks directly + full-ship-board / unaffordable-buy refused + the **single-click** ship picker — `SelectTradeShip` then a market Buy landing in the selected ship, the `Select_` button having been removed in the playtest polish; the descendant/min-height structural guards over the ship card / sail zone), `TradeRoutePanelTests` (create a route, assign a carrier, delete a route), **`RiverOverlayTests`** (the river overlay draws on an explored river tile, nothing on a fogged one), **`ImprovementOverlayTests`** (the road/plow overlay draws a road track + a plow furrow on explored tiles, nothing on a fogged one), `JourneyE2ETests` (the one scene-level E2E), `VisualGoldenTests` (golden-screenshot diff: map, colony, native settlement, remembered-fog, **rendered-units** — two own units + an in-sight brave with its owner ring — rumour-marker, **river** — a winding river course around the start unit). **Title/settings front-end** (2026-06-18): `MainMenuTests`, `SettingsScreenTests`, `PauseMenuTests`, `InfoPopupTests`, `SaveLoadTests` (L3) + `MenuGoldenTests` (L4 goldens `main-menu`/`settings-screen`/`pause-menu`/`info-popup`, via the shared `GoldenAssert`). Driven in CI by ADR-015 (CI owns the Godot install; 3-attempt retry under xvfb). **Local note:** close any running Godot **editor** before running scene tests — a live editor on the project collides with the headless GdUnit runner (the `-1073741819` cold-start crash).

## Changelog

| Date | Change | Commit |
|---|---|---|
| 2026-06-26 | **Five colony-screen commands** (`86d3fq0aw`/`86d3fq0bg`/`86d3fpxge`/`86d3fpy7n`/`86d3fpyu0`, `ColonyPanel` + `GameController`, presentation-only over existing engine): **(1) Rename** — a `RenameField` `LineEdit` (seeded with `Colony.Name`) + a `RenameButton` (also fires on Enter) in a new `ManagementRow` at the top of the card, forwarding to thin `GameController.RenameColony` → `Game.RenameColony` (blank-name `ArgumentException` → status bar; confirm disabled while blank — an input affordance). **(2) Abandon** — an `AbandonColony` button in the same row, gated on `Game.CheckAbandonColony` (greyed + reason-tooltip when pop > 1 / fortified), forwarding to `GameController.AbandonColony` → `Game.AbandonColony`, which **closes the colony panel** (`_colonyPanel.Hide()`) since the colony is gone. **(3) Clear speciality** — a `ClearSkill_{uid}` button in the per-unit `OutsideArea` row, gated on `Game.CheckClearSkill`, calling `Game.ClearSkill` via the shared `Act(...)` helper. **(4) Arm missionary** — `model.role.missionary` added to the `ArmRoles` list, so the existing arm-role loop emits an `Equip_{uid}_missionary` button gated on the church-requiring `Game.CheckEquipRole` → `Game.EquipRole`. **(5) Pay boycott** — a `BoycottArea` section (rendered only when `Game.Market.Arrears > 0` for some good) with a `PayBoycott_{good}` button per boycotted good, gated on `Game.CheckPayArrears`, forwarding to thin `GameController.PayBoycott` → `Game.PayArrears`. The three host commands `try/catch` the engine exception into the status notice (ADR-006); rename/pay rebuild the panel via `RebuildDeferred` (the host already refreshed + set the notice, so a second `Changed`/host-refresh would clear it). `OpenColonyPanel` gains a nine-arg `Open` overload (the six-arg cargo/export overload is kept for existing callers; bare-scene defaults are no-ops). **Zero rule logic in the UI** — every button gates on a `Check*` oracle and forwards an existing command; no GameLogic edited, no save/RNG change. +11 L3 `ColonyPanelTests` (incl. a sane-height structural guard for all five buttons; the existing `BuildingCell_RendersAtSaneHeight` / `BuildingButtons_AreDescendantsOfTheCellDropTarget` guards stay green) → **36 ColonyPanelTests + 2238 L1/L2 green**. Render-verified by a temporary headless 1600×1200 capture (all five buttons render + are labelled), then removed. See [colonies](../systems/colonies.md), [education-schools](../systems/education-schools.md), [monarchy](../systems/monarchy.md). | (this commit) |
| 2026-06-26 | **Establish/denounce-mission UI** (`86d3f62qr`): `NativeSettlementPanel` had no mission action — a Jesuit/missionary couldn't establish a mission from the map (the engine had shipped long ago). `BuildActions` now adds, gated on the selected unit at/adjacent to the settlement, a **Denounce mission** button (`Game.CheckDenounceMission` — a denounceable rival European mission stands here) **else** an **Establish mission** button (`Game.CheckEstablishMission` — the unit carries the missionary role). One honest button, since the engine's establish routes to denounce over a rival. Each forwards to a new thin guarded `GameController.EstablishMission(unit, settlement)` / `DenounceMission(unit, settlement)` command (`try/catch (InvalidMoveException)` → status notice, never thrown) wrapping the existing public `Game.EstablishMission`/`DenounceMission` overloads, and surfaces the outcome (established / missionary killed / rival ousted / denunciation failed) via the panel result text + the existing `onAction` callback (status bar + selection re-sync, since the action consumes the missionary). The denounce roll runs on the **engine's injected RNG stream** (human stream 0 / `RandomFor(player)`), never `new Random()` (ADR-009). `NativeSettlementPanel.Open` gains two `Func<Unit,NativeSettlement,string>` command params (wired by `OpenNativeSettlementPanel`). **Pure presentation** (ADR-006) — no GameLogic/save change; panel hidden by default → no golden churn. +3 L3 `NativeSettlementPanelTests` (a missionary's Establish founds a human-owned mission + consumes the missionary; a non-missionary shows no button; a Denounce over a rival mission, the rival's immigration zeroed, ousts the rival + installs the human's). **218 L3, 2238 L1/L2, 5 soak green.** See [natives](../systems/natives.md) §1/§3/§4. | Phase 7 (`86d3f62qr`) |
| 2026-06-26 | **Europe screen — playtest polish** (`86d3f6…`, `EuropePanel` + `EuropeDragDrop.cs` + `ColonyArt.cs` + two read-only `Game` oracles). Chris's playtest of the spatial layout: **(1)** every cell sized to its content with padding so nothing clips in the 17px-bold in-game theme (train/purchase grid buttons `118×96` with a 44px sprite + `{price}g`, full name in tooltip only; recruit cards a `116` min width + a compact `Recruit · {price}g` `ClipText` button; market rows a `34` min height + `ClipText` name + a wider bid/ask column). **(2)** Empty hold-slots are a **faint parchment outline** (`SlotStyle(filled:false)` near-transparent fill + soft border) not the old near-black box. **(3)** Single-click ship-select — the `Select_{id}` button is **removed**; the ship card's `ShipDrag_{id}` source now both selects on a single click (new **`EuropeDragSource.OnClick(Action)`** — fires on a left-button release that didn't become a drag) and sails on a drag, with an explicit min height (a bare `Control` doesn't adopt its child's min size, and the removed button used to lend the title row its height). **(4)** Two **in-transit lanes** (`InTransitZone` → two `Card`s: *Expected soon* = `Game.ShipsSailingToEurope`, *Bound for the New World* = `Game.ShipsSailingToNewWorld`, each a sprite + "arrives in `{SailTurnsRemaining}` turn(s)"). **(5)** A **harbour backdrop** — `ColonyArt.HarbourBackdrop()` (FreeCol `ui/colonydocks.png`, sky+sea) set stretched as the panel background via `BuildPanelBackground` (parchment→solid fallback), behind an **opaque** parchment content backing (`CardBackingStyle` on a `ContentBacking` wrapper) so text stays readable. Two new owner-resolved read-only oracles (`ShipsSailingToEurope`/`ShipsSailingToNewWorld`, `IReadOnlyList<Unit>`) expose already-persisted state ⇒ **no save change** (ADR-006/009). `colonydocks.png`+`.import` added under `assets/freecol/ui/` (PROVENANCE updated, GPL v2 unmodified). The descendant/min-height L3 guards stay green (the ship-card guard moved off the removed `Select_` to the `CashIn_`/`Unload_` card buttons; the picker L3 drives selection via `SelectTradeShip`). Self-verified by a headless 1280×1700 render (cells fit, no black slots, backdrop behind readable cards, both lanes render), temp capture then removed. +1 L1/L2 (`SailingTests` in-transit oracles). **213 L3/L4, 2229 L1/L2, 5 soak green.** See [europe.md](../systems/europe.md). | Europe playtest polish (`86d3f6…`) |
| 2026-06-25 | **Dead-click BLOCKER fix — drop target is now a sizing PARENT, not an overlay sibling** (`86d3f6ddfix`, `EuropeDragDrop.cs` + `EuropePanel.cs` + `ColonyPanel.cs`). Adversarial review found real mouse clicks were dead on key buttons because each zone's `EuropeDropTarget` was a **later sibling overlaying** the body with `MouseFilter.Pass` — and `Pass` forwards a click to the **parent**, not down to the sibling button beneath it. Dead buttons: Europe `Select_/Unload_/Sail_/Board_/CashIn_` (the docks-treasure cash-in had no drag fallback, so the carry-treasure-home gold was unrealisable) and colony `Staff_/Unstaff_/Worker_`; the colony **building cell also collapsed** to ~12px because a plain `Control` drop target returns only its own (0,0) min size (it doesn't aggregate children like a `Container`). **Unified fix:** `EuropeDropTarget` gains `SetContent(Control)` (adds the child, anchors it `FullRect`, wires `MinimumSizeChanged` → `UpdateMinimumSize`) + a `_GetMinimumSize()` override (per-axis max of visible children's combined min) so it sizes like a single-child container; `EuropePanel.DropZone(drop, body)` now makes the drop the **PARENT** of the body (buttons become descendants → real clicks reach them; the card area not covered by a button still catches a drag-hover); `ColonyPanel.BuildingCell` uses `SetContent` so the cell no longer collapses. Non-interactive card decorations (ship sprite/labels/hold-slot boxes, dock portraits, sail arrow/label, building image/label/slot rows) set `MouseFilter.Ignore` so a drag-hover crosses the whole card to the drop target. The clean `GoodsMarketDrop` was always a parent-of-buttons, which is why it alone worked. **Presentation-only, no engine/API/RNG/save change** (ADR-006/009). +5 L3 guards (structural-ancestor: each key button is a descendant of its zone's `EuropeDropTarget`, not an overlaid sibling; geometry: ship card / sail zone / building cell render un-collapsed, `Size.Y` > 100/40) across `EuropePanelTests` + `ColonyPanelTests`; render-PNG self-check (tall 1280×1600 headless capture, Read, confirmed un-collapsed building cells + intact harbour, then removed). All L3/L4 + L1/L2 + soak green. See [europe.md](../systems/europe.md) / [colonies.md](../systems/colonies.md). | Drop-target sizing-parent fix (`86d3f6ddfix`) |
| 2026-06-25 | **Colony screen — drag-and-drop worker assignment** (`86d3f6dnd`, `ColonyPanel` + new `ColonyDragDrop.cs`). Reuses the generic, rule-free `EuropeDragSource`/`EuropeDropTarget` transport (not Europe-specific) with a colony-only payload schema (`ColonyDrag.Worker(index)` → `{ "kind":"worker","unitId":int }`). Adds an **Idle colonists** row (`IdleColonists`) of draggable portrait chips (`IdleColonist_{index}`); wraps each tile's unchanged `Tile_x_y` click button in a `TileDrop_x_y` target (gated on `CheckAssignWork` of the tile's best-yield good, dropping through the **shared** `AssignWorkWithClaim` so native-owned land raises the same buy/steal/abandon modal the click uses — the claim handling was already a shared method) and each building cell's content in a `BuildingDrop_{building}` target (gated on `CheckAssignBuildingWork` → `AssignBuildingWork`). Both re-check the oracle inside `_DropData` (a worked tile / full building / dockless sea tile is a silent no-op, never thrown). Since a colony's idle colonists are a count + non-free-type overlay (not map units) and the assign commands auto-pick, `unitId` is the chip's idle-row index — preview/round-trip only. **Additive** — every click control (`Work_/Release_/Staff_/Unstaff_`, click-to-move) stays the accessibility/L3 fallback; **no engine/save/RNG change** (UI-only, ADR-006/009). +4 L3 (`ColonyPanelTests`: idle→free tile assigns, idle→building staffs, refused on a worked tile / a full building — driven via the callbacks directly, as Godot can't synth a headless mouse-drag); the existing click-to-assign tests stay green. **208 L3/L4, 2228 L1/L2, 5 soak green.** See [colonies.md](../systems/colonies.md). | Colony drag-drop (`86d3f6dnd`) |
| 2026-06-25 | **Europe screen — spatial harbour re-layout** (`86d3f6…`, `EuropePanel` only, presentation-only). The six zones were rebuilt from text-row lists into a **spatial two-column sprite/icon layout** that draws real FreeCol art: a `TopRow` of two **bordered titled cards** (`Card`/`CardStyle`) — left "Recruit & train" (3 recruit **portrait cards** + `UnitButtonGrid` 3-col icon grids for Train/Purchase, each button the unit/ship **sprite** + price + name-tooltip, replacing the old long vertical text lists), right "Goods market" (each good a **row with its goods icon** + bid/ask + Buy/Sell); a **ships-in-port** zone of bordered **cards** (ship **sprite** + name + `HoldSlots` drawn as a row of `CargoCapacity` boxes — a filled goods slot shows the **goods icon + amount**, a passenger slot a **portrait**, empty slots faint); a **bordered "Sail" drop panel** (`SailZoneStyle`, arrow/label) + the per-ship `Sail_{id}` fallback; an **on-the-docks** wrapping `HFlowContainer` of colonist **portrait chips** (`DockColonist_{id}` source + Board buttons). New helpers: `UnitSprite`/`UnitPortrait`/`IconRect`/`Display`/`Card`/`CardStyle`/`SailZoneStyle`, and a **`DropZone(drop, body)`** that overlays a bare `EuropeDropTarget` (a plain `Control` that doesn't adopt its child's min size) on a `MarginContainer` so the standalone drop zones don't collapse the layout — **`EuropeDragDrop.cs` untouched**; bare drag sources in containers get a `CustomMinimumSize` + a `FullRect` child for the same reason. **Every control name** and **all behaviour + the Phase-2 drag-drop wiring are unchanged.** **No engine/API/RNG/save change** (ADR-006/009). Self-verified by rendering the (enriched) `EuropePanel_DockAndShip_MatchesGolden` fixture headless and eyeballing the PNG (two columns, icon grids not text lists, ship cards with hold-slot boxes showing goods icons, a portrait-chip docks row, a bordered sail panel); the europe golden stays a render-smoke. No new tests (re-layout only); existing suites all green — **204 L3/L4, 2228 L1/L2, 5 soak**. See [europe.md](../systems/europe.md). | Europe spatial re-layout (`86d3f6…`) |
| 2026-06-25 | **In-game screen legibility (colony + Europe)** (Chris playtest — "the contrast and the text size/boldness"): split `ColonyTheme` into `Get()` (menus/dialogs — unchanged 15px regular Cardo, so their goldens stay byte-valid) and a new **`GetInGame()`** used only by `ColonyPanel` + `EuropePanel`: body 15→17px and the `DefaultFont` wrapped in a `FontVariation { BaseFont = Cardo-Regular, VariationEmbolden = 0.4 }` (Cardo ships Regular-only, so synthetic weight gives bolder, higher-contrast strokes on the parchment without a separate bold `.ttf`), header hierarchy bumped (`SectionHeader` 17→20, `ColonyTitle` 28→30). Self-verified by rendering the colony panel (no clipping; building cells + stat lines still fit). The colony/Europe goldens are already render-smokes; the menu goldens are untouched. Presentation-only, no save change. | In-game legibility (`86d3f6…`) |
| 2026-06-25 | **Europe screen — Phase 2: drag-and-drop layer + ship picker** (`86d3f6…`, `EuropePanel` + new `EuropeDragDrop.cs`). The project's first Godot 4.6 drag-and-drop: two rule-free transport Controls — **`EuropeDragSource`** (`_GetDragData` → a tagged `Godot.Collections.Dictionary` payload + `SetDragPreview`; `default` = nothing draggable, Godot issue 78507) and **`EuropeDropTarget`** (`_CanDropData` validates kind + the `Game.Check*` oracle; `_DropData` re-checks + forwards one command + `Changed()`) — wired **additively** over the Phase 1 buttons (every button still works). **Five drops:** dock colonist→ship (`Board`), ship→sail-zone (`SailToNewWorld`, inline `CanSailToNewWorld` re-check), market good→ship (`BuyEuropeGoods` 100-lot), hold cargo→market (`SellShipCargo` stack), aboard passenger→docks (`DisembarkToDock`, inline `CanDisembarkToDock` re-check); the goods payload's `fromShipId` presence disambiguates Buy vs Sell so the wrong target is rejected. A refused drop is a graceful no-op (never thrown). **Ship picker:** `Select_{id}` (`EuropePanel.SelectTradeShip`, public for L3) sets `_selectedShipId` (▶/"Selected" on the card) and the goods market's Buy/Sell target it, defaulting to the first non-repairing ship and clearing when the ship leaves — **UI-only session state, no save change**. ADR-006/009 (no rule logic in the drag layer; no Random/DateTime). +8 L3 (`EuropePanelTests`: 5 happy-path drops driven via the callbacks directly — Godot can't synth a real headless mouse-drag — + full-ship board refused + unaffordable buy refused + ship-picker buy-into-B); Phase 1 click tests stay green. **204 L3/L4, 2228 L1/L2, 5 soak green.** See [europe.md](../systems/europe.md). | Phase 2 Europe redesign (`86d3f6…`) |
| 2026-06-25 | **Europe-screen adversarial-review fixes** (`86d3f6…` zoned-harbour follow-up, `EuropePanel` + supporting engine oracles). (A/B) the goods *Sell* button used to label itself with the raw pre-tax `Market.BidPrice × amount` (an ADR-006 leak + a wrong figure — the engine credits `Market.Sell(...).GoldAfterTax`, tax-withheld + price-sliding) and had no defensive re-check; it now labels + gates from the new `Game.CheckSellShipCargo(ship, goodsId, amount)` whose `Cost` is the **after-tax proceeds** (backed by the non-mutating `Market.SaleValue`, see [europe.md](../systems/europe.md)/[market.md](../systems/market.md)), and the press re-checks (a stale button no-ops; `SellShipCargo` itself now re-checks too). (C) the *Buy* control is now a `GatedButton` — greyed with a reason tooltip when refused, no longer vanishing (findings #5/#7). (D) a boycotted good shows `boycotted (arrears N)` instead of a misleading sell price; the dead identical-arms `boycott ? "sell {bid}" : "sell {bid}"` ternary is gone (finding #4). (E) `HoldSlots` reads the new engine oracle `Game.GoodsSlotsFor(amount)` instead of a reimplemented `(amount+99)/100` (finding #3, ADR-006). (F) the L3 train-routing test now discriminates `TrainUnit` from `BuyUnit` (the specialist appears only in the Train zone, artillery only in Purchase). (G) +1 L3 asserting a `GatedButton` is **disabled** when unaffordable. **No save bump.** +8 L1/L2 (`MarketTests` `SaleValue`, `SailingTests` `CheckSellShipCargo`), +1 L3 — **2228 L1/L2, 5 soak, 196 L3 green**. See [europe.md](../systems/europe.md) | Phase 1 Europe redesign review (`86d3f6…`) |
| 2026-06-25 | **Europe screen rebuilt into a zoned harbour + train/purchase routing split** (`86d3f6…` Phase 1, click/button only — drag-and-drop deferred to Phase 2). `EuropePanel` went from a flat vertical text list to six zones inside a near-fullscreen scrolling parchment-skinned panel (`EnsureOpaqueBackground` + `ColonyTheme`, the `ColonyPanel` pattern): a **header** (treasury + next-recruit price) with an **immigration `ProgressBar`** (`ImmigrationBar`); a `[recruit/train/purchase | goods-market]` row; **ships in port** (each ship's hold drawn as `CargoCapacity` slot boxes — filled labelled good×amount / passenger, empty blank — replacing "hold 1/2"); a **sail** zone (`Sail_{id}`); and an **on-the-docks** zone (board + carried-home treasure cash-in). The **train/purchase routing fix**: specialists route to `Game.TrainUnit(string)` (flat price, new `Train_{shortName}` buttons over `UnitTypesTrainedInEurope()`), ships/artillery to `Game.BuyUnit(string)` (escalating, `Purchase_{shortName}` over `UnitTypesPurchasedInEurope()`) — the old single dropdown sent everything through `BuyUnit`. Each priced/recruit/buy-goods button labelled with its price and **greyed** (`GatedButton`) via the `Check*` oracle; the goods market shows live bid/ask + boycott (`Market.CanTrade`/`Arrears`) and trades a 100-lot through the first non-repairing in-port ship (`BuyGood_{shortName}`/`Sell_{shipId}_{shortName}`). **Tiny engine change (ADR-006):** added public `Game.CheckTrain(string)`/`TrainUnit(string)`/`EuropeUnitPrice(string)` overloads delegating to the `_human` internals (no save bump — no new persisted state). `main.tscn`: the `EuropePanel` re-anchored to near-fullscreen with margins and a `Scroll` (`Dynamic` moved under `VBox/Scroll`) so the harbour has room. **L4:** the layout change makes the `europe-panel` golden stale and the CI golden pipeline is broken (`86d3f69e9`), so `UiPanelGoldenTests.EuropePanel_DockAndShip_MatchesGolden` is **deferred to a render-without-crash smoke** (no PNG regenerated). +3 L1/L2 (`EuropePurchaseTests`: train path flat-price + no-escalation, `CheckTrain` refusals, the `EuropeUnitPrice(string)` oracle); EuropePanel L3 reworked to the zoned buttons (+train/purchase routing cases) — 2220 L1/L2, 5 soak, 195 L3 green. See [europe.md](../systems/europe.md) | Phase 1 Europe redesign (`86d3f6…`) |
| 2026-06-25 | **Isometric tile-pick accuracy fix** (`86d3f675b`): diamond-tile clicks landed on the wrong tile / missed. Root cause was the **input plumbing**, not the hit-test: `GameController._UnhandledInput` picked via `_mapView.GetLocalMousePosition()` (the **live** OS cursor sampled when the buffered event dispatches), which drifts from the click point while the camera pans/zooms. `MapView.TileAt` was already a correct point-in-diamond pick. **Fix:** new `MapView.TileAtScreen(viewportPosition)` maps the **event's own** screen position through `GetGlobalTransformWithCanvas().AffineInverse()` (camera zoom/pan-correct) into node-local space, then `TileAt`; `HandleTileClick`/`HandleRightClick` now pass `mouseEvent.Position` (right-click keeps a `Vector2?` param defaulting to the live cursor for its direct-invoke L3 test). Reads + transforms only (ADR-006); no GameLogic/save/RNG change; no golden churn. +5 L3 `MapPickingTests` (round-trip per tile, edge/corner→neighbour, click-accuracy at centre/edges/4 neighbours/2× zoom); `InputTests.RightClickTileMenu_*` made deterministic via the event-position path. See [map-terrain](../systems/map-terrain.md) §3. | Phase 7 input (`86d3f675b`) |
| 2026-06-25 | **Custom-house export-control UI** (`86d3f62q8`): `ColonyPanel` gains a **"Custom house — exports"** section (`CustomHouseArea`), rendered by `Rebuild` only when `Game.ColonyHasCustomHouse(colony)` (a new public oracle) — so a colony without a custom house, and the visual golden, are unchanged. It lists every storable+tradeable good (`ExportableGoods()` over `Ruleset.GoodsTypes`, mirroring the engine's `SetColonyExport` gate) as a row of: goods icon, an `Export_{short}` `CheckBox` (Exported on/off, seeded from `Colony.ExportOf`), and a `Retain_{short}` `SpinBox` (keep-level, capped at `Game.ColonyWarehouseCapacity(colony)` — also a new public oracle, step 100). The handlers forward to a new thin `GameController.SetColonyExport(colony, goodsId, exported, retainLevel)` command (wired through `OpenColonyPanel`'s new `Action<Colony,string,bool,int>` arg) wrapping `Game.SetColonyExport` in `try/catch (InvalidMoveException)` (status notice, never thrown to the UI); each handler preserves the un-changed dimension so a toggle never resets the level and vice-versa. **This is the seam that makes a default game's custom house actually sell** — PerGood is the default mode and only a per-good flag (settable nowhere else) enables a sale; the engine auto-sells the flagged surplus each End Turn. Reads + forwards only (ADR-006); no engine-rule/save change (export flags already persist, v28). +2 L3 `ColonyPanelTests` (toggle→retain→EndTurn auto-sells; hidden without a custom house). See [custom-house](../systems/custom-house.md), [colonies](../systems/colonies.md). | Phase 5 colony UI (`86d3f62q8`) |
| 2026-06-25 | **Two-way native trade UI** (`86d3f62qh`): `NativeSettlementPanel` makes the shipped buy/sell/haggle engine reachable. `BuildActions` now adds **Sell goods** (carrier carrying any sellable good) and **Buy goods** (carrier with free hold space + the settlement offering an affordable good) buttons; each opens an in-panel `Screen.TradePick` goods+price list (the ship's cargo for a sell, `Game.GoodsToSell` for a buy, each row priced + gated by `CheckSellToNatives`/`CheckBuyFromNatives`), then `Screen.TradeHaggle` — **Accept** commits via `SellToNatives`/`BuyFromNatives`, **Haggle** pushes one round through `TryHaggleSell`/`TryHaggleBuy` (the natives accept / counter / lose patience). `CommitTrade` re-checks the gate (a refusal sets the outcome, never throws) and reports the oracle's returned gold (the haggle is advisory — the engine charges its standard price). Outcome flows to the existing `onAction` callback (status bar + selection re-sync). Pure presentation over the existing oracles (ADR-006); no GameLogic/save/RNG change; panel hidden by default → no golden churn. +2 L3 `NativeSettlementPanelTests` (drive a buy + a sell through the real buttons, asserting gold/cargo/settlement-stock change). See [natives](../systems/natives.md) §1/§3. | Phase 7 (`86d3f62qh`) |
| 2026-06-25 | **Colony-screen cargo load/unload** (`86d3f5y8r`): `ColonyPanel` gains a *Cargo* section (`CargoArea` over `DockedCarriers()` — the player's `IsCarrier` units on/adjacent to the colony) rendered only when a carrier is present, so the colony golden is unchanged. Each carrier shows `CargoSlotsUsed/CargoCapacity`, a `Load_{id}` *Load goods…* `OptionButton` (warehouse → hold) and `Unload_{id}_{good}` buttons (hold → warehouse), moving a ≤100 lot per click. The new `ColonyPanel.Open` overload takes two `Action<Unit,Colony,string,int>` command delegates, wired by `GameController.OpenColonyPanel` to thin `LoadColonyCargo`/`UnloadColonyCargo` commands that wrap `Game.LoadFromColony`/`UnloadToColony` in `try/catch (InvalidMoveException)` — success/failure both set the status notice, never throw to the UI. A new `RebuildDeferred` re-renders the panel without re-running the host refresh (which would clear the just-set notice). Reads + forwards only (ADR-006); no save/RNG change. +1 L3 `ColonyPanelTests.CargoSection_LoadsAndUnloadsGoods_AndSafelyRefusesAFullHoldOrEmptyWarehouse`. See [transport](../systems/transport.md), [colonies](../systems/colonies.md). | Phase 5 colony UI |
| 2026-06-26 | **Reports stream — REF intelligence, Score/nation, Cargo, Naval tabs + Foreign attitude** (`86d3fq0d8`/`86d3fq0fb`/`86d3fq0e1`/`86d3fq0ee`/`86d3fq0e6`): four new `ColonyReportPanel` tabs + one enhancement, all FreeCol Report*Panel shapes. **REF** (`BuildRef`, FreeCol's expeditionary-force intelligence): the King's army by unit type — a `RefHeader` land/naval-total line then `— Land —`/`— Naval —` sections of `Ref_{type}_{role}` "N × type (role)" rows, from the **new read-only oracle `Game.ExpeditionaryForceComposition()`** (projects the REF `Force` blocks to `RefForceBlock(type, role, count, isNaval)` without materialising the base force — same no-store discipline as `ExpeditionaryForceStrength`). **Score** (`BuildScore`): a `ScoreTotal` line (human score + honorific via `Game.ScoreBreakdown` + `ScoreLevels.ForScore`) over the itemised summands, then a `ScoreRankingHeader` + `Standing_{id}` rows from the **new oracle `Game.NationRanking()`** (colonial/rebel/independent powers, score-descending, "(you)" on the human) — reuses `ScoreComponents`/`PlayerScore` from the victory/high-score screens. **Naval** (`BuildNaval`, FreeCol `ReportNavalPanel`): a standalone `Ship_{id}` fleet breakdown (the Military tab only counts ships). **Cargo** (`BuildCargo`, FreeCol `ReportCargoPanel`): a `Cargo_{id}` row per carrier with hold contents / `[empty]`. **Foreign** enhancement: each rival row now carries its **tension/attitude band** from the **new oracle `Game.TensionLevelBetween(a,b)`** (bands `TensionBetween` through FreeCol's `Tension.Level` thresholds, reusing the `AlarmLevel` enum). All oracles live in the **new `Game.Reports.cs`** partial — pure, RNG-free, never persisted (ADR-006/009). Presentation reads only; **no save/RNG change**; panel hidden by default → **no golden churn**. +3 L1 (`IndependenceTests`: REF-composition, nation-ranking, tension-band) +4 L3 (`ColonyReportPanelTests`: Naval, Cargo, REF, Score) + the Foreign test asserts the attitude column (19/19 green). See [independence.md](../systems/independence.md), [diplomacy.md](../systems/diplomacy.md). | P7 (`86d3fq0d8`/`86d3fq0fb`/`86d3fq0e1`/`86d3fq0ee`/`86d3fq0e6`) |
| 2026-06-24 | **Message-log category filter + persistence** (`86d3f0wdv`/`86d3e959t`, FreeCol's per-type `ModelMessage` `guiShow…` toggles + `MESSAGES_GROUP_BY` + persisting `ModelMessage`s on the Player): `MessageLogPanel` now (1) **tags each notice with a `GameLogic.App.MessageCategory`** (combat/diplomacy/economy/natives/monarch/colony — the controller maps each notice channel to a category in `OnEndTurnPressed` via the `Logged(category, text)` helper; `MessageLogPanel.Entry.Messages` became `IReadOnlyList<LogMessage(MessageCategory, Text)>`) and shows a **`FilterBar` of `Filter_{Category}` checkboxes** that hide every notice of an un-ticked category and drop any turn header that empties out; the hidden set is the live **`SettingsModel.HiddenMessageCategories`** client option (engine-free, L1; serialized to `settings.cfg` only when non-empty so the default config stays unchanged), persisted on each toggle via `SettingsService.Save()`. (2) **Persisted across save/load (save v58→v59)**: the controller round-trips `_messageLog` through the new additive **`SaveGame.MessageLog`** field (`SavedLogMessage(Turn, Category, Text)`), built via a `with` after `SaveGame.From` (presentation owns the strings — ADR-006) and re-grouped per-turn on load by the new `RestoreMessageLog`; **omitted when empty**, so a fresh game serialises **byte-identically to v58** and pre-v59 saves load with an empty log. The log feeds no game evolution → soak twin-determinism green (ADR-009). +6 L1 (`SettingsModelTests` hidden-categories round-trip/omit/unknown-token + default; `SaveGameTests` MessageLog omit-when-empty + JSON round-trip) +2 L3 (`MessageLogPanelTests` filter; `SaveLoadTests` message-log save/load round-trip). All version-pin tests bumped 58→59. **No golden churn** (panel hidden by default). See [save-load.md](../systems/save-load.md), [settings.md](../systems/settings.md), [turns.md](../systems/turns.md). | P7 (`86d3f0wdv`/`86d3e959t`) |
| 2026-06-24 | **Keybinding remap + named input actions** (`86d3f0wjj`): migrated `GameController`'s hardcoded hotkeys to **named `InputMap` actions** (defaults in `project.godot` `[input]`); dispatch is now `@event.IsActionPressed(actionId, exactMatch: true)` and the F1 legend regenerates from the live `InputMap` (can't drift). New engine-free `GameLogic.App.KeyBindingsModel` (action list + overrides + dictionary round-trip, L1) + `KeyBindingsService` (`InputMap` ↔ `settings.cfg` `[keybindings]`) + a `KeyBindingsScreen` rebind overlay reachable from `SettingsScreen`'s new **Key Bindings…** button; `SettingsService._Ready` boot-applies saved overrides. A key can be rebound, persists, survives restart, and the game responds to the new key. Same-frame dedup (now keyed on the raw keycode) + text-focus guard preserved; **soak byte-identical, no save bump (ADR-009)**. +10 L1 (`KeyBindingsModelTests`) +4 L3 (`InputTests.RebindingEndTurnToAnotherKey_*`, `SettingsScreenTests`: Key-Bindings button + rebind-screen list + capture→Back→reload). All 33 `InputTests` + Settings L3 green locally. **`settings-screen` L4 golden** (gained the Key Bindings button) to be **regenerated on Linux CI** — not committed from local (Windows render differs); no golden for `KeyBindingsScreen` yet. See [hud-input.md](../systems/hud-input.md), [settings.md](../systems/settings.md). | Input (`86d3f0wjj`) |
| 2026-06-24 | **Autosave + save-slot UX** (`86d3f0vb8`/`86d3f0vkg`): `SaveLoadDialog` slots are now `HBoxContainer` rows showing **Turn N + saved date/time** (`FileAccess.GetModifiedTime`), each filled slot gaining a **Delete** button (confirm → remove + rebuild); saving over a filled slot pops an **overwrite confirm** (cancellable). A read-only **Autosave** entry (`user://saves/autosave.json`) lists once it exists — loadable but never a manual-save target. `GameController.MaybeAutosave` (end of `OnEndTurnPressed`) writes it via the existing `SaveTo` when `SettingsModel.AutosavePeriod > 0 && Turn % N == 0`; the new "Autosave every N turns (0 = off)" SpinBox lives in a **Game** section of `SettingsScreen` (engine-free `SettingsModel.AutosavePeriod`, int 0–100, default 1, L1). **No save-format bump** (writes the existing format to a dedicated file; the timestamp is read from file mtime; the preference is settings.cfg only). +5 L1 (`SettingsModelTests`: default/clamp/round-trip/garbage/out-of-range) +6 L3 (`SaveLoadTests`: autosave entry appears, not a save target, delete, overwrite cancel/confirm, turn-end writes autosave). **`settings-screen` L4 golden deliberately regenerated** (the new Game section). See [save-load-ui.md](../systems/save-load-ui.md), [settings.md](../systems/settings.md). | Polish (`86d3f0vb8`/`86d3f0vkg`) |
| 2026-06-24 | **In-game help / tutorial screen** (`86d3e98db`, FreeCol's tutorial + contextual help): new `HelpPanel` (`scenes/HelpPanel.tscn`) reachable from a new **Help** button on **both** the main menu and the pause menu (copies the `AboutPanel` overlay pattern — `MainMenu.OnHelp` adds it as a child; `PauseMenu.OnHelp` routes through `TrackOverlay` so it works while paused and parks Esc). A single scrollable page covering the **goal**, the **core loops** (explore → found/run colonies → economy/Europe → liberty/independence → natives → combat), and a **controls/keybindings reference** whose key list restates the authoritative `GameController.BuildKeyBindings` table (+ F1 legend), ending with a `docs/MANUAL.md` pointer. Original wording derived from `docs/MANUAL.md`; presentation-only (ADR-006), binds **no** hotkey (F1 stays the legend, `GameController._UnhandledInput` untouched), **no save change**. Both menu panels grew by one button (Main −275/+275, Pause −258/+258). +3 L3 (`HelpPanelTests`: content + controls-match-key-table + Back→Closed) +2 L3 (`MainMenuTests`/`PauseMenuTests` Help-overlay open/close) + a `help-panel` L4 golden test. **L4 goldens (CI-Linux) pending regen**: `help-panel` (new) + `main-menu`/`pause-menu`/`info-popup` (shifted by the Help button) must be regenerated on the Linux CI gate — local Windows renders differ ~20–26% from the committed Linux goldens (font/GPU), so they are **not** committed from local. See [help.md](../systems/help.md), [main-menu.md](../systems/main-menu.md), [pause-menu.md](../systems/pause-menu.md). | Help (`86d3e98db`) |
| 2026-06-24 | **Report fidelity — nation-grouped Native Affairs Advisor + Foreign-Affairs strength** (`86d3f0wav`/`86d3f0wcg`, FreeCol `ReportIndianPanel` + `ReportForeignAffairPanel`/`NationSummary`): two `ColonyReportPanel` tabs brought up to FreeCol's shape. **Natives** (`BuildNatives`): the flat per-settlement list now **groups discovered settlements under their owning native NATION** — a `NativeNation_{nation}` header (name + discovered count + the tribe-wide tension band with a derived stance) over its settlement rows (capital first), each `Native_{x}_{y}` row gaining the colonial nation it **most hates** (`NativeSettlement.MostHated` resolved via `Game.Players`, or "—"). Tribe tension is approximated by the angriest discovered settlement's band and the stance derived from it (our natives carry no colonial stance; documented). **Foreign** (`BuildForeign`): each rival's row now also shows its **military + naval strength** — the new pure-read oracle **`Game.ColonialStrength(Player)`** (generalises `HumanMilitaryStrength`; `LandPowerOf` + the new `NavalPowerOf`), surfaced **unconditionally** like FreeCol's `NationSummary` (SoL/tax/father stay de-Witt-hidden). Pure presentation (ADR-006) bar the tiny oracle; **no save/RNG change**; opened on demand → no golden churn. +1 L1 (`IndependenceTests.ColonialStrength…`) + 2 L3 (`ColonyReportPanelTests` Foreign-strength + Natives-grouped; 15/15 green). See [natives.md](../systems/natives.md), [players.md](../systems/players.md). | P7 (`86d3f0wav`/`86d3f0wcg`) |
| 2026-06-24 | **Accessibility settings — UI scale + colourblind palette** (`86d3f0vw7`, `86d3f0wgv`): `SettingsScreen` gains an **Accessibility** section — a **UI Scale** slider (75–200%) applied via the root viewport `ContentScaleFactor` (whole-UI live resize, on boot too) and a **Colourblind palette** toggle. New static `AccessibilityPalette` is the single player-colour source for the map's identification markers (Okabe–Ito colour-universal set when on, pass-through to the default ruleset/native/rival colours when off), consumed by `GameController.OwnerColorOf` (unit owner rings) and `MiniMap` (colony/unit/settlement dots). Model fields `SettingsModel.UiScale`/`ColorblindMode` (engine-free, L1). **Persisted in `user://settings.cfg` only — no save-format bump.** Presentation-only (ADR-006). +6 L1 (`SettingsModelTests`: defaults/clamp/round-trip/garbage-safe for both fields). Default off/1.0 → existing owner-ring + minimap goldens unchanged; the `settings-screen` golden now includes the Accessibility section (CI L4 on the PR). See [settings.md](../systems/settings.md) | P7 (`86d3f0vw7`, `86d3f0wgv`) |
| 2026-06-24 | **HUD-input set** (`86d3f0vgd`/`86d3f0vjg`/`86d3f0vuy`/`86d3f0vqf`/`86d3f0vrz` — five cohesive input tasks, one switch): (1) **Disband** — a `DisbandButton` in the Orders row + the **D** key open a `ConfirmationDialog` → `Game.Disband`, gated on `CheckDisband`, clearing the selection. (2) **Hotkey completeness** — `_UnhandledInput` key dispatch + the new **F1 keys legend** are both generated from a single authoritative table (`KeyBindings`/`BuildKeyBindings`, `KeyChord` record-struct); adds **Enter** end-turn, **Ctrl+S**/**Ctrl+O** save/load via the PauseMenu dialog path (new public `PauseMenu.OpenSave`/`OpenLoad`, F5/F9 quicksave unchanged); a hotkey is suppressed while a `LineEdit`/`TextEdit` owns focus. (3) **Skip-unit** — a `SkipButton` + **Space** flag the unit in a session-only `_skippedThisTurn` set (never saved — ADR-009); `Game.NextUnitToMove` gained an optional `IReadOnlySet<int>? skip` so the W-cycle passes a skipped unit over until turn rollover. (4) **Keyboard pan + centre** — `CameraController` pans on the arrow keys (continuous, zoom-scaled, in `_Process`); **Ctrl+C** centres on the selected unit. (5) **Right-click tile menu** — `HandleRightClick` opens a `PopupMenu` to activate each own unit on the tile (fixes stack-select), centre, or arm goto; the right-drag pan still works (the camera consumes the release only after a real drag). Presentation-only (ADR-006) bar the additive `NextUnitToMove` param; **no save change** (ADR-009). +1 L1 (`GotoTests.NextUnitToMove_SkipSet…`) +7 L3 (`InputTests`: disband button/D, Enter, Space-skip, Ctrl+C centre, F1 legend, right-click activate). Build clean; L3 runs on CI (headless GdUnit runtime can't connect in the dev sandbox). See [hud-input.md](../systems/hud-input.md), [units-movement.md](../systems/units-movement.md). | P7 (`86d3f0vgd`/`86d3f0vjg`/`86d3f0vuy`/`86d3f0vqf`/`86d3f0vrz`) |
| 2026-06-24 | **Menu/release metadata — About screen + quit confirmation** (`86d3f0w2x`/`86d3f0vf5`/`86d3f0vy8`): new `AboutPanel` (`scenes/AboutPanel.tscn`, FreeCol `AboutPanel.java`) reachable from **both** the main menu and the pause menu via a new **About** button — shows the game name + `AppInfo.Version`, a GPL v2 disclaimer (→ `LICENSE` + `CREDITS.md`), a copyright line, the repo link, and a `docs/MANUAL.md` pointer (purely informational, reads `GameLogic.App.AppInfo` only). The pause menu's two quit paths (**Quit to Main Menu** + **Quit to Desktop**) now raise a **"Quit without saving?"** `ConfirmationDialog` first (tracked in `_quitConfirm`; Cancel is a no-op) — the title-screen **Quit** stays immediate (no game in progress). +5 L3 (`AboutPanelTests` ×2, `MainMenuTests`/`PauseMenuTests` About-overlay, `PauseMenuTests` ×2 quit-confirm) + the new `about-panel` L4 golden. **Goldens deliberately regenerated** (`main-menu`, `pause-menu`, `info-popup` — the new About button shifts the menu layout; `info-popup`'s host is `MainMenu.tscn`). No save change. See [about.md](../systems/about.md), [main-menu.md](../systems/main-menu.md), [pause-menu.md](../systems/pause-menu.md). | Menus (`86d3f0w2x`/`86d3f0vf5`/`86d3f0vy8`) |
| 2026-06-23 | **Finished reports + warnings/decree notices + in-session message log** (`86d3e4buh`): five report panels finished/added on `ColonyReportPanel` — **Trade** (renamed from Market; adds B4's per-good net units-sold + income before/after tax from `Game.Market.SalesOf`/`IncomeBeforeTaxesOf`/`IncomeAfterTaxesOf` + a net-after-tax summary), **Exploration** (the human's discovered regions from `Game.Map.Regions`), **Requirements** (per-colony bad-assignment / missing-expert warnings via `Ruleset.ExpertForProducing`), **Military** (human land/naval counts + land power vs the REF's counts), and the **Religion** tab gains the per-church cross breakdown. The 14-tab row is now an `HFlowContainer` (wraps). Two **read-only military oracles** added to GameLogic (ADR-006, no behaviour change): `Game.HumanMilitaryStrength()` and `Game.ExpeditionaryForceStrength()` (the latter reads `_refForce ?? BuildBaseRef()` without materialising/storing the base). **Warning + decree notices** now surface after End Turn: new transient (never-saved) notice channels `Game.WarehouseOverflowNotices` (storable goods wasted over the warehouse cap), `Game.ColonyFamineNotices` (a colonist starved but the colony lived), and `Game.MonarchDecreeNotices` (the King's *immediate* actions — lower/waive tax, declare war/peace, free support, REF growth, recorded in `DispatchMonarchAction`); the already-existing `ColonyStarvedNotices` is now surfaced too. New `MessageLogPanel` (`UI/MessageLogPanel` + a **Messages** HUD button, 28th ext_resource, `load_steps` 26→27) keeps an **in-session** history (`GameController._messageLog`) of every turn's notices and re-opens them newest-first. **No save bump (v57 unchanged)** — every new notice channel and the message log are transient/session-only (ADR-009); message-log persistence is deferred. The new notices are human-gated and the monarch ones sit behind the grace period, so the default-game determinism is intact (L2 soak green). +10 L1 (`WarehouseTests`, `ColonyEconomyTests`, `MonarchTests`, `IndependenceTests`) + 7 L3 (`ColonyReportPanelTests` Trade/Exploration/Requirements/Military, `MessageLogPanelTests` ×2). The **`pause-menu` L4 golden was deliberately regenerated** (the new Messages HUD button shows behind the pause dim — the Reports-button precedent); the `colony-seed424242` golden was **not** touched (it has a pre-existing local-GPU diff, passes under CI's xvfb). See [colonies.md](../systems/colonies.md), [monarchy.md](../systems/monarchy.md), [market.md](../systems/market.md), [independence.md](../systems/independence.md). | P7 (`86d3e4buh`) |
| 2026-06-22 | **Forced native land-claim dialog** (`86d3e4bj7`, FreeCol's pre-build `csClaimLand`): when the human founds a colony on, or works, a native-owned tile, a **buy / steal / abandon** choice is now surfaced. `GameController.FoundColony` consults the new `Game.RequiredLandClaim` and, when a claim is required, raises a **code-built `ConfirmationDialog`** (`PromptLandClaim` — OK = Buy at the land price, an added "Steal" button, Cancel = Abandon; Buy disabled when unaffordable) that forwards the chosen `LandClaimChoice` to `FoundColonyWithClaim` → `Game.FoundColony(unit, choice)`. `ColonyPanel.AssignWorkWithClaim` does the same for tile-work clicks (picker + drag + place), routing to `Game.AssignWork(colony, tile, good, choice)`; Abandon leaves the colonist idle. **No pending state** — the founding/working context is held in the dialog's closure and the claim resolves synchronously on click (no `Game`/save change, v55). Presentation-only (ADR-006); a proper `.tscn` panel + L3 coverage are a follow-up (the engine gate is L1/L2-tested). See [natives.md](../systems/natives.md). | P5 (`86d3e4bj7`) |
| 2026-06-22 | **Full negotiation table — human offer-builder + haggle** (`86d3e4bbv`, FreeCol `client/gui/dialog/NegotiationDialog`): `NegotiationPanel` is expanded from the single-clause stance proposal into a **multi-clause offer builder**. A `Negotiate_{id}` target chooser opens a builder that keeps an ordered clause list and an add-control per FreeCol clause kind — **gold** give/demand (±100 spinner), **goods**, **colony** cede/demand, **unit** hand-over/demand (non-native), **incite** a third contacted power, **stance** (de-escalation ladder, one at a time) — each reading a `Game` oracle (`Colonies`/`Units`/`Ruleset.GoodsTypes`/`Player.Gold`/`Colony.StoreOf`/`ForeignNationStances`) for the first eligible item and appending the matching `TradeItem`. **Submit** routes a `DiplomaticTrade(human, rival, Diplomatic)` through `Game.EvaluateTrade` (accept → `Game.SettleTrade`; reject → `Game.CounterOffer` surfaced as Accept/Reject counter). **No new `GameController` method** — the panel calls the public `Game` oracles directly (the established idiom). Presentation-only (ADR-006); signal-safe `RemoveChild`+`QueueFree`; **no rules/save change (no bump)**; the only RNG is inside `Game.CounterOffer` (the AI's own stream). Faithful subset: a single AI accept/counter/reject round (no human-side multi-round haggle). The 3 prior `NegotiationPanelTests` are revised to the builder flow + a new offer→counter→accept journey + a queued-AI-proposal-stays-answerable case. Build clean (0/0); L3 5/5 + MainScene/NativeSettlement regression 5/5 each green headlessly. See [diplomacy.md](../systems/diplomacy.md). | P7 (`86d3e4bbv`) |
| 2026-06-22 | **Native unit sprites — testable selection + L1 guard** (`86d3bmfcx`): the native `brave`/`indianConvert`/`nativeColonist` art and the `armedBrave`/`mountedBrave`/`nativeDragoon` role folders were already imported + wired (so braves already drew their FreeCol sprite, not the red disc), but the path-priority rule lived `private` inside the Godot `UnitMarker` node — untestable headlessly and with **no sprite-selection test** (the item's bar). Extracted that rule into the engine-free **`GameLogic/Specification/UnitSpriteCatalog.CandidatePaths(type, role)`** (role-specific → generic-role → bare-type, mirroring the `Audio/SoundEventCatalog` pattern); `UnitMarker.SetUnit` now walks the catalog and loads the first existing path, disc only if none. **+17 L1** (`UnitSpriteCatalogTests`): unarmed brave/convert/native-colonist → their bare-type sprite, the three equipped-brave roles → role-specific-first order, an on-disk check that every native sprite file the catalog can pick actually exists under `assets/freecol/units/`, + a European regression guard. Pure refactor + tests + provenance note (`indianConvert`/`nativeColonist` named in `PROVENANCE.md`); **no behaviour/RNG/save change (no bump)**; goldens untouched (the rendered paths are byte-identical). ADR-006/013/014. See `game/assets/freecol/PROVENANCE.md`. | P5 (`86d3bmfcx`) |
| 2026-06-22 | **Combat attack animation** (`86d3drn72`, FreeCol `client/gui/animation/Animations.unitAttack` + `mapviewer/UnitAnimator`): a short, non-blocking visual cue when an attack resolves. New presentation node `game/presentation/CombatAnimation.cs` (parented to `MapView/UnitLayer`) — `GameController.PlayCombatAnimation` (called from `ResolveAttackOn` **and** `AttackColonyAt`, each snapshotting the attacker's tile/type/role **before** the roll so a demote/destroy doesn't lose them) spawns one that **lunges** a transient `Sprite2D` from the attacker's tile toward the defender and snaps back over `DurationSeconds` (0.36 s) with an outcome-flavoured flash, then `QueueFree`s itself (the turn never waits). **Approach: procedural, not sprite-sequence** — FreeCol's per-unit-type `.sza` attack frames exist only for the **soldier/dragoon** roles (a `data/default` partial set) and are a FreeCol-specific zipped-frames format unusable as a Godot asset, so the faithful common denominator is FreeCol's own move-toward-defender tween (`UnitMoveAnimation` shape) — **no new art, no external asset, no provenance entry needed.** The `CombatResult`→`CombatAnimationKind` (`AttackerHit`/`AttackerRepelled`/`Evaded`) trigger map lives in the **engine-free** `GameLogic.Combat.CombatAnimationMap`/`CombatAnimationKind` so it is unit-testable headlessly (rendered playback isn't). The attacker sprite is resolved by a new public `UnitMarker.ResolveTexture(type, role)` helper (hoisted from `SetUnit` — no draw-code change), falling back to a coloured disc for art-less units. Presentation-only over the already-resolved `Game.Attack*` (ADR-006); **save version unchanged (no bump)**; non-blocking; hidden/transient → no golden churn. Minimal-testing run: build clean + **+9 L1** (`CombatAnimationMapTests`: every `CombatResult` resolves to exactly one kind, both win grades → hit, both loss grades → repelled, evade → evaded). See [combat.md](../systems/combat.md). | P7 (`86d3drn72`) |
| 2026-06-22 | **Human negotiation dialog + scout-enters-rival-colony entry** (`86d3c9xpt`/`86d3c9ubw`, FreeCol's incoming-treaty dialog + `getScoutForeignColonyChoice`/`moveScoutColony`): new `NegotiationPanel` (`game/presentation/NegotiationPanel.cs` + a hidden `UI/NegotiationPanel` node + a HUD `UI/DiplomacyButton` in `main.tscn`, 27th ext_resource, `load_steps` 25→26) — the human's diplomacy seam over the **already-shipped** backend. **(1)** It drains `Game.TakePendingHumanProposals()` and shows **Accept** (`Game.SettleTrade`) / **Decline** (drop) per queued alliance/cease-fire offer; `GameController.RefreshView` auto-opens it at turn start when `Game.PendingHumanProposals.Count > 0` (non-draining peek; same one-shot idiom as the monarch/mounds/emigration prompts), and `UpdateDefeatUi` hides it on defeat. **(2)** "Propose a treaty" lists contacted rivals (`Game.ForeignNationStances`/`StanceBetween`), offers the de-escalation ladder per current stance, and routes a single-clause `StanceTradeItem` `DiplomaticTrade` through the rival's own `Game.EvaluateTrade` (accept→`SettleTrade`). **(3)** `GameController.TryOpenScoutColonyMissions` intercepts a rival-colony map click when the selected unit passes `Game.CheckSpyOnColony` (FreeCol `moveScoutColony`) → a **Spy** (`Game.SpyOnColony` → `ColonyInteriorSnapshot` glimpse/rebuff) / **Negotiate** menu; a non-scout still falls through to `AttackColonyAt`. Reads oracles + forwards commands only (ADR-006); signal-safe `RemoveChild`+`QueueFree` rebuild; **single-clause bare-stance subset** (no gold sweetener / no human-side haggle — documented). **Presentation-only — no GameLogic/save/RNG change**; the modal is opened on demand → no golden churn. +3 L3 (`NegotiationPanelTests`: pending AI offer auto-surfaces & Accept settles the alliance both ways, Decline drops it untouched + empties the queue, human peace-offer routes through the backend to a terminal stance); build clean, L3 3/3 + MainScene/NativeSettlement regression 5/5. See [diplomacy.md](../systems/diplomacy.md). | P7 (`86d3c9xpt`/`86d3c9ubw`) |
| 2026-06-22 | **New Game game-options section — victory conditions** (`86d3drn64`, FreeCol's `GameOptionsDialog` / `gameOptions.victoryConditions`): `NewGameDialog` gains a **Victory conditions** group of three `CheckBox`es (`VictoryRefCheck`/`VictoryEuropeansCheck`/`VictoryHumansCheck` — defeat the REF / be the last European standing / be the last human standing), each pre-ticked to the ruleset's parsed spec default (REF on / Europeans on / Humans off, read data-driven from the default-variant ruleset). On **Start** they ride the new `GameController.PendingVictoryConditions` static (a nullable `(bool,bool,bool)`; null = no pick → spec defaults) across the scene change; `NewGame`→`StartNewGame` applies them to the **freshly-loaded** ruleset via the new GameLogic **config seam `Ruleset.WithVictoryConditions(defeatRef, defeatEuropeans, defeatHumans)`** before `Game.New`, so `Game.Winner` evaluates exactly the enabled checks. A configuration override of *which already-implemented win checks fire*, **not** a rules change (ADR-006); the three `Ruleset.VictoryDefeat*` props gained a `private set` for the seam. **Faithful subset:** only base game options the engine reads are surfaced — **fog-of-war (`model.option.fogOfWar`) and the other `gameOptions.map` toggles are deliberately omitted** (the engine doesn't consult them yet — fog is always-on FreeCol-default via `Game.CurrentlyVisible`, no off switch; flagged for when the engine honours them). **Session-only — not persisted → no save-version bump (v52 unchanged)** (a reloaded game re-derives the conditions from its variant's spec defaults). `MainMenu.onStart` unchanged (the new pick threads through the existing static-forwarding pattern). Minimal-testing run: build clean + **+1 L1** (`IndependenceTests.WithVictoryConditions_OverridesTheThreeFlags_AndIsHonouredByWinner` — the override flips the three flags, leaves a fresh load at the spec defaults, and is honoured end-to-end by `Game.Winner`; 5/5 green with the existing `DefeatAll*` victory tests). L3 scene test skipped (locally flaky harness per the Tests note; the dialog mirrors the established programmatic OptionButton/CheckBox pattern + compiles clean). See [independence.md](../systems/independence.md), [game-modes.md](../systems/game-modes.md), [main-menu.md](../systems/main-menu.md). | P7 (`86d3drn64`) |
| 2026-06-22 | **Declaration-of-Independence signing screen** (`86d3c9xht`, FreeCol `InGameController.declareIndependence` + `ConfirmDeclarationDialog` + `showDeclarationPanel`): new `DeclarationPanel` (`game/presentation/DeclarationPanel.cs` + a hidden `UI/DeclarationPanel` node + a gated **`IndependenceButton`** HUD action in `main.tscn`). `GameController.RefreshView` shows the button only while `Game.CheckDeclareIndependence(human).Allowed` (and hides it on defeat / once rebelled); `OpenDeclarationPanel` opens the panel. Confirm phase lists the consequences (`ConsequenceEurope` from `Game.UnitsInEurope`, `ConsequenceRef`, `ConsequenceMuster`) + the rebel-sentiment reading (`Game.NationalSonsOfLiberty`); **"Liberty or Death!"** forwards `Game.DeclareIndependence(human)` and shows the signed declaration (verbatim FreeCol `declareIndependence.resolution`); **"Maybe Later"** closes. Opened while ineligible it shows the oracle's gate reason instead. Reads oracles + forwards the one command (ADR-006); signal-safe `RemoveChild`+`QueueFree` rebuild (mirrors `FoundingFatherPanel`). **Presentation-only — no GameLogic/save/RNG change**; hidden by default → no golden churn. +2 L3 (`DeclarationPanelTests`). See [independence.md](../systems/independence.md). | P7 (`86d3c9xht`) |
| 2026-06-22 | **Background music + national anthems** (`86d3c9xu1`, FreeCol `SoundController` default playlist + `playMusic` anthems): FreeCol music copied unmodified into `game/assets/freecol/music/` — a **6-track background playlist** (`el-dorado`, `founders`, `settlers-routine`, `sunrise`, `tailwind`, `fearless-sailors`; **CC BY 4.0**, by Alexander Zhelanov) and **8 national anthems** under `music/anthem/` (`dutch`/`english`/`french`/`spanish`/`danish`/`portuguese`/`russian`/`swedish`; **GPL v2**). Provenance + the two licenses + external-author attribution in that folder's `PROVENANCE.md`; added `assets/licenses/CC-BY-4.0.txt`. New engine-free `GameLogic.Audio` (`MusicContext` enum + `MusicTrackCatalog`: context→`res://`-path-list playlist map + `model.nation.<x>`→anthem map) and a new `MusicService` autoload (`game/presentation/MusicService.cs`, `/root/Music`) that loops a shuffled background playlist on the **Music** bus from startup and plays a one-shot anthem via `PlayAnthem` (then resumes). `GameController.StartGame` cues the human player's anthem (lazy `GetNodeOrNull` → no-op when the autoload is absent, e.g. headless scene tests; no-op for natives/REF/unknown nation). Shuffle uses Godot's RNG — cosmetic order only, outside the seeded game RNG (ADR-009 unaffected). Audio assets only → **save version unchanged (no bump)**. +16 L1 (`MusicTrackCatalogTests`: every context maps to a non-empty unique `res://…/*.ogg` playlist, every European power resolves to its anthem, natives/REF/null resolve to no-anthem — audio playback can't be unit-tested headlessly). See [settings.md](../systems/settings.md). | P7 (`86d3c9xu1`) |
| 2026-06-22 | **Sound-effects pack + playback hookup** (`86d3c9xrp`): a small set of FreeCol GPL-v2 SFX copied unmodified into `game/assets/freecol/sound/` (`colony`, `building`, `attack` (=FreeCol `artillery`), `load`, `sell`, `sunk`, `illegal`, `alert`; provenance + license + external-author attribution in that folder's `PROVENANCE.md`). New engine-free `GameLogic.Audio` (`SoundEvent` enum + `SoundEventCatalog` event→`res://`-path map) and a new `SoundService` autoload (`game/presentation/SoundService.cs`, `/root/Sound`) that preloads the clips and plays them on the **SFX** bus via a round-robin `AudioStreamPlayer` pool. `GameController` plays `ColonyFounded` on founding a colony and `Combat`/`ShipSunk` on resolving an attack (lazy `GetNodeOrNull` lookup → no-op when the autoload is absent, e.g. headless scene tests). Audio assets only → **save version unchanged (no bump)**. +10 L1 (`SoundEventCatalogTests`: every event maps, paths are unique `res://…/*.ogg`, no missing/extra entries — audio playback can't be unit-tested headlessly). See [settings.md](../systems/settings.md). | P7 (`86d3c9xrp`) |
| 2026-06-22 | **High Scores screen + score persistence** (`86d3c9xb1`): new `HighScoresPanel` (`game/presentation/HighScoresPanel.cs` + a hidden `UI/HighScoresPanel` node + a **High Scores** HUD button in `main.tscn`) and `HighScoresService` (`game/presentation/HighScoresService.cs`, the `user://highscores.json` `FileAccess` bridge over the pure GameLogic `HighScoreStore`). `GameController.RecordHighScoreIfGameEnded` records the human's final score on the leaderboard the first time the game ends — a win (`Game.Winner` is the human) or defeat (`Game.IsHumanDefeated`), one-shot via `_highScoreRecorded`, called from `RefreshView`. `OpenHighScoresPanel` (High Scores button) loads the store and shows the panel; the panel renders an already-ranked list (ADR-006 — ranking lives in GameLogic). Stored **separately from the game save** → **save version unchanged (v52)**. Built with the signal-safe `RemoveChild`+`QueueFree` idiom; hidden by default → no golden churn. +3 L3 (`HighScoresPanelTests`: lists best-first w/ honorific+result+Close, empty placeholder, button opens) + 23 L1 (`HighScoreStoreTests`, engine-side). See [scoring.md](../systems/scoring.md). | P7 (`86d3c9xb1`) |
| 2026-06-22 | **Victory / end-of-game stats screen** (`86d3c9xc6`): new `VictoryPanel` (`game/presentation/VictoryPanel.cs` + a hidden `UI/VictoryPanel` node in `main.tscn`) shown when `Game.Winner` is set (REF-defeat independence, or the last-European/last-human alt-victory conditions). Names the winner, lays out the winner's final score line-by-line from the **new pure `Game.ScoreBreakdown(player)` oracle** (unit values / colony liberty / founding fathers / gold / history points + the independence % bonus), and lists end-game stats (colonies, total population, turns, year via `Game.CalendarLabel`). Auto-opened once via a `_victoryShown` one-shot in `GameController.UpdateVictoryUi` (re-armed in `StartGame`), plus `OpenVictoryPanel` for tests. Built into `VBox/Dynamic` with the signal-safe `RemoveChild`+`QueueFree` idiom; reads oracles only (ADR-006). **Presentation-only — no save-version bump** (`PlayerScore`/`ScoreBreakdown` are recomputed-on-read, RNG-free); hidden by default → no golden churn. +2 L3 (`VictoryPanelTests`: independence win opens with score+stats and Closes; no-winner Open is a no-op). See [scoring.md](../systems/scoring.md), [independence.md](../systems/independence.md). | P7 (`86d3c9xc6`) |
| 2026-06-21 | **Monarch action dialog** (`86d3c9xdb`): new `MonarchDialog` (`game/presentation/MonarchDialog.cs` + a hidden `UI/MonarchDialog` node in `main.tscn`) surfaces the home-nation King's two *answerable* demands — a **tax rise** (RAISE_TAX) or a **mercenary offer** (MONARCH/HESSIAN_MERCENARIES). `GameController.RefreshView` opens it (gated `!IsHumanDefeated`, once via `!Visible`) when `Game.PendingMonarchDemand` is set, and `UpdateDefeatUi` hides it on defeat; the dialog reads the demand + forwards Accept/Decline to `Game.RespondToMonarch(bool)` (ADR-006 — no rules in the panel). Mirrors the existing `NativeDemandPanel`/`MoundsDecisionPanel` dim-parchment pattern. The King's *immediate* actions (lower/waive tax, declare war/peace, free support, REF) have no pending seam so never open it. **Presentation-only** — no GameLogic/save/RNG change; hidden by default → no golden churn. +2 L3 (`MonarchDialogTests`: tax demand opens + Accept raises tax; mercenary offer opens + Decline). See [monarchy.md](../systems/monarchy.md). | P7 (`86d3c9xdb`) |
| 2026-06-21 | **Tab-panel rebuild stability fix** (post-wave): `ColonyReportPanel`/`ColopediaPanel` cleared their dynamic children with a synchronous `child.Free()` inside `Rebuild()` — which a tab button's `Pressed` handler calls, freeing that button *mid-signal* ("object freed while a signal is being emitted", an intermittent L3 CI failure the new tabs surfaced). Switched both to **`RemoveChild` (immediate detach, no stale/fresh tab-row collision) + `QueueFree` (deferred free)** — the Godot-safe clear-children idiom. Behaviour-identical; eliminates the flake (0 freed-while-emitting errors, was many). No save/RNG/golden impact. | P7 (wave follow-up) |
| 2026-06-21 | **Colopedia — Concepts help tab** (`86d3drn70`, FreeCol's `ConceptDetailPanel`): `ColopediaPanel` gains a `Cat_Concepts` tab — a free-text **help-topic** view, master-detail: a left `ConceptList` of topic-title buttons (`Concept_{slug}`, the active one disabled) over a right `ConceptDetail` pane (`ConceptTitle` + word-wrapped `ConceptText`). Since there are no ruleset help-strings yet, the eight topics are a small **curated stub set** (Founding a colony, Working tiles & production, Trade & Europe, Sons of Liberty & bells, Founding Fathers, Combat basics, Native relations, Declaring Independence) — the seed of an in-game help/tutorial surface. Pure presentation (ADR-006); no GameLogic/save/RNG change; hidden by default → no golden churn. +1 L3 (`ColopediaPanelTests.ConceptsTab_ListsHelpTopics_AndSelectingOneShowsItsText`) + the tab-switch case extended to assert the Concepts tab. Closes FreeCol's last Colopedia category follow-up. | P7 (`86d3drn70`) |
| 2026-06-21 | **Empire report — Education / Production / Labour tabs** (`86d3drn6f`/`86d3drn6g`/`86d3drn6m`, FreeCol `Report{Education,Production,Labour}Panel`): `ColonyReportPanel` gains three tabs. **Education** lists, per human colony with a teaching building, each `Teaches` building's eligible expert teachers (occupants in the building's `[minimum, maximum]` skill window) and a `Student_{id}_{building}` row for the colonist each will raise (`Ruleset.GetTeachingType`, least-skill-first to echo the engine's automatic pick) with its `done/needed` training progress (`Colony.SchoolTrainingTurnsAt` / `Ruleset.NeededTurnsOfTraining − ProductionBonus`). **Production** adds a `ProductionGood` `OptionButton` over the non-farmed goods; picking one shows a `Production_{id}` row per colony with its net output (`Game.ColonyNetProduction`) and the producers behind it — worked tiles (`Colony.TileWorkers` + `Game.TileYield`) and producing buildings (`BuildingType.Productions`, `workers/workplaces`). **Labour** lists every human colonist grouped by unit type (`Labour_{type}`) with location + job, from the colony worker overlays + on-map `PlayerUnits` persons. Pure presentation (ADR-006) over **existing** oracles — **no new Game oracle**, no GameLogic/save/RNG change; opened on demand so **no golden churn** (both golden suites untouched). +3 L3 (`ColonyReportPanelTests` — Education teacher+student, Production good-selector breakdown, Labour grouped roster; 10/10 green). See [colonies.md](../systems/colonies.md) | P7 (`86d3drn6f`/`86d3drn6g`/`86d3drn6m`) |
| 2026-06-21 | **Role-aware unit sprites (no more red-disc "dots")**: only `caravel`/`freeColonist` had art, so every other unit (soldiers, pioneers, dragoons, scouts, ships, artillery, braves, …) fell back to the red disc. Imported FreeCol's full `units/` set — base type sprites flat (`units/<type>.png`) + FreeCol's per-role subfolders (`units/{soldier,dragoon,scout,pioneer,missionary,infantry,cavalry,armedBrave,mountedBrave,nativeDragoon}/<type>.png`); `UnitMarker.SetUnitType` → `SetUnit(type, role)` resolving role-specific → generic-role → bare-type → disc, with `GameController` passing the unit's role short name. 68 GPL-v2 FreeCol PNGs (PROVENANCE updated). +0 logic; 2 L4 goldens regenerated + eyeballed (`rendered-units`, `native-settlement` — the brave/colonists now draw as sprites with owner-rings); the other goldens unchanged (those units were already `freeColonist`/`caravel`). ADR-006/013/014. | P5 (sprites) |
| 2026-06-21 | **Pioneer-improvement map overlay — roads + plowed fields** (`86d3dqr62`, FreeCol road/plow tile graphics): new `ImprovementOverlay` (`MapView/ImprovementLayer`, second under `MapView` — above the river course, below settlements/units/markers), sibling of `RiverOverlay` for the man-made improvement layer. Draws `GameMap`'s road tiles as a connecting earthen-brown track (spoke-to-midpoint toward each adjacent road tile, dark rim under the surface; a hub disc for a lone segment; roads connect only to roads in the drawing) and plowed tiles as a few parallel furrows inset in the diamond. **Fog-gated** like the terrain (explored only, remembered dimmed); render-time only, **no stored style/save bump** (ADR-006). Wired via `GameController.RefreshView` (`_improvementLayer.ShowState`, beside `MapView`/`RiverOverlay`); scene gains `MapView/ImprovementLayer`. A fresh game has no pioneer improvements → the overlay draws nothing in any captured state, so **every existing golden is byte-identical (zero golden regen)** — confirmed by re-running the golden suite with no diffs. +2 L3 (`ImprovementOverlayTests` — road track + plow furrow drawn on explored tiles, nothing on a fogged one). Closes the "draw roads/plowed fields on the map" follow-up. See [rivers-tile-improvements.md](../systems/rivers-tile-improvements.md) | P5 (`86d3dqr62`) |
| 2026-06-21 | **Pioneer build buttons on `SelectedUnitPanel`** (`86d3dqr62`): the selected-unit panel gains **Road** / **Plow** / **Clear** buttons beside Fortify / Sentry / Wake — `ApplyUnitOrder` → `Game.BuildImprovement(unit, id)`, each disabled via `Game.CheckBuildImprovement(unit, id)` (ADR-006 oracle, re-evaluated every `RefreshView`); `DescribeSelectedUnit` shows a "building &lt;improvement&gt; (N)" readout while the pioneer works, and Wake stays enabled to abandon an in-progress build. `main.tscn` widened the panel + added the three buttons. No GameLogic/save change here (the build action shipped in the same commit's GameLogic); hidden in golden states → no golden churn (32 scene tests + 8 goldens green). +1 L3 (`InputTests.SelectedUnitPanel_RoadButton_StartsBuildingARoad`). See [rivers-tile-improvements.md](../systems/rivers-tile-improvements.md) | P5 (`86d3dqr62`) |
| 2026-06-21 | **Fountain-of-Youth recruit choice** (`86d3c9ujx`): the `EmigrationChoicePanel` now also drives the Fountain-of-Youth immigration burst — when `Game.PendingEmigration.IsFountainOfYouth`, `Open` shows a "A Fountain of Youth!" title + "(N to choose)" info and re-opens for each of the `dx` picks (the engine re-arms `PendingEmigration` until the burst is spent). The same `Choose_{i}` → `Game.ChooseEmigrant(slot)` flow as Brewster emigration. Presentation only (ADR-006) — the routing + the no-Europe gate live in GameLogic. Hidden by default → no golden churn. +1 L3 (`EmigrationChoicePanelTests.FountainOfYouthChoice_…`). See [immigration.md](../systems/immigration.md), [lost-city-rumours.md](../systems/lost-city-rumours.md) | P7 (`86d3c9ujx`) |
| 2026-06-21 | **Rumour outcome notice** (`86d3c9umy`): a human's non-mounds Lost-City-Rumour explore now surfaces in the status bar — `GameController.RefreshView` drains `Game.TakeRumourNotices()` (the new `RumourNotice` channel) into the one-line `_notice`, so an outcome that previously resolved silently ("Tribal chiefs share their treasure with you!", "You uncover ancient ruins — treasure!", "You find nothing of note.", …) is reported. Drained in `RefreshView` (not only `OnEndTurnPressed`) so both an interactive move and a standing-goto walked during `EndTurn` show their result. The message is pre-formatted in GameLogic (keeps `LostCityRumourType` out of the UI, ADR-006). Presentation drain only; no save/RNG change; status text only → no golden churn. +1 L3 (`InputTests.ExploringARumour_BySteppingOntoIt_ShowsTheOutcomeInTheStatusBar`) + 8 L1 in GameLogic. See [lost-city-rumours.md](../systems/lost-city-rumours.md) | P7 (`86d3c9umy`) |
| 2026-06-21 | **River map overlay** (`86d3b3qdx`, FreeCol river styling): new `RiverOverlay` (`MapView/RiverLayer`, first under `MapView` so above terrain, below settlements/units) draws `GameMap`'s river improvement layer as connected blue courses — each explored river tile spokes to its drawn river neighbours' midpoints into a continuous course (dark rim under a water fill; a pool for a lone tile), **fog-gated** like the terrain. Drawn small/large width is derived **at render time** from connected-component length (no stored style, **no save bump**). Wired via `GameController.RefreshView` (`_riverLayer.ShowState`). Pure presentation (ADR-006). The 7 existing map goldens are **unchanged** (no generated river falls in their start views); a new `river-seed424242` golden (a deterministic winding course around the start unit) was added + eyeballed. +1 L3 (`RiverOverlayTests`, fog-gated render) + 1 L4. See [rivers-tile-improvements.md](../systems/rivers-tile-improvements.md) | P5 (`86d3b3qdx`) |
| 2026-06-20 | **Emigration choice dialog** (`86d3c9xft`, FreeCol `selectRecruit`): new `EmigrationChoicePanel` (`UI/EmigrationChoicePanel`, hidden by default) — when a human who has elected William Brewster is due an emigrant, `GameController.RefreshView` opens it (gated on `!IsHumanDefeated`) over `Game.PendingEmigration`, offering a `Choose_{i}` button per dock recruit; choosing one calls the new `Game.ChooseEmigrant(slot)` and reports the outcome. Backed by a new `Game.Emigration.cs` partial (`PendingEmigrationChoice`/`PendingEmigration`/`ChooseEmigrant`) + a tiny pause seam in `AccumulateImmigrationAndEmigrate` (human-with-Brewster only — every other player keeps the random auto-emigrate, RNG/goldens byte-identical). **In-memory only** (no save bump this wave). Presentation reads/forwards only (ADR-006). Hidden by default → zero goldens moved. +5 L1 (`EmigrationChoiceTests`) + 1 L3 (`EmigrationChoicePanelTests`). See [immigration.md](../systems/immigration.md), [game-logic.md](game-logic.md) | P7 wave-2 D (`86d3c9xft`) |
| 2026-06-20 | **History report tab** (`86d3c9x53`, FreeCol `ReportHistoryPanel`): `ColonyReportPanel` gains a **History** tab listing the human's notable past events (`Game.History`) in turn order — `History_{i}` "Turn N: …" rows for colonies founded / wars entered / Founding Fathers elected, under a `HistoryNote` header. Backed by a new **in-memory** `Game.History` event list (a `Game.History.cs` partial with `HistoryEvent`/`HistoryEventKind`): the engine appends an event from three tiny seams — `FoundColony` (human-owned), `SetStance`→War (human party, once on the transition), and the father-election point in `AccumulateLibertyAndElectFathers` (human). **Not persisted this wave** (no save bump — a saved log is a follow-up); a reloaded game's history starts empty. Presentation reads only (ADR-006). +1 L1 (`HistoryTests`, 4 cases) + 1 L3 (`ColonyReportPanelTests.HistoryTab_ListsNotablePastEvents`); golden suites green, **zero moved**. See [game-logic.md](game-logic.md) | P7 wave-2 D (`86d3c9x53`) |
| 2026-06-20 | **Colopedia — remaining categories** (`86d3c9x9t`, FreeCol `Colopedia`): `ColopediaPanel` gains a `Cat_{Category}` tab row (mirroring `ColonyReportPanel`'s tabs) switching the existing Goods view to six more read-only ruleset views — **Terrain** (kind/move/defence/worked-for), **Units** (kind/movement/combat/how-obtained), **Buildings** (pop/workplaces/cost), **Founding Fathers** (category + plain effect summary), **Nations** (playable + advantage, REF skipped), **Resources** (unscoped yield bonuses) — each a named `<Category>_{shortName}` row over a `Game.Ruleset` collection. Pure presentation (ADR-006) over existing oracles; no GameLogic/save/RNG change; hidden by default → no golden churn (both golden suites green, **zero moved**). Only FreeCol's free-text **Concepts** category remains a follow-up. +1 L3 (`ColopediaPanelTests.CategoryTabs_SwitchToTheOtherRulesetViews`). | P7 wave-2 D (`86d3c9x9t`) |
| 2026-06-20 | **Colopedia reference panel — Goods category** (`86d3c9x9t`, FreeCol `Colopedia`): new `ColopediaPanel` (`UI/ColopediaPanel`, hidden by default), opened by a **Colopedia** button / the **C** key (`OpenColopediaPanel`). Lists every `Game.Ruleset.GoodsTypes` entry as a `Goods_{shortName}` row with the good's name + kind (food/raw/New World/refined/manufactured) + its market sell/buy price (`Game.Market`) for tradeable goods. Pure presentation (ADR-006) over `Game.Ruleset`/`Game.Market`; no GameLogic/save/RNG change; hidden by default → no golden churn (both golden suites green, **zero moved**). Scoped to the Goods category for this slice; the other Colopedia categories (Terrain/Units/Buildings/Fathers/Nations/Resources/Concepts) are a follow-up. +1 L3 (`ColopediaPanelTests.ColopediaButton_OpensTheGoodsReference_ListingEachGood_AndCloses`). See [market.md](../systems/market.md) | P7 (`86d3c9x9t`) |
| 2026-06-20 | **End-of-turn message panel** (`86d3c9x53`, FreeCol `ReportTurnPanel`): new `TurnMessagePanel` (`UI/TurnMessagePanel`, hidden by default) — `OnEndTurnPressed` now formats every AI-phase notice channel (`Game.CombatNotices`/`ColonyRaidNotices`/`ColonyLossNotices` **+ the newly-routed `Game.CustomHouseSaleNotices`** via `FormatCustomHouseSaleNotice`, + the defeat line) into player-facing rows and routes them into this dismissible panel (one `Message_{i}` row each, **OK** to close) instead of concatenating them into the one-line status bar. The formatting helpers stay in `GameController`. Pure presentation (ADR-006); no GameLogic/save/RNG change; hidden by default → no golden churn (VisualGolden + MenuGolden suites green, **zero moved**). +1 L3 (`InputTests.TurnMessagePanel_AfterARaid_ListsTheNotice_AndOkDismissesIt`); the two existing notice L3s + the defeat L3 retargeted from the status bar to the panel rows. | P7 (`86d3c9x53`) |
| 2026-06-20 | **Continental Congress report tab** (`86d3c9x53`): `ColonyReportPanel` gains a **Congress** tab — a `CongressProgress` header (the father being recruited + banked-liberty progress `Game.Liberty`/`Game.TotalFoundingFatherCost()`) and one `Father_{shortName}` row per offered father (`Game.OfferedFathers`) with its category (`FoundingFather.Type`), marking the in-progress pick. **Read-only** (the election stays in `FoundingFatherPanel`). Pure presentation (ADR-006) over existing oracles; no GameLogic/save/RNG change; hidden by default → no golden churn. Carves out the Congress facet of the Empire-reports epic. +1 L3 (`ColonyReportPanelTests.CongressTab_ShowsTheFoundingFatherElectionState`). See [founding-fathers.md](../systems/founding-fathers.md) | P7 (`86d3c9x53`) |
| 2026-06-20 | **HUD tile-info readout** (`86d3c9x6y`, tile-info facet): new `TileInfoPanel` (top-left HUD) describes the **last-clicked tile** — terrain + bonus resource + occupant (your colony / a `<nation>` settlement / an on-map unit's type), via the new `GameController.DescribeTile(Position)` over existing `Game` oracles (`IsExplored`/`Map.TerrainAt`/`ResourceAt`/`ColonyAt`/`NativeSettlementAt`); unexplored tiles read "Unexplored". `HandleTileClick` records `_inspectedTile`; the readout is empty/hidden until a tile is clicked → no golden churn, and `mouse_filter=Ignore` on panel + children (no map-click steal). Closes the residual tile-info sub-slice of the map-controls panel (`SelectedUnitPanel`/orders/minimap already shipped). Pure presentation (ADR-006); no GameLogic/save/RNG change. +1 L3 (`InputTests.ClickingATile_ShowsTheTileInfoReadout_WithTerrainAndOccupant`). | P7 (`86d3c9x6y`) |
| 2026-06-20 | **Market report tab** (`86d3dmn6d`): `ColonyReportPanel` gains a **Market** tab — a `Market_{good}` row per tradeable good showing its sell (bid) / buy (ask) price + boycott flag, read from `Game.Market`. Pure presentation (ADR-006) over existing oracles; no GameLogic/save/RNG change; hidden by default → no golden churn. Carves out the Trade facet deferred from Empire reports batch 1. Sales/income columns scoped out (need an accumulator + save bump). +1 L3 (`ColonyReportPanelTests.MarketTab_ListsTradeableGoodsWithPrices`). See [market.md](../systems/market.md) | P7 (`86d3dmn6d`) |
| 2026-06-20 | **Pre-combat odds dialog** (`86d3c9xmw`): new `PreCombatPanel` modal — clicking an adjacent enemy unit now opens it (via `AttackUnitAt`) showing both combat powers + the win chance from the side-effect-free `Game.CombatOddsAgainst` oracle, with **Attack** (rolls the real `Game.Attack` in `ResolveAttackOn`) / **Cancel** (no-op). Pure presentation (ADR-006) over the new oracle; no save change; ADR-009 byte-stable (no RNG until confirmed); hidden by default → no golden churn. +1 L3 (`InputTests.CancellingThePreCombatDialog_LeavesTheAttackerUntouched`) + the existing attack L3 updated to drive the dialog. See [combat.md](../systems/combat.md) | P7 (`86d3c9xmw`) |
| 2026-06-20 | **Standing-orders buttons on `SelectedUnitPanel`** (`86d3dmh6b`): the selected-unit panel gains **Fortify** / **Sentry** / **Wake** buttons (`ApplyUnitOrder` → `Game.Fortify`/`Sentry`/`ClearOrders`), each disabled via its `Check…` oracle (ADR-006). Panel + label stay `mouse_filter=Ignore`; only the buttons take clicks. Pairs with the next-unit cycle (which skips fortified/sentry units). Disband scoped out. No GameLogic/save change; hidden in golden states → no golden churn. +1 L3 (`InputTests.SelectedUnitPanel_FortifyButton…`). See [units-movement.md](../systems/units-movement.md) | P7 (`86d3dmh6b`) |
| 2026-06-20 | **"Next unit needing orders" cycle** (`86d3dmfvq`, key **W**): `GameController.SelectNextUnitToMove` selects the next of the human's units still needing orders (`Game.NextUnitToMove`) and centres on it (`CenterCameraOnTile`); no-op notice when none remain. Pure presentation (ADR-006) over a shipped, tested oracle; no GameLogic/save change; no golden churn (a key). +1 L3 (`InputTests.PressingW_SelectsTheNextUnitNeedingOrders_AndCentresOnIt`). Closes the goto "cycling-to-next-unit UI" follow-up. See [units-movement.md](../systems/units-movement.md) | P7 (`86d3dmfvq`) |
| 2026-06-20 | **Selected-unit HUD info line** (`86d3dmej6`, sub-slice of `86d3c9x6y`): new `SelectedUnitPanel` (top-left, shown only while a unit is selected) reads the selected unit's type / moves / role / orders / goto via `DescribeSelectedUnit` (`Unit` reads only). Presentation-only (ADR-006); `mouse_filter=Ignore` (no click-steal); hidden in golden states → no golden churn. +1 L3 (`InputTests.SelectingAUnit_ShowsTheSelectedUnitInfoPanel`). The order-button cluster + tile info stay on the parent `86d3c9x6y`. | P7 (`86d3dmej6`) |
| 2026-06-20 | **On-map rumour-marker glyph** (`86d3dm9tq`): new drawn `RumourMarker` (mound + glint, no art) on each explored Lost-City-Rumour tile, placed by `SyncRumourMarkers` into a new `MapView/RumourLayer`, fog-gated like settlement markers. Presentation-only (ADR-006); no GameLogic/save change. Existing goldens unchanged; +1 new L4 (`rumour-marker-seed424242`) + 1 L3 (`RumourMarkerTests`). The last Lost-City-Rumour presentation slice. See [lost-city-rumours.md](../systems/lost-city-rumours.md) | P7 (`86d3dm9tq`) |
| 2026-06-20 | **Strange-mounds decision panel** (`86d3cqqu5`): new `MoundsDecisionPanel` (mirrors `NativeDemandPanel`) — opened from `RefreshView` when `Game.PendingMounds` is set; **Investigate**/**Leave them be** call the new public `Game.ResolvePendingMounds(bool)` oracle (wraps the internal investigate/decline + returns a player-facing outcome). Presentation-only (ADR-006) bar the thin oracle; no save/RNG change; hidden by default → no golden churn. +3 L1 (`LostCityRumourTests.ResolvePendingMounds_*`) + 2 L3 (`MoundsDecisionPanelTests`). See [lost-city-rumours.md](../systems/lost-city-rumours.md) | P5 (`86d3cqqu5`) |
| 2026-06-20 | **Map-side board / disembark** (`86d3c9t4v`): `HandleTileClick` now embarks a selected land unit when it clicks an adjacent/same-tile friendly carrier (`Game.CheckBoard`/`Board`) and puts a passenger ashore when a selected carrier clicks an adjacent land tile (`Game.CheckDisembark`/`Disembark`, the first eligible `Passengers`) — the same oracles the Europe screen uses, now reachable on the map. Presentation-only (ADR-006); no GameLogic/save change. +1 L3 (`InputTests.ClickingAnAdjacentShip_Boards_AndClickingLand_Disembarks`). See [transport.md](../systems/transport.md) | P5 (`86d3c9t4v`) |
| 2026-06-20 | **GoTo orders + destination UI** (`86d3c9xjy`): the **G** key arms goto-target mode for the selected unit; the next map click forwards to `SetSelectedDestination` → `Game.CheckSetDestination`/`SetDestination` (the per-turn `ProcessGotos` walk was already shipped + tested). A new `GotoMarker` (`MapView/GotoMarker`) outlines the standing destination tile, shown/positioned in `RefreshView`. Presentation-only (ADR-006); no GameLogic/save change; hidden by default → no golden churn. +1 L3 (`InputTests.GotoMode_Arms…`). See [units-movement.md](../systems/units-movement.md) | P5/P7 (`86d3c9xjy`) |
| 2026-06-20 | **Status report tabs** (`86d3c9x3c` — Foreign Affairs / Native / Religion): added three tabs to `ColonyReportPanel` — Foreign (rival powers' stance/#colonies/#units/gold via `Game.Players`/`StanceBetween`, faithful to FreeCol `NationSummary`), Natives (discovered settlements' alarm/skill/mission/wanted goods, fog-gated on `IsExplored`), Religion (the `Immigration`/`ImmigrationRequired` crosses bar). Presentation-only (ADR-006) over existing oracles; no GameLogic/save change; hidden by default → no golden churn. +1 L3 (`ColonyReportPanelTests.StatusTabs…`). **Exploration** facet deferred (region-discovery logic is missing — its own slice); per-church cross breakdown deferred. | P5/P7 (`86d3c9x3c`) |
| 2026-06-20 | **Unit report tab** (`86d3c9x15` — Labour/Military/Naval/Cargo): `ColonyReportPanel` is now a tabbed empire-reports screen (`Tab_Colonies`/`Tab_Units`); the Units tab groups the human's units into Military (`Game.IsMilitaryUnit` — a new pure, L1-tested oracle: non-naval offensive type or role), Naval, Cargo (carriers + hold contents), Labour (person residual), each with role + location. Presentation-only (ADR-006) bar the tiny oracle; no save change; hidden by default → no golden churn. +1 L1 (`UnitCategoryTests`) + 1 L3 (`ColonyReportPanelTests.UnitsTab…`). Faithful-subset: an unarmed veteran (expertSoldier ability, unmodelled) lists under Labour for now. See [units-movement.md](../systems/units-movement.md) | P5/P7 (`86d3c9x15`) |
| 2026-06-20 | **Founding Father choice dialog** (`86d3c9xet`): new `FoundingFatherPanel` (key **F**) lists the offered fathers (one per category) + the current recruit + liberty-to-elect; choosing one calls `Game.ChooseFather`. Presentation-only (ADR-006) over `Game.OfferedFathers`/`CurrentFather`; no GameLogic/save change; hidden by default → no golden churn. +1 L3 (`FoundingFatherPanelTests`). | P5 (`86d3c9xet`) |
| 2026-06-20 | **Find Settlement dialog** (`86d3c9xkx`): new `FindSettlementPanel` (key **L**) lists the human's colonies and recenters the camera on the chosen one (`CenterCameraOnTile`). Presentation-only (ADR-006) over `Game.Colonies`; no GameLogic/save change; hidden by default → no golden churn. +1 L3 (`FindSettlementPanelTests`). | P5/P7 (`86d3c9xkx`) |
| 2026-06-20 | **Empire colony report** (`86d3c9wz6`, batch 1 — Colony/Production/Requirements): new `ColonyReportPanel` (opened by a **Reports** button) lists each human colony's population / SoL% / bonus / net food / current build + materials-required / net production. The net-production fold was **lifted out of `ColonyPanel.NetProduction()` into a tested `Game.ColonyNetProduction(colony)` oracle** so the colony screen + the report + L3 tests share one source. Presentation-only (ADR-006); the only logic is the pure oracle (no save/RNG change). +1 L1 (`ColonyEconomyTests.ColonyNetProduction…`) + 2 L3 (`ColonyReportPanelTests`); the `pause-menu` L4 golden was **deliberately regenerated** (the new Reports button shows behind the pause dim). See [colonies.md](../systems/colonies.md) | P5/P7 (`86d3c9wz6`) |
| 2026-06-20 | **Corner minimap** (`86d3c9x64`): new `MiniMap` node (`UI/MiniMap`, bottom-left) draws the explored world as scaled flat-colour terrain cells + colony/native/unit dots (fog-respecting), recenters the main camera on click (`TileSelected` → `GameController.CenterCameraOnTile`), and zooms via +/- buttons (`PixelsPerTile` steps). Reads `Game` fog/terrain/entity oracles only (ADR-006); no GameLogic/save change. +3 L3 (`MiniMapTests`: loads, click-recenters, zoom) + 1 new L4 golden (`minimap-seed424242`, cropped to the panel's opaque rect). The `pause-menu` L4 golden was **deliberately regenerated** (the minimap shows behind the translucent pause dim, bottom-left). See [turns.md] map docs / [map-terrain.md](../systems/map-terrain.md) | P5/P7 (`86d3c9x64`) |
| 2026-06-20 | **Dedicated HUD year/season readout** (`86d3cz54z`): a `UI/CalendarPanel/CalendarLabel` near the turn controls (above the Trade Routes button) shows `Game.CalendarLabel` ("1492" → "Spring 1600"), set in `GameController`'s view refresh, distinct from the dev status string. Presentation-only (ADR-006); no GameLogic/save change. +1 L3 (`MainSceneTests.CalendarHud_ShowsTheDate_AndAdvancesWithTheTurn`); the `pause-menu` L4 golden was **deliberately regenerated** (the new panel shows through the 0.55-alpha pause dim, as the other HUD controls already do). See [turns.md](../systems/turns.md) | P5 (`86d3cz54z`) |
| 2026-06-21 | **Map-side "Sail to Europe" order** (bug fix): added a `SailToEuropeButton` to the selected-unit HUD (`main.tscn`) wired in `GameController` (`ApplyUnitOrder(CheckSailToEurope, SailToEurope)`); shown only for naval units, enabled only on a high-seas tile. The existing engine command had no UI, so a ship on the map could not be sent to Europe. Presentation-only (ADR-006); no engine/save/golden change. +1 L3 (`InputTests`). See [europe.md](../systems/europe.md) | P5 (bug fix) |
| 2026-06-25 | **"Cash in treasure" reframed as the King's offer** (`86d3fb5mj`): with the cash-in moved to the Col1 fee model (the King's at-colony cut = the player's tax rate; carry-home is full value), the `CashInSelectedTreasureTrain` `ConfirmationDialog` text now reads as the **King's transport offer** at a colony — "The King will carry your {train}'s {amount} gold to Europe and take {`Game.TaxRate`}% ({`Game.TreasureKingsCut`}g); you keep {`CashInValue`}g. (Carry it home on a galleon yourself to keep all {amount}g.)" — and as the full fee-free, tax-free amount in Europe. Uses the new public `Game.TreasureKingsCut` oracle alongside the existing `TreasureCashInIsFeeFree`. Presentation-only (ADR-006) bar the additive oracle; no save/golden change. L3 (`InputTests`) updated: the at-colony case now stages 25% tax and asserts the "25%"/"750" wording. See [treasure-train.md](../systems/treasure-train.md), [monarchy.md](../systems/monarchy.md) | P6 (`86d3fb5mj`) |
| 2026-06-25 | **Map-side "Cash in treasure" order** (`86d3f62q1`): added a `CashInTreasureButton` to the selected-unit HUD (`main.tscn`) wired in `GameController` (`CashInSelectedTreasureTrain` → a `ConfirmationDialog` showing the net `CashInValue` + the King's-cut-vs-fee-free line via the new public `Game.TreasureCashInIsFeeFree` oracle → `Game.CashInTreasureTrain`); shown only for treasure-carrying units, enabled only when `CheckCashInTreasureTrain` allows it (at an owned colony, or aboard a galleon docked in Europe), a refusal lands in the status bar. The existing engine cash-in had no UI — only the AI could call it. Presentation-only (ADR-006) bar the additive oracle; no save/golden change. +2 L3 (`InputTests`). See [treasure-train.md](../systems/treasure-train.md) | P5 (bug fix) |
| 2026-06-25 | **Europe-screen cash-in for a carried-home treasure train** (bug fix): the map-side Orders button only reaches the selected *on-map* unit, so a treasure train carried home on a galleon and unloaded to the Europe dock had **no reachable cash-in** — `EuropePanel` had no treasure handling, the train fell through every section, and the fee-free play dead-ended at zero gold. Added `EuropePanel.CashInButton(train)` (a `CashIn_{id}` button labelled `Cash in treasure ({CashInValue}g)`) in two spots in `BuildDynamicSections`: beside a train **aboard a galleon docked in Europe** in the ship's hold list, and under a new **"Treasure"** section for a train **on the dock** (Location InEurope, neither carrier nor person). Both gated on `Game.CheckCashInTreasureTrain` and forwarding to `Game.CashInTreasureTrain` (banks the fee-free net, consumes the train, frees the hold, refreshes). Confirmed the engine preserves `TreasureAmount` through the carry-home + unload (no value-loss bug). Presentation-only (ADR-006); no engine/save/golden change. +2 L3 (`EuropePanelTests`). See [treasure-train.md](../systems/treasure-train.md) | P5 (bug fix) |
| 2026-06-21 | **New-game Map source (America)** (slice C): `NewGameDialog` gains a **Map** dropdown (Random New World / America (fixed)); picking America disables the size/land dropdowns (a fixed map sets its own dimensions). The menu forwards it via a new `GameController.PendingMapSource` static through `StartNewGame(seed, size, land, difficulty, mapSource)` → `Game.New(mapSource:)`. Presentation-only (ADR-006); the `FixedMap` loader + decorate pass live in GameLogic. Default unchanged (Random) → no golden churn (the on-demand dialog isn't in a golden frame). +1 L3, +1 L3-updated (`MainMenuTests`). See [map-terrain.md](../systems/map-terrain.md) | P5 (slice C) |
| 2026-06-20 | **New-game world-options dialog** (`86d3c9w9c`): `NewGameDialog` (programmatic overlay) lets the player pick world size + land mass before starting; the menu's **New Game** opens it and forwards the choice via `GameController.PendingWorldSize`/`PendingLandMass` → `Game.New`. Presentation-only (ADR-006). Default unchanged → map goldens untouched. +2 L3 (`MainMenuTests`). See [map-terrain.md](../systems/map-terrain.md) | P5 (`86d3c9w9c`) |
| 2026-06-22 | **New-game fog-of-war toggle** (`86d3dzdw3`): `NewGameDialog` gains a **Fog of war** `CheckBox` (`FogOfWarCheck`, pre-ticked to the ruleset's spec default — classic on); it forwards via a new `GameController.PendingFogOfWar` static through `StartNewGame(…, fogOfWar:)` → the config seam `Ruleset.WithFogOfWar`. With fog off the engine keeps every explored tile permanently visible (`Game.CurrentlyVisible`/`IsVisible`). Presentation-only (ADR-006); session-only override, not persisted (no save bump). Default (fog on) unchanged → no golden churn (the on-demand dialog isn't in a golden frame). The engine behaviour + L1 tests live in GameLogic. See [fog-of-war.md](../systems/fog-of-war.md) | P7 (`86d3dzdw3`) |
| 2026-06-20 | **Colony build-queue editor** (`86d3c9p63`): `ColonyPanel.ConstructionPanel` rebuilt to show the full `BuildQueue` (each item reorderable ▲/▼ + removable ✕), an **Add to queue** picker listing buildings *and* units, and a *Clear queue*; queued ids resolved via `Game.DescribeBuildable` (a queued unit no longer mis-renders/crashes as a building). Presentation-only (ADR-006); drives new `Game.RemoveFromBuildQueue`/`MoveBuildQueueItem` + the existing `EnqueueBuild`. +2 L3 (`ColonyPanelTests`: enqueue+remove a unit, reorder). See [colonies.md](../systems/colonies.md) | P5 (`86d3c9p63`) |
| 2026-06-20 | **Trade-route management UI** (`86d3c9rrd`): `TradeRoutePanel` (list routes + per-route assign-carrier dropdown + delete; new-route from→to + load-good quick-create), wired into `GameController` via `OpenTradeRoutePanel` + a **Trade Routes** button. Presentation-only (ADR-006); drives `Game.CreateTradeRoute`/`AssignTradeRoute`/`RemoveTradeRoute`. +3 L3 `TradeRoutePanelTests`. See [trade-routes.md](../systems/trade-routes.md) | P5 (`86d3c9rrd`) |
| 2026-06-18 | **Save/Load dialog** (title/settings Slice F): five-slot `SaveLoadDialog`; main-menu **Load Game** boots a save via `GameController.PendingLoadPath`; pause-menu **Save/Load** via `GameController.SaveTo`/`LoadFrom` + an `InfoPopup`. +`SaveLoadTests` (L3). See [save-load-ui.md](../systems/save-load-ui.md) | a804549 |
| 2026-06-18 | **Reusable info popup** (Slice E): `InfoPopup` modal + static `Show(host,title,message)`; +`InfoPopupTests` (L3) + `info-popup` golden. See [info-popup.md](../systems/info-popup.md) | a804549 |
| 2026-06-18 | **Bundled UI font + menu goldens** (Slice D): Cardo (SIL OFL) set as `ColonyTheme`'s default font; `GoldenAssert` helper extracted (the map goldens reuse it); `MenuGoldenTests` adds `main-menu`/`settings-screen`/`pause-menu` goldens. | a804549 |
| 2026-06-18 | **In-game pause menu** (Slice C): `PauseMenu` (Esc) over a paused tree; `SettingsScreen` refactored into a reusable overlay (emits `Closed`); +`PauseMenuTests` (L3). See [pause-menu.md](../systems/pause-menu.md) | a804549 |
| 2026-06-18 | **Settings screen + persistence** (Slice B): `SettingsService` autoload (`user://settings.cfg`) + `SettingsScreen` (video/audio, live-apply) over the engine-free `GameLogic.App.SettingsModel`; +`SettingsScreenTests` (L3). See [settings.md](../systems/settings.md) | a804549 |
| 2026-06-18 | **Main menu** (Slice A): `MainMenu` boot scene (FreeCol antique-map backdrop + parchment/wood panel) replaces booting into a running game; `run/main_scene` → `MainMenu.tscn`; reusable `ColonyArt.ParchmentSkin()` hoisted. +`MainMenuTests` (L3). See [main-menu.md](../systems/main-menu.md) | a804549 |
| 2026-06-16 | **Effective tile yields on the colony screen**: the tile badges + *Work…* picker now show each tile's yield as the colony actually banks it — base **+ the Sons-of-Liberty production bonus**, floored at 0 (`ColonyPanel.EffectiveYield`, mirrors `RunColonyTurn`) — so a high-membership colony's tiles read +1/+2 and a bad-government one reads less, matching production. Presentation-only (ADR-006; the production already applies the bonus). +1 L3 `TileBadges_ShowTheSonsOfLibertyBoostedYield`. | Phase 5 colony UI |
| 2026-06-16 | **Colony-screen carved-wood border**: a `UI/ColonyBorder` `NinePatchRect` (FreeCol's `carvedwoodenborder` pieces composited into a 194×194 nine-patch, `ColonyArt.ColonyBorder`) overlays the panel edge — `draw_center=false` so the parchment shows through, `mouse_filter=Ignore`, patch margins 23. `ColonyPanel._Ready` sets its texture and syncs its visibility to the panel (`VisibilityChanged`); the panel's content margin bumped to 26 so content clears the frame. Frames the colony window like FreeCol's. 19 scene tests green; goldens unaffected. | Phase 5 colony UI |
| 2026-06-16 | **Sons-of-Liberty bar** (Slice A presentation): `ColonyPanel.SonsOfLibertyBar` replaces the portrait placeholder with FreeCol's population panel — a **Rebels · Population · Royalists** row (named labels `RebelCount`/`RebelPercent`/`PopulationCount`/`ProductionBonus`/`RoyalistCount`/`RoyalistPercent`) with the production bonus (red when negative) over a two-segment `SolMeter` (gold rebel fill + dark royalist remainder). Reads `Colony.SonsOfLiberty`/`RebelCount`/`ToryCount`/`ProductionBonus` only (ADR-006). +1 L3 `ColonyPanelTests`. See [sons-of-liberty.md](../systems/sons-of-liberty.md) | Phase 5 colony UI |
| 2026-06-16 | **Colony-screen polish + resize pass** (live-playtest: "if i resize the game window everything seems wrong" + "super unpolished"; designed via a multi-agent research workflow + adversarial critique). New `ColonyTheme` (cached `Theme` assigned to `ColonyPanel` so it cascades): wood-toned `Button`/`OptionButton` + `PopupMenu`, a bordered-parchment `BuildingCell` `PanelContainer` variation, dark-ink `Label`s with a light halo, engraved `HSeparator`s, and `ColonyTitle`/`SectionHeader` size/colour hierarchy — it deliberately leaves the `panel` slot to the parchment override. **Resize fix:** the bands now live in a content card that sizes-to-content + `ShrinkCenter`s, with the `Scroll` set to `horizontal_scroll_mode=Auto` — so it centres with balanced margins on wide windows and scrolls (not clips) when narrow; fits cleanly from 1280 up. Building cells uniform with wrapping Title-Case names (`Display` camelCase-splitter, label-only — `Name`s still use `Short`); real section headers; readable tile badges (explicit light overrides). Presentation-only (ADR-006); 38 scene + 552 logic green; verified at 1100/1280/1600px. **Font deferred:** FreeCol's ShadowedBlack ships with no stated licence (don't adopt on a fabricated licence) — hierarchy is size+colour on the engine font for now. | Phase 5 colony UI |
| 2026-06-16 | **Click-to-move tile workers** (live-playtest fix): each surrounding tile gets a transparent whole-tile hit `Button` (`Tile_x_y`, added under the small ✕/picker so those keep their clicks) driving `OnTileClicked` + a `_heldFrom` field — click a worked tile to pick the colonist up (highlight + hint), click a free producible tile to move it (best-yield good via `AssignWork`/`UnassignWork`), or the colony centre to send it idle; an idle colonist drops straight onto a free tile. Presentation-only (ADR-006). +1 L3 `ColonyPanelTests`. | Phase 5 colony UI |
| 2026-06-16 | **Colony-screen FreeCol fidelity pass** (driven by a multi-agent research workflow over FreeCol's `ColonyPanel.java`): (1) the panel now wears FreeCol's **brown parchment skin** — `EnsureOpaqueBackground` builds a cached tiled `StyleBoxTexture` from `ColonyArt.PanelParchment()` (`ui/bg_paper_brown.png`), falling back to the warm solid fill when the asset is absent (CI-safe); (2) `Rebuild` re-laid-out into bands — a full-width production strip, a compact middle row of `[LeftColumn | BuildingsColumn]` (left fixed-width `Fill`, buildings `ExpandFill` + grid `ShrinkCenter` — one horizontal expander, mirroring FreeCol's `[fill][474!]`), then the units + warehouse bars beneath (both bars `ExpandFill` full-width); (3) `LeftColumn` gains a **colonist-portrait population strip** (`PopulationStrip`) — FreeCol's portrait row, no fabricated Sons-of-Liberty numbers (no per-colony liberty data exists; ADR-006 — see follow-up). All presentation (ADR-006); 37 scene + 552 logic green; goldens untouched (UI hidden in capture). The decorative wood border + SoL bar remain deferred. | Phase 5 colony UI |
| 2026-06-16 | **Colony-screen layout fixes** (live-playtest feedback at a ~1960px window): (1) the panel had **no opaque background** so the map drew through it — `ColonyPanel.Open` now sets an opaque `StyleBoxFlat` via `AddThemeStyleboxOverride("panel", …)` (`EnsureOpaqueBackground`); (2) the bottom bars (units + warehouse) **fell into a void** because the middle `HBox` had `SizeFlagsVertical=ExpandFill` — removed; the two columns now `ExpandFill` horizontally as equal halves with their content (`TilesView`, `BuildingsGrid`) `ShrinkCenter`-centred, and the buildings inner `ScrollContainer` dropped (the whole panel already scrolls). Verified at 1920×1040; 12 scene tests green. Presentation-only (ADR-006). | Phase 5 colony UI |
| 2026-06-16 | **FreeCol-look colony screen** (stages 2–5 of the rebuild): `ColonyPanel` re-laid-out after FreeCol's New Amsterdam — a top **production bar** (`NetProduction` goods icons), a left **isometric tile view** (`TilesView`, 160×80 diamonds, settlement centre, colonist + yield on worked tiles) over the **construction** panel, a right 4-wide **buildings grid** (`BuildingsGrid`) of `ColonyArt.BuildingImage` cells with `+`/`−` staffing, an outside-the-colony units row, and a bottom **warehouse** goods-icon bar. `ColonyArt` extended with `BuildingImage`/`GoodsIcon` (+ 42 building & 22 goods FreeCol sprites imported, PROVENANCE updated). The old 3×3 `GridContainer` tile test → an art/structure test (`ColonyScreen_DrawsTheSurroundingTilesAsArt_AndTheBuildingsAsAGrid`). All 37 scene + 552 logic tests green; `MapView`/goldens untouched (UI hidden in L4 capture). Reads + forwards only (ADR-006). **Deferred:** FreeCol's parchment UI *theme/skin* + the Rebels/Royalists SoL bar (no SoL data in the model yet). See [colonies](../systems/colonies.md) | Phase 5 colony UI |
| 2026-06-14 | FP-4: `GameController` owner-gates the rivals — `SyncColonyMarkers` fog-gates foreign colonies (hidden under the human's fog), and tile-click / HUD subject / camera focus resolve the human by owner (can't manage or centre on a foreign colony). FP-5 (AI economy) added no presentation change — rival economies are off-screen | FP-4 |
| 2026-06-15 | On-map combat UI: `HandleTileClick` routes a click on an adjacent enemy unit / native settlement to `Attack`/`AttackSettlement` (else move), HUD outcome notice, selection cleared after; L3 `ClickingAnEnemy_WithSelectedUnit_Attacks` | combat UI |
| 2026-06-15 | Native-raid feedback (slice 1b): `OnEndTurnPressed` reads `Game.CombatNotices` and `FormatCombatNotice` renders them into the status bar (from the human defender's view); L3 `NativeRaid_DuringEndTurn_ShowsANoticeInTheStatusBar`. Presentation-only (ADR-006) | Phase 5 slice 1b |
| 2026-06-15 | Rival/own-unit rendering (slice 1c-1): replaced the single `UnitMarker` node with a reconciled `MapView/UnitLayer` (`SyncUnitMarkers`) drawing every on-map unit the human can see — own units always, foreign powers + braves when in live sight (`IsVisible`); non-human units get an owner ring (`OwnerColorOf`: foreign `EuropeanNation.Color`, native constant), the human's none. +2 L3 + 1 L4 golden (`rendered-units-seed424242`); the 4 `MapView/UnitMarker` L3 tests migrated to `UnitLayer`. Presentation-only | Phase 5 slice 1c-1 |
| 2026-06-15 | Ship repair UI (slice 1c-3b): `EuropePanel` shows a damaged ship as "under repair (N turns)" and omits its Sail/Buy controls until whole (the logic guards in `SailToNewWorld`/`CheckBuyEuropeGoods`/`CheckBoard` are authoritative — the panel only reads `IsUnderRepair`). +1 L3 `ShipUnderRepair_ShowsNoSailButton`. Presentation reads only (ADR-006) | Phase 5 slice 1c-3b |
| 2026-06-15 | Native interaction UI (slice 1c-UI): new `NativeSettlementPanel` + `HandleTileClick` routes a discovered-settlement click (fog-gated) to it (was: immediate attack); offers speak/learn/attack gated by `CheckVisit`/`CheckLearnSkill`/`CheckAttackSettlement`; removed the now-unused `AttackSettlementAt`. +1 L3 `ClickingANativeSettlement_OpensInteractionPanel_AndSpeakWithChiefVisits`. Presentation reads only (ADR-006) | Phase 5 slice 1c-UI |
| 2026-06-15 | Piracy notice (slice 1c-3d-i): `FormatCombatNotice` renders the `Game.UnknownEnemyNationId` sentinel as an anonymous "privateer" (no nation named) when a foreign privateer raids the human. Presentation formatting only (ADR-006) | Phase 5 slice 1c-3d-i |
| 2026-06-15 | Colony-capture routing (slice 1c-3e): `HandleTileClick` routes a click on an ungarrisoned rival colony to `AttackColonyAt` (was: a CheckMove dead-end) → `CheckAttackColony`/`AttackColony`; reports "You captured X!" / "repelled". Presentation routing only (ADR-006) | Phase 5 slice 1c-3e |
| 2026-06-15 | Colony-loss notice (slice 1c-3f): `OnEndTurnPressed` now also renders `Game.ColonyLossNotices` via `FormatColonyLossNotice` ("⚑ The X captured your colony Y!"); `NationLabel` extracted and shared with `FormatCombatNotice` (no behaviour change). +1 L3 `ForeignPowerCapturesUndefendedColony_DuringEndTurn_ShowsALossNotice`. Presentation formatting only (ADR-006) | Phase 5 slice 1c-3f |
| 2026-06-15 | Colony-raid notice (native pillage): `OnEndTurnPressed` also renders `Game.ColonyRaidNotices` via `FormatColonyRaidNotice` ("⚔ The X raided Y and carried off N goods!"). Mirrors the L3-tested colony-loss notice path (same `OnEndTurnPressed` concat). Presentation formatting only (ADR-006) | Phase 5 native pillage |
| 2026-06-16 | Defeat banner (human defeat, `86d3bx04e`): `OnEndTurnPressed` appends a "💀 You have been defeated…" status message when `Game.IsHumanDefeated` (no colonies + no units). Message-only first cut: the End Turn button isn't disabled and `Game.EndTurn` does **not** short-circuit (that would break ADR-009 stream-0 byte-stability), so the game keeps advancing and the banner re-displays on repeated presses — a full game-over flow (disable the button + a game-over screen) is a deferred presentation follow-up. +1 L3 `WhenTheHumanIsWipedOut_EndTurn_ShowsDefeat`. Reads-only (ADR-006) | Phase 5 (human defeat) |
| 2026-06-16 | Game-over flow (`86d3c0x3f`) — the deferred follow-up above, now done: `RefreshView` calls a new `UpdateDefeatUi`, which on `Game.IsHumanDefeated` shows a full-rect `UI/GameOverScreen` overlay (a dim `ColorRect` + a centred "💀 Defeated" panel with a runtime "lost on turn N" message + a **New Game** button wired to `NewGame`) and **disables + relabels** End Turn to "Game Over"; starting a fresh game clears the defeat and hides the overlay. Still presentation-only — `Game.EndTurn`/logic/save unchanged (ADR-006; ADR-009 stream-0 untouched). The overlay's `Control` swallows map clicks while shown; goldens are unaffected (the L4 capture hides the `UI` layer). +1 L3 `WhenTheHumanIsWipedOut_TheGameOverScreenShows_AndEndTurnIsDisabled`. | Phase 5 (game-over flow) |
| 2026-06-16 | Native tribute-demand modal (`86d3bkc3w`): new `NativeDemandPanel` (a centred `UI/NativeDemandPanel` PanelContainer) shows what a brave demanded of a colony and offers **Pay tribute** (`Game.AcceptPendingDemand`) / **Refuse** (`Game.RefusePendingDemand`); `OnEndTurnPressed` opens it for `Game.PendingDemand` after draining the notice channels (gated on `!IsHumanDefeated`), else hides it, and `UpdateDefeatUi` hides it on defeat. Presentation only (ADR-006) — the panel renders ids/amounts and forwards the choice; no logic/save change. +1 L3 `NativeTributeDemand_DuringEndTurn_OpensThePanel_AndPayingTransfers`. See [natives](../systems/natives.md) | Phase 5 native tribute |
| 2026-06-16 | Native-pillage gold notice: `FormatColonyRaidNotice` now renders a `ColonyRaidNotice` with a **null `GoodsId`** as "⚔ The X raided Y and made off with N gold!" (the gold-steal sibling of the goods raid). Presentation formatting only (ADR-006). See [combat](../systems/combat.md) | Phase 5 native pillage |
| 2026-06-16 | Colony tile-work picker: `ColonyPanel`'s Fields section now lists each free surrounding tile with an `OptionButton` of its producible goods (`Game.TileWorkOptions` → `AssignWork`), so an idle colonist can be put on a **non-food** tile good (lumber/ore/cotton/…), not just the food auto-assign. Reads + forwards only (ADR-006). +1 L3 `ColonyPanelTests.TileWorkPicker_AssignsAColonist_ToANonFoodTileGood`. See [colonies](../systems/colonies.md) | Phase 5 colony UI |
| 2026-06-16 | Colony join/leave UI: `ColonyPanel`'s new *Colonists* section adds a **Join colony** button per eligible on/adjacent human unit (`JoinColony`) and a **Send a colonist out** button (`LeaveColony`). `GameController.RefreshView` now clears a stale `_selectedUnit` (a joined unit leaves `Game.Units`). Reads + forwards only (ADR-006). +2 L3 `ColonyPanelTests` (Join / Leave). See [colonies](../systems/colonies.md) | Phase 5 colony UI |
| 2026-06-16 | Colony arm UI: the *Colonists* section's per-unit row (now unified over on/adjacent units) also offers **Arm soldier/dragoon/scout/pioneer** + **Disarm** for a unit standing in the colony (`CheckEquipRole`/`EquipRole`); the shared `Act(unitId, …)` helper re-resolves the live unit before each action. Reads + forwards only (ADR-006). +1 L3 `ColonyPanelTests.ArmButton_EquipsAColonistAsASoldier`. See [colonies](../systems/colonies.md) | Phase 5 colony UI |
| 2026-06-16 | Colony screen art (stage 1 of the FreeCol-look rebuild): the 3×3 tiles grid now draws **real FreeCol art** — each `TileCell`'s `TileArt` stacks the terrain diamond (base + forest/elevation overlay) with the settlement sprite at the centre or a colonist sprite on a worked tile, via the new `ColonyArt` loader (`res://assets/freecol/{terrain,settlements,units}`). Controls + structure preserved (all 37 scene tests green); `MapView` untouched (goldens safe). Reuses already-registered assets. | Phase 5 colony UI |
| 2026-06-16 | Colony-screen crash fix: `ColonyPanel.Changed` now **defers** the rebuild (`Callable.From(ApplyChange).CallDeferred()`) — rebuilding frees every child, and freeing an `OptionButton` inside its own `ItemSelected` callback (popup still closing) hard-crashed Godot (use-after-free). Surfaced on assigning a colonist to a tile via the *Work…* picker; the EmitSignal-based L3 tests didn't reproduce it (no real popup). No behaviour change. | Phase 5 colony UI |
| 2026-06-16 | Full colony screen: `ColonyPanel` rebuilt from a vertical text list into a laid-out screen — summary line + a **3×3 surrounding-tiles `GridContainer`** (`TileCell` helper) + buildings / warehouse / colonists / construction, in a new `ScrollContainer` (scene: the `Dynamic` `VBox` moved under `VBox/Scroll`; panel enlarged to 760×600). Every named control + the `ColonyTitle`/`ColonyInfo`/`CloseButton` structure preserved, so all existing L3 tests pass. Reads + forwards only (ADR-006). +1 L3 `ColonyPanelTests` (grid renders, colony at centre). See [colonies](../systems/colonies.md) | Phase 5 colony UI |
| 2026-06-14 | `GameController` only selects/renders the **player's** units (native braves are skipped as the HUD unit and on click); camera centres on a player unit → colony → map centre (braves now share the unit list) | Phase 5 slice 5b |
| 2026-06-13 | Walking-skeleton scene: map view, unit, camera, turn UI, quicksave; GdUnit4 L3 wiring | Phase 1 skeleton |
| 2026-06-13 | Isometric rendering with FreeCol terrain/unit/settlement art (ADR-014); L4 visual-golden harness | Phase 2 |
| 2026-06-13 | Interactive colony screen (`ColonyPanel`); L3 input + panel tests | Phase 3 |
| 2026-06-13 | Europe screen (`EuropePanel`): dock/recruit, ships, board/sail; map renders only on-map units; L3-tested | Phase 4 slice 6 |
| 2026-06-13 | Europe screen: goods Sell/Buy (slice 10) and Buy/train units (slice 11); L3-tested | Phase 4 slices 10–11 |
| 2026-06-13 | Native settlements on the map (`NativeSettlementMarker`, fog-gated; FreeCol indian art, ADR-014); `native-settlement` L4 golden | Phase 5 slice 1 |
| 2026-06-13 | `GameController` selects a game variant — new game loads its ruleset, saves record/restore it (ADR-018) | Phase 5 (variant layer) |
| 2026-06-13 | `MapView` dims explored-but-unseen tiles (explored-vs-visible fog); `remembered-fog` L4 golden | Phase 5 (fog upgrade) |
