# System: Independence (the War of Independence)

| | |
|---|---|
| **Status** | Implemented (GameLogic) — declaration + continental muster + **native re-stancing** + **on-declaration Hessian mercenary offer**, REF arrival + War-of-Independence combat, the **foreign Intervention Force** (hold out long enough and a friendly power sends troops), victory (**defeat the REF / defeat all Europeans / defeat all humans**, ruleset-gated) + Spanish Succession, and defeat (lose your last port). The war UI is P7. |
| **Last verified** | 2026-06-21 @ specialised REF combat AI (`86d3drn5a` — colony-first targeting, no unit-chase before a colony falls, navy hunts rebel ships); IndependenceTests 39/39 green (targeted Independence/Ref/Combat 294 green) |
| **Code** | `game/src/GameLogic/GameSession/Game.Independence.cs`, `Force.cs`; `Player.PlayerType`/`DeclaredIndependenceTurn`/`InterventionBells`; `Ruleset.LastColonialYear`/`InterventionBells`/`InterventionTurns`/`InterventionForce` (`Specification/Ruleset.cs`) |
| **Tests** | `game/tests/GameLogic.Tests/GameSession/IndependenceTests.cs` |
| **FreeCol reference** | `InGameController.declareIndependence` (native re-stancing + `loadMercenaryForce`/`csMercenaries`), `REFAIPlayer` (REF combat doctrine: `findColonyTargets`/`adjustMission`/`REFNavyGoalDecider`), `ServerGame.checkForWinner` + `csSpanishSuccession`, `Player.getSoL`, `model.event.declareIndependence` |
| **Related systems** | [monarchy](monarchy.md), [royal-expeditionary-force](royal-expeditionary-force.md), [sons-of-liberty](sons-of-liberty.md), [combat](combat.md) |

## 1. How it works (plain English)

Once your colonies are fired up enough — at least **half your colonists are rebels** (Sons of Liberty ≥ 50% nationwide) and you hold a **port** — you can **declare independence**, as long as it isn't already too late in history. There's a **last year** after which the mother country will no longer let a colony break away — in the classic game that's **1800**. That cutoff year isn't a number baked into the code: it's a **ruleset setting** read from the game's data, so a different scenario (or the Australia variant) can move it without touching any code. When you do declare:

- your nation becomes a **rebel** fighting for freedom;
- everything you had **waiting in Europe is lost**, and Europe closes its doors (no more recruiting or trade with the mother country);
- in each fired-up colony (over half rebels), some of the **veteran soldiers stationed there rise up as Continental regulars** — the bigger and more fervent the colony, the more of its own veterans muster;
- the **natives who most resented the old Crown side with you** — the native nation you'd angered the most (among those you've met) cools off and throws in with your rebellion;
- the **King makes one parting offer**: a force of professional **Hessian mercenaries** you can hire for gold to help face his army (you accept or decline — nothing is forced on you, and you're never charged unless you say yes);
- and the **King sends his army** — the Royal Expeditionary Force he's been building all game — to crush you. From now on it's war.

From the next turn the **King's army sails in and lands near your ports**, then fights with a clear, single-minded plan rather than wandering about. Its **soldiers go straight for your colonies** — your **coastal ports first** (to cut you off from any foreign ally), and the weakest-defended of those before the well-fortified ones. They deliberately **ignore your loose troops in the field until they've actually captured one of your colonies** — the King wants your towns, not a scattered chase — and only once a colony has fallen do his regulars also start hunting down your stray units. His **men-o-war hunt your ships** and, when there's no rebel vessel nearby, sail in to support the landings. Winning means defeating or driving off that army; losing means being driven from your last port.

**There's more than one way to win.** The game's rules can switch on alternative victory conditions: by default you win the moment you **defeat the King's army and secure independence**, *and* you also win if you become **the last European power left standing** (everyone else's colonies and armies wiped out). A third optional condition — being the last **human** player — exists for multiplayer and is **off** in the classic single-player game. Which of these count is a rules setting read from the game's data, so a variant can turn them on or off without touching code.

**A foreign ally comes to your aid.** While the war drags on, your rebellion keeps producing liberty (bells). A sympathetic foreign power is watching, and the longer and harder you fight, the closer it comes to committing: every bell you generate during the war counts toward a hidden tally. Once that tally crosses a threshold (the classic game's medium setting is **5000**), the foreign power lands a small **Intervention Force** — a couple of colonial regulars, some dragoons, artillery, and a brace of men-o-war — right beside one of your ports, fighting on your side. The tally then resets, so a truly drawn-out war can draw a second wave. It's the game's way of rewarding a stubborn rebellion: hold out long enough and you won't face the King alone.

**What the player sees and does:** nothing yet — the Declaration screen and the war UI are P7. The GameLogic decision is exposed for that UI to call.

## 2. Detailed rules

- **The gate** (`CheckDeclareIndependence`, FreeCol `model.event.declareIndependence` limits): still a plain **colonial** power, **national Sons of Liberty ≥ 50%** (rebels across all colonies ÷ total population), **≥ 1 connected-port colony**, and the **year ≤ the last colonial year** (`Ruleset.LastColonialYear`, the spec `model.option.lastColonialYear` integer game option in the `gameOptions.years` group; classic **1800**). The cutoff is read from the ruleset, not hardcoded — a variant spec moves it by data alone (a missing option falls back to 1800).
- **Declaring** (`DeclareIndependence`):
  - the player becomes a **`Rebel`** and records the turn (drives the score and the intervention force later);
  - every unit **in or bound for Europe is removed**; the **recruit dock is cleared**;
  - **continental-army muster** (`MusterContinentalArmy`): **per colony**, for each colony with SoL > 50 the cap is `(unitCount + 2) · (SoL − 50) / 100` — `unitCount` being *that colony's own* units (its population of workers + the units garrisoning its tile, FreeCol `Settlement.getAllUnitsList`) — and up to that many of *that colony's own* garrisoned **veteran soldiers** rise to **colonial regulars** (earliest first by id; colonies processed in id order). A veteran in the open field, or in a colony that hasn't crossed 50% SoL, is not mustered;
  - the **Royal Expeditionary Force** enters play as a new `RoyalExpeditionaryForce` AI player **at war** with the rebel, its amassed [force](royal-expeditionary-force.md) realised into real units mustering in Europe;
  - **native re-stancing** (`ShiftNativeStanceOnDeclaration`, FreeCol `csDeclareIndependence` native block): among the native nations the rebel has **contacted** (spoken with a chief, `NativeSettlement.HasBeenVisitedBy(rebel)`), the **most-hostile** one (highest peak `Alarm`, id-tie-broken) **swings behind the rebel** — every one of its settlements is calmed to at most FreeCol's CONTENT band (`NativeAllyCalmedAlarm` = 600). RNG-free. *Faithful-subset:* FreeCol also turns that ally **hateful toward the REF** and makes the *least*-hostile contacted nation turn on the rebel; our single-player native model tracks only one alarm figure (toward the human) with **no native↔REF relationship and no second human**, so only the representable half — the angriest contacted nation backing the rebel — is implemented (the other two halves have no analogue);
  - **war-time mercenary offer** (`OfferWarMercenaries`, FreeCol `loadMercenaryForce` + `csMercenaries(HESSIAN_MERCENARIES)`): the King makes a one-off **Hessian** force offer, surfaced as a `PendingMonarchDemand(HessianMercenaries, Offer, Price)` — the *same* accept/decline seam as the in-game monarch mercenary offers — built from the existing affordability-trimmed `LoadMercenaries` generator on the **monarch RNG** (ephemeral, off the human's live state + turn, never stream 0). It is **applied only on accept** via `RespondToMonarch` (units spawn on the rebel's Europe dock, gold deducted); decline charges nothing. No offer is made when none is affordable. Surfaced for the **human** rebel only (an AI rebel auto-fights). *Faithful-subset:* FreeCol draws from the Monarch's fixed pre-built `mercenaryForce`; we reuse our veteran-soldier mercenary generator (the same documented simplification the in-game offer makes);
  - a **`DeclaredIndependence` history event** is recorded (in-memory history log, not persisted — like every `RecordHistory` call).
- **The war** (`RunRefTurn`, item 8 + the **specialised REF combat doctrine** `86d3drn5a`): each REF turn, units still in Europe **land** (`LandRefUnits`) — the King's fleet makes landfall at its **fixed entry tile** (a water tile near the human's start, chosen deterministically at game creation and persisted, FreeCol `Player.entryTile`/`86d3c9w5n`): each unit takes the nearest empty tile to that beachhead (`FindLandingTileNear`, land ashore / ships on water), falling back to the ring around the rebel's connected-port colonies when the beachhead is full or unset (a pre-v47 save). Then the REF **prosecutes the war with a bespoke doctrine mirroring FreeCol `REFAIPlayer`** (no longer the generic foreign-power war AI), each unit on the REF's own stream:
  - **the rebel is found explicitly** (`RefRebel` = the `Rebel`/`Independent` player the REF is at war with), not assumed to be "the human" — they coincide in single-player, but the doctrine targets the rebel directly;
  - a **REF land unit** (`RunRefLandUnit`, FreeCol `findColonyTargets`/`adjustMission`): captures an **adjacent undefended rebel colony** outright; else marches on the **best-scored rebel colony in seek range** (`PickRefColonyTarget`) — the shared `ScoreColonyTarget` heuristic (value − distance, attacker strength, loot, −200·stockade) **plus +500 for a connected port** (`RefConnectedPortBonus`, FreeCol's connected-settlement bonus), so coastal/port colonies and the weakest-defended ones come first — fighting a garrison as a field unit when adjacent; **it does not chase loose rebel field units until the REF has captured a colony** (`holdsAColony` gate, FreeCol "Do not chase units until at least one colony is captured" / "The REF is more interested in colonies"), after which `PickRefUnitTarget` lets it seek-and-destroy field units too; with nothing scored in range it closes on the **nearest rebel colony at any distance** (`NearestRebelColony`) so the army besieges rather than idling;
  - a **REF man-o-war** (`RunRefNavalUnit`, FreeCol `REFNavyGoalDecider` + the navy block in `initialize`): seek-and-destroys the **rebel's warships** (`PickRefUnitTarget`, same-domain — ships hunt ships), engaging when adjacent, and — with no rebel ship in reach — escorts toward the invasion (`NearestRebelColony`), supporting the landings;
  - a REF unit standing in a **just-captured colony presses on** to the next objective (it has no defend-and-dig-in branch — FreeCol's "garrison thinly").

  Combat resolves through the REF-stream `AttackRebelUnit`/`CaptureRebelColony` (siblings of the generic `AttackHumanUnit`/`CapturePlayerColony`), recording the same `CombatNotice`/`ColonyLossNotice` for the presentation. The REF draws entirely from **its own RNG stream**; the rebel attacks the REF through the normal stream-0 combat. The REF↔rebel war stance never cools (the colonial-tension decay touches only `Colonial` players).
- **Foreign Intervention Force** (`AccrueInterventionBells`/`SpawnInterventionForce`, FreeCol `ServerPlayer.csNewTurn` rebel branch + `Monarch.getInterventionForce`): each turn during the war the rebel banks the **net liberty it produced that turn** into `Player.InterventionBells` (the same figure FreeCol adds in `Player.modifyLiberty` while `isRebel()`). When the running total reaches the ruleset's `interventionBells` threshold (`Ruleset.InterventionBells`, classic medium **5000**), a friendly foreign power sends its `Ruleset.InterventionForce` (classic medium **2 colonial-regular soldiers + 2 colonial-regular dragoons + 2 artillery + 2 men-o-war**): the **men-o-war make landfall on the water off a randomly-chosen connected port** of the rebel, **carrying the land units aboard as passengers** (FreeCol `loadShips` — the ally fleet arrives offshore with the troops embarked, and the player disembarks them where they choose), all **owned by, and fighting for, the rebel** (FreeCol creates the force on the rebel itself). Because it only needs sea room for the ships — not an open ring of beach — the ally can still reach a **besieged** port, which is the whole point. The counter then **resets to 0**, so a long war can draw a second wave (FreeCol grows the standing force every `interventionTurns` = classic 52). The port and the exact sea tiles are the only random choices, drawn from a **dedicated reserved RNG stream** (`InterventionStreamId = 103`), never the human's stream 0 (ADR-009) — so even a human rebel's economy stream is untouched. If the rebel holds no connected port, or no sea room for even one man-o-war, nothing lands.
- **Winning** (`ResolveWarOfIndependence`, item 9): each turn, the rebel wins (FreeCol `checkForREFDefeat`) when the REF **holds no colonies**, the rebel's **land power ≥ 1.5×** the REF's, and the REF is reduced **below 7 land or 2 naval** units (counting its whole force, so the win can't fire before the army even lands). On winning (`GiveIndependence`): peace with the King, the rebel becomes **`Independent`** with **no tax**, the **surviving redcoats on the map surrender** to the new nation (the fleet sails home).
- **Victory conditions** (`Game.Winner`, FreeCol `ServerGame.checkForWinner`): the `Winner` read is now a **pure, ruleset-gated** evaluation of the three FreeCol victory toggles, checked in FreeCol's order:
  1. **Defeat the REF** (`Ruleset.VictoryDefeatRef`, classic **on**) — the first `Independent` nation wins (the headline War-of-Independence win, unchanged behaviour);
  2. **Defeat all Europeans** (`Ruleset.VictoryDefeatEuropeans`, classic **on**) — when exactly **one** live European power (a `Colonial`/`Rebel`/`Independent` player still holding a colony or a non-native unit; the REF and natives excluded) remains, it wins;
  3. **Defeat all humans** (`Ruleset.VictoryDefeatHumans`, classic **off**) — when exactly one live **non-AI** European power remains, it wins.
  Each toggle is parsed from the spec `gameOptions.victoryConditions` group (`model.option.victoryDefeat{REF,Europeans,Humans}`, a `<booleanOption>` read by `Ruleset.ParseBooleanOption`); a spec without them falls back to the classic defaults (true/true/false). All three are **pure reads** (like the old `Winner` and `IsRebelDefeated`); `EndTurn` never short-circuits on them (ADR-009 byte-stability — the presentation reads `Winner` for the victory screen). The default game's outcome is unchanged: with the human + 3 living foreign powers no Europeans/Humans condition fires, and `victoryDefeatHumans` is off.
- **Spanish Succession** (`RunSpanishSuccession`, a separate event): once, from **1600**, a fading European AI (SoL < 50) is **absorbed** by the dominant one (SoL > 50) — its colonies and units change hands. (Not the win condition; a late-game consolidation.)
- **Losing** (`IsRebelDefeated`, item 10): once you've declared, holding **no connected port** (`GetNumberOfPorts == 0` — the REF has taken them all) means you've **lost** the War of Independence. This is a flag the presentation reads for the defeat screen; the turn loop keeps running regardless (a defeated human must not freeze stream 0 — ADR-009). A plain colony with no port is never "rebel-defeated".

**Deviations from original / FreeCol:** the gate (SoL ≥ 50, ≥ 1 connected port, year cutoff), the **per-colony continental muster** (cap + veteran→colonial-regular draw), the win thresholds (1.5× / 7 land / 2 naval) and the REF realisation match FreeCol. **Faithful-subset simplifications (documented, not yet faithful):** (1) the **continental muster** now caps and draws **per colony** (FreeCol `csDeclareIndependence`): each colony with SoL > 50 gets cap `(unitCount + 2) · (SoL − 50) / 100` over *its own* units and upgrades up to that many of *its own* veterans. The cap term faithfully counts the colony's population (workers) **plus** the units garrisoning its tile (FreeCol `getAllUnitsList` = worker units + tile units). The one residual deviation: because our colony workers are a `Population` **count**, not `Unit` objects, the veterans actually drawn can only be taken from the **tile garrison**, not from worker colonists — a veteran serving *as a colony worker* isn't mustered until taken out as a unit (FreeCol can upgrade a worker veteran in place). In practice veterans garrison the colony tile rather than working a job, so this rarely bites. (2) the **Spanish Succession** ranks the absorbed/absorbing pair by **Sons-of-Liberty** (we have no overall game-score yet) rather than FreeCol's score, and only an AI (not the human) can satisfy its strong-limit trigger. (3) the REF now runs a **bespoke combat doctrine** (`RunRefTurn`/`RunRefLandUnit`/`RunRefNavalUnit`, `86d3drn5a`) mirroring FreeCol `REFAIPlayer`'s priorities — **colony-first targeting with connected ports favoured** (+500), **no field-unit chase until a colony is captured**, and **men-o-war hunting the rebel's ships / supporting the landings** — rather than the generic foreign-power war AI. The modelled subset is the *target selection and the colony-first gate*, not FreeCol's full mission/transport scheduler: there is **no dedicated amphibious transport doctrine** (REF land units march overland from the beachhead rather than being re-embarked and ferried colony-to-colony), the score's drydock/military-supplier multipliers and the ±20% random twiddle from `findColonyTargets` are omitted (we keep the SoL + stockade terms), and the REF combat ambush-penalty is still deferred. (4) the **Intervention Force** lands a single fixed force at the threshold; FreeCol additionally *grows* the standing force every `interventionTurns` (we parse the option for fidelity but don't yet grow). The fleet arrives FreeCol-faithfully — the men-o-war on the water carrying the land units aboard (FreeCol `loadShips`), troops over the holds disposed — so a besieged port still gets its ally. The bell accrual is recomputed from the rebel's liberty gain via a transient per-turn snapshot rather than hooked into `modifyLiberty`; a turn that straddles a save/load, or a turn the rebel elects a Founding Father (liberty spent), can drop that single turn's contribution — negligible against the 5000 threshold. (5) **Native re-stancing** on declaration implements only the representable half of FreeCol's rule — the angriest *contacted* native nation is calmed to back the rebel — since our single-player native model has no native↔REF relationship and no second human to turn hostile. (6) The **on-declaration Hessian offer** reuses our affordability-trimmed mercenary generator rather than FreeCol's fixed pre-built `mercenaryForce`, and is surfaced to the human rebel only (an AI rebel auto-fights). (7) The **alternative victory conditions** (defeat all Europeans / humans) are faithful pure reads; "live European power" is approximated as *holds a colony or a non-native unit* (FreeCol's full `isAlive`/death model is richer, but this matches in practice).

## 3. Technical design

- `PlayerType` gains `Rebel`, `Independent`, `RoyalExpeditionaryForce` (the lifecycle Colonial → Rebel → Independent; the REF is its own type). `RunPlayerTurn` routes Rebel/Independent through the colonial economy path and the REF through `RunRefTurn` (`Game.Independence.cs`), which lands the in-Europe units (`LandRefUnits`) then drives the **specialised REF combat doctrine** (`RunRefLandUnit`/`RunRefNavalUnit` + the targeting helpers `PickRefColonyTarget`/`PickRefUnitTarget`/`AdjacentCapturableRebelColony`/`NearestRebelColony` and the REF-stream combat `AttackRebelUnit`/`CaptureRebelColony`) — see item 8 above and FreeCol `REFAIPlayer`. The colony-score reuses the shared `ScoreColonyTarget`/`ScoreUnitTarget` (`Game.cs`) plus the `RefConnectedPortBonus` (+500); no new state, no save change.
- `Game.NationalSonsOfLiberty(player)` = `sum(colony.RebelCount) · 100 / max(1, sum(Population))`.
- `CheckDeclareIndependence`/`DeclareIndependence` (ADR-006). The year branch reads `Ruleset.LastColonialYear` (no longer a `LastColonialYear` const). `MusterContinentalArmy` iterates the rebel's colonies with SoL > 50 (in id order); per colony it computes `unitCount = colony.Population + |on-map non-native units owned by the rebel standing on the colony tile|` (FreeCol `getAllUnitsList().size()`), `limit = (unitCount + 2) · (SoL − 50) / 100`, then upgrades up to `limit` of the rebel's veteran soldiers garrisoning that tile (ordered by id) via the existing `UpgradeUnitType` swap (units are immutable in their type). The per-colony loop and tile-scoped draw replace the earlier nationwide `Sum`/whole-map `Take`. `CreateRefPlayer` adds a non-human `RoyalExpeditionaryForce` player with its **own RNG stream** (seeded off the human's current state, read non-destructively — stream 0 untouched), sets the WAR stance both ways, and realises `_refForce` into units via `SpawnInEurope`.
- **Native re-stancing + Hessian offer** (`Game.Independence.cs`): `DeclareIndependence` now also calls `ShiftNativeStanceOnDeclaration` and `OfferWarMercenaries`, then `RecordHistory(DeclaredIndependence, …)`. `ShiftNativeStanceOnDeclaration` groups the rebel-contacted settlements by `NationTypeId`, picks the group with the highest peak `Alarm` (id-tie-broken), and `ChangeNativeAlarm`s each of that nation's settlements down to `NativeAllyCalmedAlarm` (= `NativeSettlement.AlarmContentMax` = 600) when above it — RNG-free, no save change (the per-settlement `Alarm` already persists; a nation-scope tension is not modelled). `OfferWarMercenaries` (human rebel only) builds a `PendingMonarchDemand(HessianMercenaries, Offer, Price)` from `LoadMercenaries(rng)` on the ephemeral monarch stream (`MonarchStreamId`, seeded off the human's live state + `Turn`); the existing `RespondToMonarch` mercenary branch applies/declines it. `HistoryEventKind` gains `DeclaredIndependence` (in-memory history; not persisted).
- **Victory** (`Game.Winner` in `Game.Independence.cs`): generalised from "first `Independent`" to FreeCol's three-condition `checkForWinner`, gated on `Ruleset.VictoryDefeatRef`/`VictoryDefeatEuropeans`/`VictoryDefeatHumans`. `LiveEuropeanPowers()` is the private helper — `Colonial`/`Rebel`/`Independent` players that still hold a colony or a non-native unit (REF + natives excluded). `Ruleset` gains the three `bool` properties, parsed in `Ruleset.Load` via `ParseBooleanOption(root, "model.option.victoryDefeat{REF,Europeans,Humans}", fallback: true/true/false)` (the `gameOptions.victoryConditions` group; classic uses `defaultValue`). Not persisted (re-derived from the ruleset on load, like the other game options) → **no save-version bump**; the default classic values leave `Winner`'s default-game result unchanged (ADR-009).
- **`Ruleset.LastColonialYear`** is parsed in `Ruleset.Load` by `ParseIntOption(root, "model.option.lastColonialYear", fallback: 1800)` — a top-level `<integerOption>` reader (mirrors `ParseBooleanOption`; reads `value` then `defaultValue`). It's a **game option, not a difficulty option** (it doesn't vary by level, so it lives on the ruleset root beside the `gameOptions.years` calendar, not in `DifficultyOptions`). Not persisted in the save (re-derived from the ruleset on load, like the calendar) → **no save-version bump**; the default classic spec value is 1800, so the default game's gate is byte-identical to the old const.
- **Intervention options** (`Ruleset.InterventionBells`/`InterventionTurns`/`InterventionForce`): parsed by `Ruleset.ParseIntervention(root, difficultyLevelId)`, which — like `ParseDifficulty` — first selects the chosen difficulty level's `optionGroup` subtree (the options are restated under every level, so reading over the whole doc would pick `veryEasy`), then reads `model.option.interventionBells`/`interventionTurns` (`<integerOption>`) and the `model.option.interventionForce` `<unitListOption>` of `<unitOption>` (`unitType`/`role`/`number`) blocks. A missing level — or any missing option — falls back to the classic-medium values (`InterventionForceComposition.ClassicMedium` = 5000 / 52 / the 8-unit ally force), so the default game and a spec without the options are unchanged. `InterventionForceComposition`/`InterventionForceUnit` are records declared alongside `Ruleset`; the land/naval split is decided at spawn time from each unit type's `IsNaval` flag (ruleset-agnostic).
- **Intervention accrual + spawn** (`Game.Independence.cs`): `ResolveWarOfIndependence` (per turn, in `EndTurn`'s world-advance band) calls `AccrueInterventionBells(rebel)` for each rebel. It banks `max(0, rebel.Liberty − snapshot)` into `rebel.InterventionBells` (a transient `_interventionLibertySnapshot` dict seeds from the rebel's current liberty on first sight — so nothing pre-rebellion is counted, and a save/load straddle costs at most one turn's bells), then if `InterventionBells ≥ Ruleset.InterventionBells` it resets the counter to 0 and calls `SpawnInterventionForce(rebel)`. The spawn picks a random connected port, drops the **men-o-war** on the nearest free sea tiles around it (`FindLandingTileNear(naval: true)`), then **boards the land units onto that fleet as passengers** (round-robin by free `CargoSlotsFree`, `CarrierId` set to the carrying ship; land units that exceed the holds are left behind, FreeCol-style) — all owned by the rebel. With no ship able to make landfall, nothing lands. The RNG is a fresh `Pcg32Random(seed, InterventionStreamId)` seeded off the REF's (or rebel's) persisted stream state + `Turn` — deterministic, reproducible across save/load, and never stream 0. `ResolveWarOfIndependence` is `internal` so a test can land the ally in isolation (the REF's own units are still mustering in Europe at that point, so the fleet arrives unmolested — a full `EndTurn` would land the REF and sink the just-arrived fleet the same turn).
- **Determinism (ADR-009):** the REF runs on its own stream (like a foreign power); the human/rebel on stream 0; the monarch tick stops once the player is no longer `Colonial`. The **Intervention Force** lands on the reserved `InterventionStreamId = 103` stream (high id, like LCR/resource-quantity), so a human rebel's economy stream 0 is untouched — the soak (which never reaches the war) stays byte-identical.
- **Victory options** (`Ruleset.VictoryDefeatRef`/`VictoryDefeatEuropeans`/`VictoryDefeatHumans`): parsed in `Ruleset.Load` by `ParseBooleanOption(root, "model.option.victoryDefeat{REF,Europeans,Humans}", fallback)` — the same top-level `<booleanOption>` reader used for `enableUpkeep` (reads `value` then `defaultValue`; classic ships these with `defaultValue`). They live in the `gameOptions.victoryConditions` game-option group (not a difficulty option). Fallbacks are the classic defaults (true/true/false). Not persisted (re-derived from the ruleset on load) → **no save-version bump**; the default game's `Winner` result is unchanged.
- **Save v41 (unchanged — no bump):** `SavedPlayer.DeclaredIndependenceTurn` + `InterventionBells` (both omitted before independence); the `PlayerType` ordinals (Rebel/Independent/REF) and the REF **player row** persist for free, and the REF's + the landed intervention force's units via `SavedUnit.OwnerId`; a pre-independence game is byte-identical to v40. The Intervention Force **reuses the existing `Player.InterventionBells`** field (no new field, no version bump); the per-turn liberty snapshot is transient (reseeded on load). The native re-stancing, Hessian offer, victory options, and the `DeclaredIndependence` history event add **no new persisted field** (native alarm already persists; the pending offer rides the existing transient `PendingMonarchDemand`; the victory options are ruleset-derived; history is in-memory) → **still v41, no bump**.

## 4. Verification

| Layer | Required? | Tests | Status |
|---|---|---|---|
| L1 Unit | Always | `IndependenceTests`: `NationalSonsOfLiberty`; the declare gate (SoL); the **last-colonial-year gate** (classic ruleset parses 1800; `ParseIntOption` reads a non-default value + falls back; the gate reads the **ruleset** year not a hardcoded 1800; allows on the cutoff year, blocks the year after); declaring turns Rebel + loses Europe units + starts the REF at war + realises ≥ 60 REF units; **per-colony continental muster** — a colony's tile garrison upgrades up to its own `(unitCount + 2)·(SoL−50)/100` cap with a remainder left un-mustered, the draw comes from each colony's own garrison not the whole map (a field veteran stays a veteran), and the cap counts only that colony's own units; **intervention options** (classic parses 5000 / 52 / the 8-unit force; `ParseIntervention` reads the chosen level + falls back); **native re-stancing** (the most-hostile contacted nation is calmed to CONTENT, an uncontacted/never-met nation is untouched, RNG-free byte-stable on stream 0); **Hessian offer** (an affordable declaration surfaces a pending `HessianMercenaries` offer; accept spawns the force in Europe + charges gold; decline spawns nothing + charges nothing; broke → no offer); **victory options** (classic parses REF on / Europeans on / Humans off; `ParseBooleanOption` reads `defaultValue` + falls back; defeat-all-Europeans wins with one survivor + no win while two survive + no win when the option is off; defeat-all-humans wins when enabled); pre-independence save omits the tokens | ✅ |
| L2 Scenario | Always | `IndependenceTests`: EndTurn runs cleanly after declaration (rebel colonial path + REF turn); the full rebellion (Rebel + REF + war stance + REF units) round-trips save/load; the REF **sails in and lands** near the rebel + the **war stance never cools** + the whole war is **deterministic across twin games** + **draws nothing from stream 0**; **specialised REF combat doctrine** (`86d3drn5a`) — a REF land unit **does not attack an adjacent rebel field unit before a colony is captured** (the field soldier is left unharmed, no combat notice), a REF **man-o-war beside a rebel ship engages it** (a combat notice fires), and a REF land unit with no field unit in range **marches on the rebel colony** (closes the Chebyshev distance); **Intervention Force** — reaching the threshold lands the full force off a port (2 men-o-war on the water carrying 6 land units aboard) + resets the counter, below the threshold nothing lands, bells accrue the rebel's liberty each turn, the landing is deterministic across twins + draws nothing from stream 0, and round-trips save/load; **Spanish Succession E2E** — a scripted year-1600 two-AI scenario (one fervent SoL>50, one fading SoL<50) runs the turn and the fading power's colony + unit change hands to the dominant one (flag set once), and does **not** consolidate when both AIs are fervent | ✅ |

- **FreeCol cross-check:** ✅ gate limits + muster cap + REF realisation match `csDeclareIndependence`; the **REF combat doctrine** (colony-first targeting with connected-port +500, no field-unit chase until a colony falls, navy hunting rebel ships / supporting landings) mirrors `REFAIPlayer.findColonyTargets`/`adjustMission`/`REFNavyGoalDecider` as a faithful subset (target selection + colony-first gate; no separate amphibious transport scheduler); intervention accrual matches `ServerPlayer.csNewTurn` + `Monarch.getInterventionForce`; **native re-stancing** (most-hostile contacted nation calmed to back the rebel) is the representable half of `csDeclareIndependence`'s native block; the **Hessian offer** mirrors `loadMercenaryForce` + `csMercenaries(HESSIAN_MERCENARIES)`; the **victory conditions** match `ServerGame.checkForWinner`; the **Spanish Succession** consolidation matches `ServerGame.csSpanishSuccession` (year ≥ 1600, weak SoL < 50, strong SoL > 50).

## 5. Open issues / TODO

- [x] Declare Independence + continental muster + REF takes the field (`86d3c9v28`, save v41).
- [x] **Per-colony continental muster** (`86d3dbufx`) — `MusterContinentalArmy` caps and draws the veteran→colonial-regular upgrade per colony from each colony's own population + tile garrison (FreeCol `csDeclareIndependence`/`getAllUnitsList`), not nationwide. Residual: worker-veterans can't be drawn (workers aren't units) — only the tile garrison. No save bump.
- [x] REF arrival/landing + War-of-Independence combat (`86d3c9v8k`) — `RunRefTurn`/`LandRefUnits` on the REF stream.
- [x] **Specialised REF combat AI** (`86d3drn5a`) — `RunRefTurn` now runs a bespoke doctrine (`RunRefLandUnit`/`RunRefNavalUnit` + `PickRefColonyTarget`/`PickRefUnitTarget`/`NearestRebelColony`) mirroring FreeCol `REFAIPlayer`: colony-first targeting (connected ports +500), no field-unit chase until a colony falls, men-o-war hunting rebel ships / supporting the landings. Faithful subset (no separate amphibious transport scheduler; drydock/military-supplier multipliers + the ±20% twiddle omitted). No save bump.
- [x] REF **fixed entry tile** near the human start (`86d3c9w5n`, save v47) — `Game.RefEntryTile` set at `Game.New` (`NearestWaterTile`), `LandRefUnits` lands the fleet there via `FindLandingTileNear` (falls back to rebel ports / pre-v47 saves).
- [x] Win: defeat/expel the REF → independence granted + Spanish Succession (`86d3c9vfn`, save v42).
- [x] Lose: rebel loses its last connected port (`86d3c9vh1`) — `IsRebelDefeated` (derived; EndTurn doesn't short-circuit).
- [x] Last-colonial-year via ruleset (`86d3drn4t`) — `Ruleset.LastColonialYear` from `model.option.lastColonialYear` (default 1800); the gate reads it instead of a const. No save bump.
- [x] **Foreign Intervention Force** (`86d3c9vap`) — the rebel accrues `interventionBells` = its per-turn liberty; at the threshold a friendly power lands `Ruleset.InterventionForce` near a rebel port (own RNG stream) + the counter resets. Parsed `interventionBells`/`interventionTurns`/`interventionForce`. Reuses `Player.InterventionBells` (no save bump).
- [x] Load the land units aboard the men-o-war before landfall (FreeCol `loadShips`) — the ally fleet arrives offshore with the troops embarked, so a besieged port still gets its ally.
- [ ] Grow the standing intervention force every `interventionTurns` (FreeCol `Monarch.updateInterventionForce`) — a fidelity follow-up.
- [x] **Native re-stancing on declaration** (`86d3drn4p`) — `ShiftNativeStanceOnDeclaration` calms the most-hostile *contacted* native nation to back the rebel (FreeCol `csDeclareIndependence` native block; the representable half of the rule). RNG-free, no save bump.
- [x] **War-time Hessian mercenary offer** (`86d3c9vdb`) — `OfferWarMercenaries` surfaces a one-off `PendingMonarchDemand(HessianMercenaries)` on declaration (human rebel), applied only on accept via `RespondToMonarch`. Monarch RNG, no save bump.
- [x] **Alternative victory conditions** (`86d3drn5n`) — `Game.Winner` now evaluates defeat-REF / defeat-all-Europeans / defeat-all-humans, each gated on a parsed `Ruleset.VictoryDefeat*` boolean (classic true/true/false). Pure reads, no save bump.
- [x] **Spanish Succession E2E test** (`86d3dbugp`) — a scripted year-1600 two-AI consolidation test pinning `RunSpanishSuccession` (no production change). 
- [ ] Remaining native-block fidelity (`86d3c9rg6`): the ally turning hateful toward the REF + the least-hostile contacted nation turning on the rebel — both need a native↔REF / multi-human relationship our single-player native model doesn't have.
- [ ] The War-of-Independence UI (declaration screen, defeat/victory screens, REF-arrival warning) — P7.

## Changelog

| Date | Change | Commit |
|---|---|---|
| 2026-06-21 | **Specialised REF combat AI** (`86d3drn5a`, FreeCol `REFAIPlayer`): `RunRefTurn` replaces the generic foreign-power war AI with a bespoke REF doctrine. The rebel is found explicitly (`RefRebel`); a **REF land unit** (`RunRefLandUnit`) captures an adjacent undefended rebel colony, else marches on the best-scored rebel colony (`PickRefColonyTarget` = the shared `ScoreColonyTarget` + a **`RefConnectedPortBonus` = +500** for connected ports, so coastal/weakest-defended colonies come first), fighting a garrison as a field unit; it **does not chase loose rebel field units until the REF holds a colony** (`holdsAColony` gate, FreeCol "more interested in colonies"), then `PickRefUnitTarget` lets it; with nothing in range it besieges the **nearest rebel colony** (`NearestRebelColony`). A **REF man-o-war** (`RunRefNavalUnit`) seek-and-destroys the rebel's warships (same-domain) and, with none in reach, escorts toward the invasion. Combat resolves on the REF stream via `AttackRebelUnit`/`CaptureRebelColony` (siblings of the generic helpers), recording the same notices. Faithful subset: target selection + colony-first gate only — **no amphibious transport scheduler** (units march overland), the drydock/military-supplier score multipliers + the ±20% `findColonyTargets` twiddle omitted (SoL + stockade terms kept), ambush penalty deferred. **No new state, no save bump.** Runs only after a declaration (the default game/soak never reaches the war) → default game byte-identical; the REF draws only its own stream (existing twin + stream-0 isolation tests stay green). +3 L2 (`IndependenceTests`: no field-unit chase before a colony falls; navy engages a rebel ship; land unit marches on the colony). Targeted Independence/Ref/Combat 294 green | P6 (`86d3drn5a`) |
| 2026-06-21 | **Native re-stancing + Hessian offer + alt-victory + Spanish-Succession E2E** (`86d3drn4p`/`86d3c9vdb`/`86d3drn5n`/`86d3dbugp`). (1) **Native re-stancing** (FreeCol `declareIndependence` native block): on declaration `ShiftNativeStanceOnDeclaration` calms the **most-hostile contacted** native nation (highest peak `Alarm`, among nations the rebel has met) down to the CONTENT band (600) — it backs the rebellion. Faithful-subset: our single-player native model has no native↔REF link and no second human, so only this half is implemented. (2) **War-time Hessian offer** (FreeCol `loadMercenaryForce` + `csMercenaries(HESSIAN_MERCENARIES)`): `OfferWarMercenaries` surfaces a one-off `PendingMonarchDemand(HessianMercenaries)` (human rebel only, on the ephemeral monarch RNG), applied only on accept via the existing `RespondToMonarch` mercenary branch (units to Europe, gold charged); no offer when unaffordable. (3) **Alternative victory conditions** (FreeCol `ServerGame.checkForWinner`): `Game.Winner` now evaluates defeat-REF / defeat-all-Europeans / defeat-all-humans, each gated on a parsed `Ruleset.VictoryDefeat{REF,Europeans,Humans}` boolean (`gameOptions.victoryConditions`; classic true/true/false via `ParseBooleanOption`). Pure reads; `EndTurn` never short-circuits. (4) **Spanish Succession E2E** test pinning `RunSpanishSuccession` (no production change). Also: `HistoryEventKind.DeclaredIndependence` recorded on declaration (in-memory). **No save-version bump** (native alarm/PendingMonarchDemand already persist or are transient; victory options are ruleset-derived; history is in-memory). RNG-free / off-stream-0 changes on the declaration + victory-read paths the default game/soak never reach → default game byte-identical. +12 L1/L2 (`IndependenceTests` 36/36 green) | P6 (`86d3drn4p`/`86d3c9vdb`/`86d3drn5n`/`86d3dbugp`) |
| 2026-06-21 | **Per-colony continental muster** (`86d3dbufx`, FreeCol `csDeclareIndependence` + `Settlement.getAllUnitsList`): `MusterContinentalArmy` now caps and draws the veteran→colonial-regular upgrade **per colony** instead of nationwide. For each colony with SoL > 50 the cap is `(unitCount + 2)·(SoL−50)/100` over *that colony's own* units — its `Population` (workers) plus the rebel's units garrisoning its tile (faithfully reproducing `allUnits.size()`) — and up to that many of *that colony's own* tile-garrison veterans rise (earliest first by id, colonies in id order). Residual faithful-subset deviation: our colony workers are a count not `Unit` objects, so the veterans actually upgraded can only come from the **tile garrison**, never from worker colonists. Conservation preserved (only type swaps) and deterministic. RNG-free change on the declaration path, which the default game/soak never reaches → default game byte-identical; **no save-version bump** (no new persisted field). +2 L1 (`IndependenceTests`: the muster draws from each colony's own garrison not the whole map; the cap counts only the colony's own units) and the existing muster test re-pinned to the per-colony cap from the tile garrison. 1606 L1/L2 + 4 soak green | P6 (`86d3dbufx`) |
| 2026-06-21 | **Intervention Force lands as a fleet with troops aboard** (`86d3c9vap` integration fix, FreeCol `loadShips`): `SpawnInterventionForce` now drops the men-o-war on the water off the rebel's port and **boards the land units aboard them as passengers** (round-robin by hold space, left-overs disposed), rather than scattering all 8 on separate beach tiles. This matches FreeCol *and* lets the ally reach a **besieged** port (it needs sea room for ~2 ships, not an open beach ring) — the prior scatter landed **zero** units when the REF swamped the port. `ResolveWarOfIndependence` made `internal` so the landing is tested in isolation (the REF's units are still in Europe at that point, so the fleet arrives unmolested; a full `EndTurn` lands the REF and sinks it the same turn). Also corrected the S2-delivered FIF tests, which never passed (a men-o-war *entry-count* assertion read 1 not 2; the two turn tests asserted net unit counts confounded by REF combat). RNG-free change on `InterventionStreamId` → default soak byte-identical; no save bump. 1608 L1/L2 + 4 soak green | P6 (`86d3c9vap`) |
| 2026-06-21 | **Foreign Intervention Force** (`86d3c9vap`, FreeCol `ServerPlayer.csNewTurn` rebel branch + `Monarch.getInterventionForce`): during the War of Independence a rebel banks the net liberty it produces each turn into `Player.InterventionBells`; at the ruleset's `interventionBells` threshold (classic medium **5000**) a friendly foreign power lands `Ruleset.InterventionForce` (classic medium 2 colonial-regular soldiers + 2 dragoons + 2 artillery + 2 men-o-war) near a randomly-chosen rebel port — owned by, and fighting for, the rebel — and the counter resets (a long war can draw a second wave). `Ruleset.ParseIntervention` reads `interventionBells`/`interventionTurns`/`interventionForce` from the chosen difficulty level (fallback 5000 / 52 / the 8-unit force). The landfall draws on a **dedicated reserved RNG stream** (`InterventionStreamId = 103`), never stream 0 (ADR-009) — the default soak (never reaching the war) is byte-identical. **Reuses `Player.InterventionBells` → no save bump.** +8 L1/L2 (`IndependenceTests`: classic parses 5000/52/8-unit; `ParseIntervention` reads the level + falls back; threshold lands the force + resets + 2 men-o-war on water; below-threshold nothing; bells accrue liberty; deterministic twin landing; stream-0 untouched; round-trips save/load). 1566 L1/L2 + 4 soak green | P6 (`86d3c9vap`) |
| 2026-06-21 | **`lastColonialYear` from the ruleset** (`86d3drn4t`): the declare-independence year cutoff is now `Ruleset.LastColonialYear` (spec `model.option.lastColonialYear`, classic **1800**), parsed by a new `Ruleset.ParseIntOption` top-level integer-option reader; the hardcoded `LastColonialYear = 1800` const is gone. It's a game option (not a difficulty option) and isn't saved → **no save-version bump**; the default classic game's gate is byte-identical (1800 either way, ADR-009). +5 L1 (`IndependenceTests`: classic parses 1800; `ParseIntOption` reads non-default + falls back; the gate reads the ruleset year not a hardcoded 1800; allows on the cutoff year, blocks the year after). 1536 L1/L2 + soak green | P6 (`86d3drn4t`) |
| 2026-06-20 | **REF fixed entry tile** (`86d3c9w5n`, FreeCol `Player.entryTile`): `Game.New` fixes the REF's entry tile to the water tile nearest the human's start (`NearestWaterTile`, deterministic — no RNG draw), stored on `Game.RefEntryTile` and persisted (save **v47**, omitted when unset → pre-v47 loads with none). `LandRefUnits` now lands the King's fleet at that beachhead first (`FindLandingTileNear`), falling back to the rebel's connected-port colonies when it's full/unset — so the REF always makes landfall at a fixed, faithful spot. No RNG draw → stream 0 byte-stable. +3 L1 (`MultiPlayerTests`: entry tile set on water nearest the start, save round-trip, omit-when-unset/pre-v47 load); 1328 L1/L2 + soak green. See [save-load](save-load.md) | P5 (`86d3c9w5n`) |
| 2026-06-19 | **Declare Independence**: `PlayerType` Rebel/Independent/REF; `NationalSonsOfLiberty`; `CheckDeclareIndependence`/`DeclareIndependence` (gate + lose-Europe + continental muster veteran→colonial-regular + REF player at war on its own stream). Save **v41** (`DeclaredIndependenceTurn` + `InterventionBells` + REF player/units). REF landing/combat + win/lose follow | P6 (`86d3c9v28`) |
| 2026-06-19 | **REF arrival + war combat**: `RunRefTurn` lands the REF (`LandRefUnits`) near rebel ports then reuses the foreign-power war AI to assault the rebel — all on the REF's own RNG stream; the REF↔rebel war stance never decays (colonial-only). No save change (REF units persist via v41). Deterministic twin-game + round-trip tested | P6 (`86d3c9v8k`) |
| 2026-06-19 | **Victory**: `ResolveWarOfIndependence` (per-turn) fires `GiveIndependence` on `CheckForRefDefeat` (no REF colonies + rebel land power ≥ 1.5× REF + REF < 7 land/2 naval) → rebel becomes `Independent` (tax 0), redcoats surrender, `Winner` set. `RunSpanishSuccession` (year ≥ 1600, once). Save **v42** (`SpanishSuccession` flag, omit-until-fired) | P6 (`86d3c9vfn`) |
| 2026-06-19 | **Defeat**: `GetNumberOfPorts` + `IsRebelDefeated` — a declared nation with no connected port has lost; a derived presentation flag, EndTurn never short-circuits (ADR-009). No save change | P6 (`86d3c9vh1`) |
