# System: Independence (the War of Independence)

| | |
|---|---|
| **Status** | Implemented (GameLogic) — declaration + continental muster, REF arrival + War-of-Independence combat, the **foreign Intervention Force** (hold out long enough and a friendly power sends troops), victory (defeat the REF) + Spanish Succession, and defeat (lose your last port). The war UI is P7. |
| **Last verified** | 2026-06-21 @ Intervention Force lands as a fleet with troops aboard (`86d3c9vap` integration fix); 1608 L1/L2 + 4 soak green |
| **Code** | `game/src/GameLogic/GameSession/Game.Independence.cs`, `Force.cs`; `Player.PlayerType`/`DeclaredIndependenceTurn`/`InterventionBells`; `Ruleset.LastColonialYear`/`InterventionBells`/`InterventionTurns`/`InterventionForce` (`Specification/Ruleset.cs`) |
| **Tests** | `game/tests/GameLogic.Tests/GameSession/IndependenceTests.cs` |
| **FreeCol reference** | `InGameController.csDeclareIndependence`, `Player.getSoL`, `model.event.declareIndependence` |
| **Related systems** | [monarchy](monarchy.md), [royal-expeditionary-force](royal-expeditionary-force.md), [sons-of-liberty](sons-of-liberty.md), [combat](combat.md) |

## 1. How it works (plain English)

Once your colonies are fired up enough — at least **half your colonists are rebels** (Sons of Liberty ≥ 50% nationwide) and you hold a **port** — you can **declare independence**, as long as it isn't already too late in history. There's a **last year** after which the mother country will no longer let a colony break away — in the classic game that's **1800**. That cutoff year isn't a number baked into the code: it's a **ruleset setting** read from the game's data, so a different scenario (or the Australia variant) can move it without touching any code. When you do declare:

- your nation becomes a **rebel** fighting for freedom;
- everything you had **waiting in Europe is lost**, and Europe closes its doors (no more recruiting or trade with the mother country);
- your **veteran soldiers rise up as Continental regulars** — the more fervent your colonies, the more of them;
- and the **King sends his army** — the Royal Expeditionary Force he's been building all game — to crush you. From now on it's war.

From the next turn the **King's army sails in and lands near your ports**, then marches on your colonies and hunts your troops. Winning means defeating or driving off that army; losing means being driven from your last port.

**A foreign ally comes to your aid.** While the war drags on, your rebellion keeps producing liberty (bells). A sympathetic foreign power is watching, and the longer and harder you fight, the closer it comes to committing: every bell you generate during the war counts toward a hidden tally. Once that tally crosses a threshold (the classic game's medium setting is **5000**), the foreign power lands a small **Intervention Force** — a couple of colonial regulars, some dragoons, artillery, and a brace of men-o-war — right beside one of your ports, fighting on your side. The tally then resets, so a truly drawn-out war can draw a second wave. It's the game's way of rewarding a stubborn rebellion: hold out long enough and you won't face the King alone.

**What the player sees and does:** nothing yet — the Declaration screen and the war UI are P7. The GameLogic decision is exposed for that UI to call.

## 2. Detailed rules

- **The gate** (`CheckDeclareIndependence`, FreeCol `model.event.declareIndependence` limits): still a plain **colonial** power, **national Sons of Liberty ≥ 50%** (rebels across all colonies ÷ total population), **≥ 1 connected-port colony**, and the **year ≤ the last colonial year** (`Ruleset.LastColonialYear`, the spec `model.option.lastColonialYear` integer game option in the `gameOptions.years` group; classic **1800**). The cutoff is read from the ruleset, not hardcoded — a variant spec moves it by data alone (a missing option falls back to 1800).
- **Declaring** (`DeclareIndependence`):
  - the player becomes a **`Rebel`** and records the turn (drives the score and the intervention force later);
  - every unit **in or bound for Europe is removed**; the **recruit dock is cleared**;
  - **continental-army muster**: veteran soldiers upgrade to **colonial regulars**, the cap summed over colonies with SoL > 50 at `(unitCount + 2) · (SoL − 50) / 100` (the earliest rise first);
  - the **Royal Expeditionary Force** enters play as a new `RoyalExpeditionaryForce` AI player **at war** with the rebel, its amassed [force](royal-expeditionary-force.md) realised into real units mustering in Europe.
- **The war** (`RunRefTurn`, item 8): each REF turn, units still in Europe **land** (`LandRefUnits`) — the King's fleet makes landfall at its **fixed entry tile** (a water tile near the human's start, chosen deterministically at game creation and persisted, FreeCol `Player.entryTile`/`86d3c9w5n`): each unit takes the nearest empty tile to that beachhead (`FindLandingTileNear`, land ashore / ships on water), falling back to the ring around the rebel's connected-port colonies when the beachhead is full or unset (a pre-v47 save). Then the REF **prosecutes the war** — because the rebel *is* the human, the REF reuses the existing foreign-power war AI (hunt + assault the rebel's units and colonies). The REF draws entirely from **its own RNG stream**; the rebel attacks the REF through the normal stream-0 combat. The REF↔rebel war stance never cools (the colonial-tension decay touches only `Colonial` players).
- **Foreign Intervention Force** (`AccrueInterventionBells`/`SpawnInterventionForce`, FreeCol `ServerPlayer.csNewTurn` rebel branch + `Monarch.getInterventionForce`): each turn during the war the rebel banks the **net liberty it produced that turn** into `Player.InterventionBells` (the same figure FreeCol adds in `Player.modifyLiberty` while `isRebel()`). When the running total reaches the ruleset's `interventionBells` threshold (`Ruleset.InterventionBells`, classic medium **5000**), a friendly foreign power sends its `Ruleset.InterventionForce` (classic medium **2 colonial-regular soldiers + 2 colonial-regular dragoons + 2 artillery + 2 men-o-war**): the **men-o-war make landfall on the water off a randomly-chosen connected port** of the rebel, **carrying the land units aboard as passengers** (FreeCol `loadShips` — the ally fleet arrives offshore with the troops embarked, and the player disembarks them where they choose), all **owned by, and fighting for, the rebel** (FreeCol creates the force on the rebel itself). Because it only needs sea room for the ships — not an open ring of beach — the ally can still reach a **besieged** port, which is the whole point. The counter then **resets to 0**, so a long war can draw a second wave (FreeCol grows the standing force every `interventionTurns` = classic 52). The port and the exact sea tiles are the only random choices, drawn from a **dedicated reserved RNG stream** (`InterventionStreamId = 103`), never the human's stream 0 (ADR-009) — so even a human rebel's economy stream is untouched. If the rebel holds no connected port, or no sea room for even one man-o-war, nothing lands.
- **Winning** (`ResolveWarOfIndependence`, item 9): each turn, the rebel wins (FreeCol `checkForREFDefeat`) when the REF **holds no colonies**, the rebel's **land power ≥ 1.5×** the REF's, and the REF is reduced **below 7 land or 2 naval** units (counting its whole force, so the win can't fire before the army even lands). On winning (`GiveIndependence`): peace with the King, the rebel becomes **`Independent`** with **no tax**, the **surviving redcoats on the map surrender** to the new nation (the fleet sails home), and that nation is the **`Winner`**.
- **Spanish Succession** (`RunSpanishSuccession`, a separate event): once, from **1600**, a fading European AI (SoL < 50) is **absorbed** by the dominant one (SoL > 50) — its colonies and units change hands. (Not the win condition; a late-game consolidation.)
- **Losing** (`IsRebelDefeated`, item 10): once you've declared, holding **no connected port** (`GetNumberOfPorts == 0` — the REF has taken them all) means you've **lost** the War of Independence. This is a flag the presentation reads for the defeat screen; the turn loop keeps running regardless (a defeated human must not freeze stream 0 — ADR-009). A plain colony with no port is never "rebel-defeated".

**Deviations from original / FreeCol:** the gate (SoL ≥ 50, ≥ 1 connected port, year cutoff), the veteran→colonial-regular upgrade, the win thresholds (1.5× / 7 land / 2 naval) and the REF realisation match FreeCol. **Faithful-subset simplifications (documented, not yet faithful):** (1) the **continental muster** caps and draws *nationwide* (the rebel's whole unit count + any on-map veteran), not per-colony from each colony's own residents as FreeCol does — our colony workers aren't in the unit list; this can over-muster in a multi-colony rebellion (per-colony port is a follow-up). (2) the **Spanish Succession** ranks the absorbed/absorbing pair by **Sons-of-Liberty** (we have no overall game-score yet) rather than FreeCol's score, and only an AI (not the human) can satisfy its strong-limit trigger. (3) the REF reuses the foreign-power war AI rather than a bespoke amphibious doctrine; the REF combat ambush-penalty and a dedicated naval transport doctrine are deferred. (4) the **Intervention Force** lands a single fixed force at the threshold; FreeCol additionally *grows* the standing force every `interventionTurns` (we parse the option for fidelity but don't yet grow). The fleet arrives FreeCol-faithfully — the men-o-war on the water carrying the land units aboard (FreeCol `loadShips`), troops over the holds disposed — so a besieged port still gets its ally. The bell accrual is recomputed from the rebel's liberty gain via a transient per-turn snapshot rather than hooked into `modifyLiberty`; a turn that straddles a save/load, or a turn the rebel elects a Founding Father (liberty spent), can drop that single turn's contribution — negligible against the 5000 threshold. Native re-stancing on declaration and the on-declaration mercenary offer are deferred.

## 3. Technical design

- `PlayerType` gains `Rebel`, `Independent`, `RoyalExpeditionaryForce` (the lifecycle Colonial → Rebel → Independent; the REF is its own type). `RunPlayerTurn` routes Rebel/Independent through the colonial economy path and the REF through `RunRefTurn` (a stub until item 8).
- `Game.NationalSonsOfLiberty(player)` = `sum(colony.RebelCount) · 100 / max(1, sum(Population))`.
- `CheckDeclareIndependence`/`DeclareIndependence` (ADR-006). The year branch reads `Ruleset.LastColonialYear` (no longer a `LastColonialYear` const). `MusterContinentalArmy` upgrades veterans via the existing `UpgradeUnitType` swap (units are immutable in their type). `CreateRefPlayer` adds a non-human `RoyalExpeditionaryForce` player with its **own RNG stream** (seeded off the human's current state, read non-destructively — stream 0 untouched), sets the WAR stance both ways, and realises `_refForce` into units via `SpawnInEurope`.
- **`Ruleset.LastColonialYear`** is parsed in `Ruleset.Load` by `ParseIntOption(root, "model.option.lastColonialYear", fallback: 1800)` — a top-level `<integerOption>` reader (mirrors `ParseBooleanOption`; reads `value` then `defaultValue`). It's a **game option, not a difficulty option** (it doesn't vary by level, so it lives on the ruleset root beside the `gameOptions.years` calendar, not in `DifficultyOptions`). Not persisted in the save (re-derived from the ruleset on load, like the calendar) → **no save-version bump**; the default classic spec value is 1800, so the default game's gate is byte-identical to the old const.
- **Intervention options** (`Ruleset.InterventionBells`/`InterventionTurns`/`InterventionForce`): parsed by `Ruleset.ParseIntervention(root, difficultyLevelId)`, which — like `ParseDifficulty` — first selects the chosen difficulty level's `optionGroup` subtree (the options are restated under every level, so reading over the whole doc would pick `veryEasy`), then reads `model.option.interventionBells`/`interventionTurns` (`<integerOption>`) and the `model.option.interventionForce` `<unitListOption>` of `<unitOption>` (`unitType`/`role`/`number`) blocks. A missing level — or any missing option — falls back to the classic-medium values (`InterventionForceComposition.ClassicMedium` = 5000 / 52 / the 8-unit ally force), so the default game and a spec without the options are unchanged. `InterventionForceComposition`/`InterventionForceUnit` are records declared alongside `Ruleset`; the land/naval split is decided at spawn time from each unit type's `IsNaval` flag (ruleset-agnostic).
- **Intervention accrual + spawn** (`Game.Independence.cs`): `ResolveWarOfIndependence` (per turn, in `EndTurn`'s world-advance band) calls `AccrueInterventionBells(rebel)` for each rebel. It banks `max(0, rebel.Liberty − snapshot)` into `rebel.InterventionBells` (a transient `_interventionLibertySnapshot` dict seeds from the rebel's current liberty on first sight — so nothing pre-rebellion is counted, and a save/load straddle costs at most one turn's bells), then if `InterventionBells ≥ Ruleset.InterventionBells` it resets the counter to 0 and calls `SpawnInterventionForce(rebel)`. The spawn picks a random connected port, drops the **men-o-war** on the nearest free sea tiles around it (`FindLandingTileNear(naval: true)`), then **boards the land units onto that fleet as passengers** (round-robin by free `CargoSlotsFree`, `CarrierId` set to the carrying ship; land units that exceed the holds are left behind, FreeCol-style) — all owned by the rebel. With no ship able to make landfall, nothing lands. The RNG is a fresh `Pcg32Random(seed, InterventionStreamId)` seeded off the REF's (or rebel's) persisted stream state + `Turn` — deterministic, reproducible across save/load, and never stream 0. `ResolveWarOfIndependence` is `internal` so a test can land the ally in isolation (the REF's own units are still mustering in Europe at that point, so the fleet arrives unmolested — a full `EndTurn` would land the REF and sink the just-arrived fleet the same turn).
- **Determinism (ADR-009):** the REF runs on its own stream (like a foreign power); the human/rebel on stream 0; the monarch tick stops once the player is no longer `Colonial`. The **Intervention Force** lands on the reserved `InterventionStreamId = 103` stream (high id, like LCR/resource-quantity), so a human rebel's economy stream 0 is untouched — the soak (which never reaches the war) stays byte-identical.
- **Save v41 (unchanged — no bump):** `SavedPlayer.DeclaredIndependenceTurn` + `InterventionBells` (both omitted before independence); the `PlayerType` ordinals (Rebel/Independent/REF) and the REF **player row** persist for free, and the REF's + the landed intervention force's units via `SavedUnit.OwnerId`; a pre-independence game is byte-identical to v40. The Intervention Force **reuses the existing `Player.InterventionBells`** field (no new field, no version bump); the per-turn liberty snapshot is transient (reseeded on load).

## 4. Verification

| Layer | Required? | Tests | Status |
|---|---|---|---|
| L1 Unit | Always | `IndependenceTests`: `NationalSonsOfLiberty`; the declare gate (SoL); the **last-colonial-year gate** (classic ruleset parses 1800; `ParseIntOption` reads a non-default value + falls back; the gate reads the **ruleset** year not a hardcoded 1800; allows on the cutoff year, blocks the year after); declaring turns Rebel + loses Europe units + starts the REF at war + realises ≥ 60 REF units; continental muster upgrades veterans (conservation + ≥ 1); **intervention options** (classic parses 5000 / 52 / the 8-unit force; `ParseIntervention` reads the chosen level + falls back); pre-independence save omits the tokens | ✅ |
| L2 Scenario | Always | `IndependenceTests`: EndTurn runs cleanly after declaration (REF stub + rebel colonial path); the full rebellion (Rebel + REF + war stance + REF units) round-trips save/load; **Intervention Force** — reaching the threshold lands the full force off a port (2 men-o-war on the water carrying 6 land units aboard) + resets the counter, below the threshold nothing lands, bells accrue the rebel's liberty each turn, the landing is deterministic across twins + draws nothing from stream 0, and round-trips save/load | ✅ |

- **FreeCol cross-check:** ✅ gate limits + muster cap + REF realisation match `csDeclareIndependence`; intervention accrual (rebel liberty → `interventionBells`, threshold → force near a port, reset) matches `ServerPlayer.csNewTurn` + `Monarch.getInterventionForce`.

## 5. Open issues / TODO

- [x] Declare Independence + continental muster + REF takes the field (`86d3c9v28`, save v41).
- [x] REF arrival/landing + War-of-Independence combat (`86d3c9v8k`) — `RunRefTurn`/`LandRefUnits`, reuses the war AI on the REF stream.
- [x] REF **fixed entry tile** near the human start (`86d3c9w5n`, save v47) — `Game.RefEntryTile` set at `Game.New` (`NearestWaterTile`), `LandRefUnits` lands the fleet there via `FindLandingTileNear` (falls back to rebel ports / pre-v47 saves).
- [x] Win: defeat/expel the REF → independence granted + Spanish Succession (`86d3c9vfn`, save v42).
- [x] Lose: rebel loses its last connected port (`86d3c9vh1`) — `IsRebelDefeated` (derived; EndTurn doesn't short-circuit).
- [x] Last-colonial-year via ruleset (`86d3drn4t`) — `Ruleset.LastColonialYear` from `model.option.lastColonialYear` (default 1800); the gate reads it instead of a const. No save bump.
- [x] **Foreign Intervention Force** (`86d3c9vap`) — the rebel accrues `interventionBells` = its per-turn liberty; at the threshold a friendly power lands `Ruleset.InterventionForce` near a rebel port (own RNG stream) + the counter resets. Parsed `interventionBells`/`interventionTurns`/`interventionForce`. Reuses `Player.InterventionBells` (no save bump).
- [x] Load the land units aboard the men-o-war before landfall (FreeCol `loadShips`) — the ally fleet arrives offshore with the troops embarked, so a besieged port still gets its ally.
- [ ] Grow the standing intervention force every `interventionTurns` (FreeCol `Monarch.updateInterventionForce`) — a fidelity follow-up.
- [ ] Native re-stancing + on-declaration mercenary offer (`86d3c9rg6`).
- [ ] The War-of-Independence UI (declaration screen, defeat/victory screens, REF-arrival warning) — P7.

## Changelog

| Date | Change | Commit |
|---|---|---|
| 2026-06-21 | **Intervention Force lands as a fleet with troops aboard** (`86d3c9vap` integration fix, FreeCol `loadShips`): `SpawnInterventionForce` now drops the men-o-war on the water off the rebel's port and **boards the land units aboard them as passengers** (round-robin by hold space, left-overs disposed), rather than scattering all 8 on separate beach tiles. This matches FreeCol *and* lets the ally reach a **besieged** port (it needs sea room for ~2 ships, not an open beach ring) — the prior scatter landed **zero** units when the REF swamped the port. `ResolveWarOfIndependence` made `internal` so the landing is tested in isolation (the REF's units are still in Europe at that point, so the fleet arrives unmolested; a full `EndTurn` lands the REF and sinks it the same turn). Also corrected the S2-delivered FIF tests, which never passed (a men-o-war *entry-count* assertion read 1 not 2; the two turn tests asserted net unit counts confounded by REF combat). RNG-free change on `InterventionStreamId` → default soak byte-identical; no save bump. 1608 L1/L2 + 4 soak green | P6 (`86d3c9vap`) |
| 2026-06-21 | **Foreign Intervention Force** (`86d3c9vap`, FreeCol `ServerPlayer.csNewTurn` rebel branch + `Monarch.getInterventionForce`): during the War of Independence a rebel banks the net liberty it produces each turn into `Player.InterventionBells`; at the ruleset's `interventionBells` threshold (classic medium **5000**) a friendly foreign power lands `Ruleset.InterventionForce` (classic medium 2 colonial-regular soldiers + 2 dragoons + 2 artillery + 2 men-o-war) near a randomly-chosen rebel port — owned by, and fighting for, the rebel — and the counter resets (a long war can draw a second wave). `Ruleset.ParseIntervention` reads `interventionBells`/`interventionTurns`/`interventionForce` from the chosen difficulty level (fallback 5000 / 52 / the 8-unit force). The landfall draws on a **dedicated reserved RNG stream** (`InterventionStreamId = 103`), never stream 0 (ADR-009) — the default soak (never reaching the war) is byte-identical. **Reuses `Player.InterventionBells` → no save bump.** +8 L1/L2 (`IndependenceTests`: classic parses 5000/52/8-unit; `ParseIntervention` reads the level + falls back; threshold lands the force + resets + 2 men-o-war on water; below-threshold nothing; bells accrue liberty; deterministic twin landing; stream-0 untouched; round-trips save/load). 1566 L1/L2 + 4 soak green | P6 (`86d3c9vap`) |
| 2026-06-21 | **`lastColonialYear` from the ruleset** (`86d3drn4t`): the declare-independence year cutoff is now `Ruleset.LastColonialYear` (spec `model.option.lastColonialYear`, classic **1800**), parsed by a new `Ruleset.ParseIntOption` top-level integer-option reader; the hardcoded `LastColonialYear = 1800` const is gone. It's a game option (not a difficulty option) and isn't saved → **no save-version bump**; the default classic game's gate is byte-identical (1800 either way, ADR-009). +5 L1 (`IndependenceTests`: classic parses 1800; `ParseIntOption` reads non-default + falls back; the gate reads the ruleset year not a hardcoded 1800; allows on the cutoff year, blocks the year after). 1536 L1/L2 + soak green | P6 (`86d3drn4t`) |
| 2026-06-20 | **REF fixed entry tile** (`86d3c9w5n`, FreeCol `Player.entryTile`): `Game.New` fixes the REF's entry tile to the water tile nearest the human's start (`NearestWaterTile`, deterministic — no RNG draw), stored on `Game.RefEntryTile` and persisted (save **v47**, omitted when unset → pre-v47 loads with none). `LandRefUnits` now lands the King's fleet at that beachhead first (`FindLandingTileNear`), falling back to the rebel's connected-port colonies when it's full/unset — so the REF always makes landfall at a fixed, faithful spot. No RNG draw → stream 0 byte-stable. +3 L1 (`MultiPlayerTests`: entry tile set on water nearest the start, save round-trip, omit-when-unset/pre-v47 load); 1328 L1/L2 + soak green. See [save-load](save-load.md) | P5 (`86d3c9w5n`) |
| 2026-06-19 | **Declare Independence**: `PlayerType` Rebel/Independent/REF; `NationalSonsOfLiberty`; `CheckDeclareIndependence`/`DeclareIndependence` (gate + lose-Europe + continental muster veteran→colonial-regular + REF player at war on its own stream). Save **v41** (`DeclaredIndependenceTurn` + `InterventionBells` + REF player/units). REF landing/combat + win/lose follow | P6 (`86d3c9v28`) |
| 2026-06-19 | **REF arrival + war combat**: `RunRefTurn` lands the REF (`LandRefUnits`) near rebel ports then reuses the foreign-power war AI to assault the rebel — all on the REF's own RNG stream; the REF↔rebel war stance never decays (colonial-only). No save change (REF units persist via v41). Deterministic twin-game + round-trip tested | P6 (`86d3c9v8k`) |
| 2026-06-19 | **Victory**: `ResolveWarOfIndependence` (per-turn) fires `GiveIndependence` on `CheckForRefDefeat` (no REF colonies + rebel land power ≥ 1.5× REF + REF < 7 land/2 naval) → rebel becomes `Independent` (tax 0), redcoats surrender, `Winner` set. `RunSpanishSuccession` (year ≥ 1600, once). Save **v42** (`SpanishSuccession` flag, omit-until-fired) | P6 (`86d3c9vfn`) |
| 2026-06-19 | **Defeat**: `GetNumberOfPorts` + `IsRebelDefeated` — a declared nation with no connected port has lost; a derived presentation flag, EndTurn never short-circuits (ADR-009). No save change | P6 (`86d3c9vh1`) |
