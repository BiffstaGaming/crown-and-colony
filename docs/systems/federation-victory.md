# System: Federation Victory (Australian variant)

| | |
|---|---|
| **Status** | In development (Phase-4a core loop + WS3.2 per-region targets implemented) |
| **Last verified** | 2026-07-12 (WS3.2 — per-region referendum targets; full L1/L2 2958 + determinism soak + L3 green) |
| **Code** | `game/src/GameLogic/GameSession/Game.Federation.cs`, `Game.AustralianEffects.cs`, `FederationPhase.cs`, `game/src/GameLogic/Colonies/Colony.cs` (FederationSupport), `game/src/GameLogic/Specification/Ruleset.cs` (`FederationRegionTargets`), `game/presentation/FederationPanel.cs` |
| **Tests** | `game/tests/GameLogic.Tests/GameSession/FederationVictoryTests.cs` (L1/L2), `game/presentation/tests/FederationPanelTests.cs` (L3) |
| **FreeCol reference** | `freecol/data/rules/classic/specification.xml` (`gameOptions.victoryConditions`), FreeCol `Player.checkForDeath` / victory checks (concept only — Federation is our own win path, not in FreeCol) |
| **Related systems** | [independence.md](independence.md), [founding-fathers.md](founding-fathers.md), [colonies.md](colonies.md), [game-modes.md](game-modes.md) |

## 1. How it works (plain English)

*Audience: anyone — no jargon, no class names.*

In the Australian scenario the player does **not** win by declaring war on the King and beating his army (the classic "War of Independence"). Instead the six Australian colonies — New South Wales, Victoria, Queensland, South Australia, Tasmania and Western Australia — must be persuaded, one at a time, to **federate** into a single Commonwealth of Australia by 1901. It is a political win, not a military one.

Each colony builds up **Federation Support** the same way it builds up rebel sentiment in the classic game: from its **Civic Voice** (the Australian name for Liberty Bells — produced by Town Halls, newspapers, schools and the like). The more Civic Voice a colony makes each turn, the more its Federation Support grows. As the movement grows the player also banks national **Convention Points**, a slower separate resource that represents the constitutional groundwork.

Once enough colonies are behind the idea, the player calls a **Federation Convention**; the convention drafts a **constitution**; and finally the colonies hold a **referendum**. If the referendum carries, the **Commonwealth is proclaimed and the player wins**. If it fails, the movement loses some ground but can try again — a failed vote never ends the game.

**The rules, in plain words:**
- Only the Australian scenario uses this. The classic game is completely unchanged — it still wins by War of Independence.
- Every Australian colony banks Federation Support from its Civic Voice each turn (just like it banks Liberty).
- A colony's Federation Support is shown as a percentage (0–100%), the same scale as Sons-of-Liberty support.
- The country is divided into six colony regions. A region's support is the average of the player's colonies in it.
- You can **call a Federation Convention** once at least **4 of the 6 regions are above 40% support** and you have banked at least **200 Convention Points**.
- After the convention is called, the constitution **drafts itself** once you have banked enough Convention Points (400).
- Once the constitution is drafted you can **put Federation to a referendum**, but only when **every colony region you hold has reached its own historical target** — not a flat number. Each colony has a different bar, taken from how it actually voted in 1898–1900: New South Wales **57%**, Queensland **56%**, Western Australia **70%**, South Australia **80%**, and the small, strongly pro-Federation colonies Tasmania and Victoria a steep **94%**. So the hard part of the campaign is winning over the colonies with the highest bars.
- The referendum is a chance-based vote: the stronger your support, the more likely it carries. Carry it and you win; fail it and you lose a little support and can try again.

**Worked example:**
> You hold colonies in New South Wales, Victoria, Queensland and South Australia. Each has a busy Town Hall, so their Federation Support climbs a few points every turn. After many turns all four regions pass 40% and you have 200 Convention Points banked — the **Call the Federation Convention** button lights up. You call it. A few turns later your Convention Points reach 400 and the draft constitution completes on its own. Now each colony must reach **its own target**: New South Wales only needs 57%, but Victoria needs 94%, so you keep pouring Civic Voice into Victoria long after New South Wales is ready. Once all four have crossed their bars you hit **Put Federation to a Referendum**. Support is high, the vote carries, and on the next turn the Commonwealth of Australia is proclaimed — you win.

**What the player sees and does:** In the Australian game a **Federation…** button appears in the bottom-right HUD (where the Declare-Independence button sits in the classic game). It opens the **Road to Federation** screen: the current phase, your Convention Points, a support bar for each of the six colonies, and the context action (Call Convention, or Put to Referendum) — greyed out with the reason shown until its requirements are met.

**The Federation champions:** five of the Australian Pioneers you can recruit into the Federation Convention are the historical champions of Federation itself, and electing one gives your movement a real shove:
- **Henry Parkes** — the "Father of Federation": his Tenterfield Oration lifts Federation Support in **every** one of your colonies at once.
- **Edmund Barton** — the first Prime Minister: he banks a big chunk of Convention Points, bringing the convention forward, and — a New South Welshman himself — **lowers New South Wales's referendum target** by a few points.
- **John Quick** — the referendum's architect: while he sits in your Convention, a close referendum is **more likely to carry** (the vote's bar is lowered).
- **Samuel Griffith** — the constitution's chief drafter: he banks Convention Points toward finishing the draft, so the constitution completes sooner, and — winning over the most reluctant colony — **lowers your hardest colony's referendum target**.
- **Catherine Helen Spence** — the effective-voting campaigner: she reassures the **smaller colonies** (South Australia, Tasmania, Western Australia), lifting their Federation Support specifically — the small states' fear of being outvoted was the real obstacle to Federation.

(These are on top of the general civic bonus each of those figures already gives.) None of them auto-wins the game — the region-support and referendum gates still apply; they just move you along the road faster.

## 2. Detailed rules

*Audience: designers/testers — exact, but still readable.*

**Core resources**

| Resource | Where it lives | How it grows |
|---|---|---|
| Federation Support (per colony) | `Colony.FederationSupport` (points) / `Colony.FederationSupportPercent` (0–100) | Banks the same net Civic Voice (bells) the colony's Liberty pool receives, each turn; floored at 0, capped at the 100%-support ceiling (`RebelLibertyDivisor × population`, classic 200 × pop). |
| Region Federation Support | `Game.RegionFederationSupport(regionKey)` | Unweighted average of `FederationSupportPercent` over the human colonies whose tile lies in that region (0 when none). |
| Convention Points (national) | `Game.ConventionPoints` | Accrues 25% of each turn's positive net human Civic Voice (a sub-point remainder carries so low output still accrues); trails support as a slower axis. |

**Phase machine (`FederationPhase`)**

| Phase | Ordinal | Meaning | Advances when |
|---|---:|---|---|
| ColonialMaturity | 0 | Default; support still gathering. A classic game never leaves this. | Player calls a convention (manual) once the gate opens. |
| ConventionCalled | 1 | Convention called; constitution being drafted. | Convention Points reach 400 (automatic, at end of turn). |
| ConstitutionDrafted | 2 | Constitution complete; a referendum may be put. | Player puts a referendum (manual). |
| Referendum | 3 | A referendum has been held; if it failed, the movement rebuilds here. | A carried referendum → Commonwealth (automatic, at end of turn). |
| Commonwealth | 4 | Terminal. Human has won by Federation. | — (`Game.Winner` reports the human). |

**Action gates**

| Action | Method | Requirements (all must hold) |
|---|---|---|
| Call Convention | `CheckCallConvention` / `CallConvention` | Federation victory enabled · phase = ColonialMaturity · ≥ 4 of 6 regions at ≥ 40% support · ≥ 200 Convention Points. |
| Put to Referendum | `CheckPutToReferendum` / `HoldReferendum` | Federation victory enabled · phase = ConstitutionDrafted or Referendum (retry) · at least one settled region · **every** settled region at ≥ its own referendum **target** (`ReferendumTargetFor` — NSW 57 / Vic 94 / Qld 56 / SA 80 / Tas 94 / WA 70 for Australia; the uniform 50 as a fallback), WS3.2. |

**Referendum roll (deterministic, ADR-009):** `HoldReferendum` seeds a dedicated generator (`Pcg32Random`, stream 107) from the human's own RNG state read **without advancing** stream 0, mixed with the turn and the attempt count. It rolls `0–99`; the vote **carries** when `roll < averageSettledSupport` (so support 100 always carries, 0 never does). The compared support is `min(100, AverageSettledSupport() + ReferendumThresholdRelief())` — the relief is `+10` while **John Quick** sits in the human's Congress (his "Corowa Plan", Phase-4d.7), else 0; it rides the persisted Congress (no new save state) and only ever fires for an Australia game, so classic is byte-identical. A carried vote sets a "carried" flag that `ResolveCommonwealthFederation` reads next turn to proclaim the Commonwealth. A failed vote increments the attempt count and sheds ~10% of each settled colony's banked support (anti-Federation momentum), leaving the phase at Referendum for a retry.

**Democracy & Federation Pioneers — on-election Federation effects (Phase-4d.7):** the five Democracy & Federation Pioneers each drive the loop directly on election, on top of the reused civic effect each already carried. Each is gated on an **Australia-only ability** (classic declares none → no-op → byte-identical), and every Federation-state lever additionally short-circuits unless `Ruleset.VictoryFederation` is on. RNG-free.

| Pioneer | Ability (Australia-only) | Federation effect | API |
|---|---|---|---|
| Henry Parkes ("Tenterfield Oration") | `model.ability.tenterfieldOration` | +`TenterfieldOrationSupport` (60) Federation Support to **every** colony on election — a broad boost across all regions. | `AddFederationSupportToAllColonies` |
| Edmund Barton ("Nation for a Continent") | `model.ability.conventionDrive` | +`ConventionDrivePoints` (100 = half the call gate) national Convention Points on election — enter the convention sequence earlier (human-only). **WS3.2:** additionally **lowers New South Wales's referendum target** by `BartonNswTargetReduction` (3) — 57 → 54 (his home colony; doc 11 C3). | `AddConventionPoints`, `ReduceFederationTarget` |
| John Quick ("Corowa Plan") | `model.ability.corowaPlan` | While in Congress, **lowers the referendum pass threshold** by `QuickReferendumRelief` (+10) — read live in `HoldReferendum`, not a one-off. | `ReferendumThresholdRelief` (in `HoldReferendum`) |
| Samuel Griffith ("Draft Constitution") | `model.ability.draftConstitution` | +`DraftConstitutionPoints` (120 = 30% of the 400 draft gate) national Convention Points on election — the draft completes sooner (human-only). **WS3.2:** additionally **lowers the hardest settled region's referendum target** by `GriffithHardestTargetReduction` (5) — the region furthest below its own target (doc 11 C2). | `AddConventionPoints`, `ReduceHardestRegionTarget` |
| Catherine Helen Spence ("Fair Representation") | `model.ability.fairRepresentation` | +`FairRepresentationSupport` (45) Federation Support to colonies in the **small** regions only (South Australia, Tasmania, Western Australia) on election. | `AddFederationSupportToSmallColonies` |
| George Fife Angas ("Colonial Credit") — WS4.4 | `model.ability.colonialCredit` | +`ColonialCreditSupport` (5) Federation Support to **South Australia** on election (the "Father of South Australia"; doc 08 C4). | `AddFederationSupportToRegion` |
| Mary Lee (South-Australian suffrage) — WS3.2 | `model.ability.womensSuffrage` | **Lowers South Australia's referendum target** by `WomensSuffrageTargetReduction` (5) — 80 → 75 — on election (doc 12 C2, "SA target −5"; human-only). | `ReduceFederationTarget` |

The point/support/reduction magnitudes are balance placeholders (see §5 — Chris to tune). The handlers live in `Game.AustralianEffects.cs` (dispatched from `ApplyAustralianElectionEffects`, itself called once on election from `ElectAndRefreshFounders`); the state-mutating API (`AddConventionPoints`, `AddFederationSupportToAllColonies`, `AddFederationSupportToSmallColonies`, `AddFederationSupportToRegion`, `ReduceFederationTarget`, `ReduceHardestRegionTarget`, `ReferendumThresholdRelief`) lives in `Game.Federation.cs`. **Support vs target (WS3.2):** Barton, Griffith and Mary Lee now lower a region's referendum *target* directly (the honest form of their doc "target −N" clauses); Parkes/Spence/Angas remain +support boosts. The target reductions bank in `Game.FederationTargetReductions` (persisted, v73). **Stacking note:** Angas' +support and Mary Lee's target-cut both help South Australia (and Spence's small-colony boost includes SA), so SA can accumulate several one-off effects if all are elected — flagged for the balance pass (§5).

**Winner:** `Game.Winner` checks Federation **first** — when the victory is enabled and the phase has reached Commonwealth, the human wins. Off (and skipped) for classic.

**Deviations from original 1994 / FreeCol behavior:**
- **This whole system is new** — Colonization 1994 and FreeCol have no Federation win. It is the Australian variant's replacement for the War-of-Independence victory (ADR-021).
- **Imperial Pressure is political/economic only.** Per ADR-021, no Royal-Expeditionary-Force invasion is wired into the Federation path; the classic monarch/REF machinery is left entirely untouched. The King can still tax and bluster, but Federation is won at the ballot box, not on the battlefield.
- **Simplified from the full design.** The design doc (`05_Federation_Victory_System.md`) lists six narrative phases, per-colony historical support targets, Anti-Federation Sentiment, constitutional clauses, the NSW quota-failure rule, the Western-Australia late-entry rule, and five victory **grades**. Phase-4a implemented the mechanically-distinct **core loop** (five states, a single Commonwealth win); **WS3.2** added the **per-colony historical referendum targets** (NSW 57 / Vic 94 / Qld 56 / SA 80 / Tas 94 / WA 70) as the referendum bar (the convention counter stays a uniform 40%). The remaining richer content (constitutional clauses, Anti-Federation Sentiment, phase-1/2 gates, NSW quota / WA late-entry, victory grades) is deferred (see §5).

## 3. Technical design

*Audience: developers / future Claude sessions.*

**Domain model:**
- `FederationPhase` (enum) — the five-state mechanical machine; ordinal-persisted.
- `FederationStage` (enum, WS3.8) — the design's **six named narrative phases** (Colonial Maturity → Federation Movement → Convention Process → Draft Constitution → Referendums → Commonwealth). A presentation view, not persisted; derived by `Game.CurrentFederationStage` (pure read, `None` for classic) which splits Maturity vs Movement on `CurrentEventEra == EventEra.Federation` (the 1889+ band).
- `Game.Federation.cs` (partial `Game`) — owns the phase, Convention Points, referendum state, the accrual hook, the per-region aggregates, the action commands/gates, and the end-turn resolution. Pure C#, engine-free (ADR-006).
- `Colony.FederationSupport` / `AddFederationSupport` / `FederationSupportPercent` — the per-colony banked support, modelled exactly on `Liberty` / `AddLiberty` / `SonsOfLiberty`.
- **Per-region targets (WS3.2):** `Ruleset.FederationRegionTargets` (region key → target %, parsed from the spec) holds each region's historical referendum bar; `Game.ReferendumTargetFor(regionKey)` = base target (or the uniform `RegionSupportForReferendum` = 50 fallback) minus any banked reduction, clamped 0–100; `Game.ReduceFederationTarget` / `ReduceHardestRegionTarget` bank Pioneer target cuts into the persisted `_federationTargetReductions` map. Classic authors no targets → every region reads the uniform fallback (byte-identical).
- `FederationPanel` (presentation) — renders the oracles and forwards the two commands; no rules. **Designed treatment (WS2.7; six-stage tracker + era indicator WS3.8):** on the parchment/wood theme it shows a six-step **phase tracker** (the current design stage lit) with a gold **era-indicator** header ("Phase 2 · Federation Movement"), each colony's support as a **styled gauge** (recessed trough, support-coloured fill — barn-red / ochre / federation-green by how it sits against the referendum bar — and a gold **threshold marker** at the bar, read from the new `Game.ReferendumSupportThreshold` oracle), a gold-edged **Convention Points** chip, and title/header in the theme's `ColonyTitle`/`SectionHeader` variations. Pure presentation (colours re-declared locally from `ColonyTheme`, the `EuropePanel` precedent).
- `PioneerAttainedPanel` (presentation, WS2.7) — the Historical-Figure-attained celebration modal. On electing a Founding Father / Pioneer it names the figure, the body joined (`CongressName`), their design category, a lore line, and a plain-English perk summary. `GameController` opens it when the human's `Congress` grows (Australia-only, gated on `Ruleset.VictoryFederation`). No rules; reads the ruleset only. Centred by absolute position off `GetViewportRect` and content-hugged (autowrap labels pin a width) — the code-built-overlay positioning contract.

**Data sources:** `game/data/rules/australia/specification.xml` → `gameOptions.victoryConditions` → `model.option.victoryFederation` (`true` for Australia). Parsed in `Ruleset.cs` (`ParseBooleanOption`, fallback `false`) into `Ruleset.VictoryFederation`. Classic ships no such option, so it defaults false. **WS3.2:** the same group carries the six `model.option.federationTarget.<region-suffix>` integer options (NSW 57 / Vic 94 / Qld 56 / SA 80 / Tas 94 / WA 70), parsed by `Ruleset.ParseFederationRegionTargets` (id-suffix → region key) into `Ruleset.FederationRegionTargets`; classic authors none → empty dictionary → uniform-bar fallback (ADR-018 variant data; classic byte-identical).

**Algorithms & formulas** (all in `Game.Federation.cs`):
- Accrual — `AccrueFederationSupport(player, colony, netCivicVoice)`, called from the liberty-accumulation loop in `Game.cs` beside `colony.AddLiberty(net)`. Gated on `Ruleset.VictoryFederation`. `_conventionPoints += net × 25 / 100` with a hundredths remainder carry (`_conventionPointsHundredths`), human-only.
- Region average — `RegionFederationSupport` = `Σ FederationSupportPercent / count` over `HumanColoniesInRegion` (resolved via `Map.RegionOf(c.Position)?.Key`). The six keys are `FederationRegionKeys`.
- Referendum gate (WS3.2) — `CheckPutToReferendum` requires every settled region at ≥ `ReferendumTargetFor(k)` (its own per-region target); the failure message names the region furthest below its target with its numbers. The pass *roll* (`HoldReferendum`, stream `FederationStreamId = 107`) still compares against `AverageSettledSupport` (see §2) — the per-region targets are the gate, not the roll.
- Hardest region (Griffith) — `ReduceHardestRegionTarget` picks the settled region with the smallest `RegionFederationSupport(k) − ReferendumTargetFor(k)` margin (furthest below its own target), ties broken by canonical `FederationRegionKeys` order.
- Resolution — `ResolveCommonwealthFederation`, called from `EndTurn` beside `ResolveWarOfIndependence`; drafts the constitution and proclaims the Commonwealth (both automatic transitions). A strict no-op in classic.

**Integration points:** turn resolution order in `Game.cs` — `ResolveWarOfIndependence()` then `ResolveCommonwealthFederation()`. Accrual rides the existing per-colony bells→liberty bake. `Game.Winner` (in `Game.Independence.cs`) checks Federation first. The HUD (`GameController`) shows the Australia-only Federation button (sharing the Independence grid slot; the two are mutually exclusive by `VictoryFederation`) and opens `FederationPanel`.

**Persistence (SaveGame v73):** five additive **omit-when-default** slices, so a classic game writes none of them and serialises byte-identically (ADR-009):
- `SavedColony.FederationSupport` — omitted when 0.
- `SaveGame.FederationPhase` (int?) — omitted for the default `ColonialMaturity` (0).
- `SaveGame.ConventionPoints` (int?) — omitted when 0.
- `SaveGame.Referendum` (`SavedReferendum`: attempts + carried) — omitted when no referendum has been held.
- `SaveGame.FederationTargetReductions` (region key → points, WS3.2/v73) — omitted when empty; a token appears only once a target-lowering Pioneer (Barton / Griffith / Mary Lee) is elected in an Australia game. Griffith's chosen region is state-dependent at election time, so the reductions are persisted rather than re-derived.

The Convention-Points sub-point remainder (`_conventionPointsHundredths`) is **ephemeral** (not persisted); it is only ever touched by an Australia game, so it never shifts the classic soak, and at most a fraction of a point is lost across a save/reload on the slow points axis.

## 4. Verification

*How we know this works — the testing contract for this system (see `docs/TESTING.md` for layer definitions).*

| Layer | Required? | Tests / goldens | Status |
|---|---|---|---|
| L1 Unit | Always | `FederationVictoryTests` — option parse; classic accrues nothing; accrual from Civic Voice; region aggregate; phase gates/advances; referendum determinism; persistence round-trip + classic-omits-tokens. **WS3.2:** the six per-region targets parse (empty for classic); `ReferendumTargetFor` returns them and falls back to the uniform 50; the referendum gate blocks a region at 90% whose target is 94; a Barton target reduction (57→54) round-trips through a v73 save and is absent before any election. **WS3.8:** `CurrentFederationStage` maps each mechanical phase to its design stage, splits Colonial Maturity vs Federation Movement at the 1889 era boundary, and returns `None` for classic. **`AustralianContentTests` (Phase-4d.7)** — the five Democracy Pioneers carry their Australia-only ability (classic has none); electing Parkes lifts every colony; electing Barton/Griffith bank Convention Points; electing Spence lifts the small colonies (SA/Tas/WA) more than the no-Spence control and leaves NSW/Vic untouched; Quick's relief flips a marginal referendum (seed 25) and never turns a carrying vote into a failure. | ✅ |
| L2 Scenario | Always | `FederationVictoryTests.FullySupportedFederation_ReachesTheCommonwealthWin_ViaWinner` (full loop → `Game.Winner`); the Phase-4d.7 Pioneer tests elect each figure in a live Australia-map game and assert its Federation effect; classic byte-stability proven by the global `SoakTests`. | ✅ |
| L3 Interaction | Yes (has UI) | `FederationPanelTests` — renders phase + Convention Points + six region rows; Call-Convention disabled with reason until thresholds met; **WS2.7** the designed treatment is guarded (the `PhaseTracker`, each region `Bar` carrying `fill`/`background` stylebox overrides + the `ReferendumMark`, the `ColonyTitle` title variation). **WS3.2:** each region's `ReferendumMark` anchors at its own target (NSW 0.57, Tas 0.94). **WS3.8:** the tracker has six steps and the `StageName` era indicator names the current design phase (Phase 4 · Draft Constitution when advanced), not a hard-coded one. **`PioneerAttainedPanelTests` (WS2.7)** — the celebration renders the figure name/body/category/perk and hugs its content (a sizing regression guard); electing a figure through a live `GameController` fires the popup automatically. | ✅ |
| L4 Visual | Deferred (render-verified) | No committed pixel golden yet (golden regen is CI-Linux-only — a Windows render would fail the diff); the WS2.7 redesign was **render-verified** via the `DOCS_CAPTURE=1` capture (`docs/guide/img/australia-federation.png`). Classic goldens unaffected (button/panel invisible in classic). A committed golden can be regenerated on the CI runner when the panel-golden pipeline is confirmed stable. | ⬜ |
| L5 Soak | Covered by global suite | `SoakTests` (25 seeds × 200 turns, classic) stays byte-identical — the Federation loop and every Pioneer effect are gated off. | ✅ |

- **FreeCol cross-check:** N/A for the win path itself (Federation is our own design). The **accrual** reuses the FreeCol-faithful bells→Liberty bake unchanged, and Federation Support mirrors the FreeCol Sons-of-Liberty percentage formula (`points × 100 / (LibertyPerRebel × population)`, clamped 0–100). The Democracy-Pioneer on-election effects are also our own design (the classic fathers have no Federation equivalent).
- **Local results (2026-07-09, Phase-4d.7):** L1/L2 `dotnet test … --filter "Category!=Soak"` → 2923 passed / 0 failed (7 new Pioneer-effect tests in `AustralianContentTests`). Soak `--filter "Category=Soak"` → 5 passed / 0 failed (classic byte-identical — every Pioneer effect is keyed on an Australia-only ability).

## 5. Open issues / TODO

- [ ] **Full five victory grades deferred** (Bare / Stable / Reform / Treaty / Economic Commonwealth) — Phase-4a ships a single Commonwealth win. Grading needs legitimacy, First-Nations relations, debt/infrastructure and reform-clause inputs that don't exist yet.
- [x] **Per-colony historical support targets — DONE (WS3.2, 2026-07-12).** The referendum bar is now each region's own historical target (NSW 57 / Vic 94 / Qld 56 / SA 80 / Tas 94 / WA 70), authored as Australia variant data. The convention counter deliberately stays a uniform 40% (design Phase 3 is a separate number). *Follow-on (deferred): whether targets should be difficulty-scaled, and population-weighting the region average.*
- [ ] **Constitutional clauses deferred** — no clause drafting/effects; the constitution "drafts" on a Convention-Points gate only.
- [ ] **Anti-Federation Sentiment deferred** — modelled implicitly as low support / the failed-referendum support drain, not as a separate tracked resource.
- [ ] **NSW quota-failure and WA late-entry special rules deferred.**
- [ ] **Phases 1–2 prerequisites (Colonial Maturity / Federation Movement gates) not wired** — settlement-maturity/region-activation oracles exist (`SettlementMaturityOracleTests`, doc 06) but are not yet gates on the loop.
- [ ] **AI opponents do not pursue Federation** — the loop is human-only (it is the human's win path).
- [ ] **L4 Australia HUD golden** — add once the Australia in-game HUD is a stable capture target.
- [ ] **New-Game victory override seam** — `VictoryFederation` is parse-time only (no in-game toggle, unlike the three defeat conditions).
- [ ] **Pioneer effect magnitudes are balance placeholders (needs Chris)** — Parkes +60 support/colony, Spence +45 to each small colony, Barton +100 / Griffith +120 Convention Points, Quick +10 referendum relief, Angas +5 SA support, and the WS3.2 target reductions: Barton −3 NSW target, Griffith −5 hardest-region target, Mary Lee −5 SA target. First-pass values (a colony's 100%-support ceiling is `200 × population`, so +60 is ~30% of a pop-1 colony's bar; a −N target cut makes that region cross ~N points sooner). Tune once the loop is playtested end-to-end.
- [x] **Barton / Griffith / Mary Lee "target −N" clauses honoured (WS3.2).** With real per-region targets, these three now lower a region's referendum target directly (Barton NSW −3, Griffith hardest-region −5, Mary Lee SA −5), replacing the WS4.4 +support proxy. *Still mapped to the nearest lever:* Griffith's "+30% draft progress" → 30% of the draft Convention-Points gate (no separate constitution-progress bar yet); Parkes' "−10 apathy" folded into the broad support boost (no Anti-Federation Sentiment resource); Quick's clause → referendum threshold relief (no `Elected Delegates` mechanic); Spence's Senate-Equality/PR clauses deferred (no constitutional-clause system — WS3.3).

## Changelog

| Date | Change | Commit |
|---|---|---|
| 2026-07-12 | **Six named campaign stages + era indicator (WS3.8 — Federation depth, presentation-only).** The design's six narrative phases (doc 05: Colonial Maturity → Federation Movement → Convention Process → Draft Constitution → Referendums → the Commonwealth win) are now shown, mapped from the coarser mechanical `FederationPhase` by a new pure oracle **`Game.CurrentFederationStage`** (returns the `FederationStage` enum; `None` for classic). Within the single mechanical `ColonialMaturity` state it distinguishes **Colonial Maturity** vs **Federation Movement** by the calendar — the movement is under way once the 1889+ Federation era has begun (`CurrentEventEra == EventEra.Federation`), reusing the 4c.10 era bands rather than a magic year. The `FederationPanel` phase tracker grew from five to **six** steps and gained a gold **era-indicator** header ("Phase 2 · Federation Movement"); the narrative line folds in design Phase 6 (Failure/Retry) after a failed vote. **No new game state** (a pure derived read, no save bump) → classic byte-identical. +2 L1 (`FederationVictoryTests`: the six-stage mapping incl. the 1889 Colonial-Maturity→Federation-Movement split and classic `None`) +2 L3 (`FederationPanelTests`: the six-step tracker + the era indicator names the current phase, not a hard-coded one). Render-verified (`australia-federation.png`). | (this commit) |
| 2026-07-12 | **Per-colony historical referendum targets (WS3.2 — Federation depth).** The referendum gate was a uniform 50% for every colony; it is now each region's own historical target (NSW 57 / Vic 94 / Qld 56 / SA 80 / Tas 94 / WA 70), authored as Australia variant data in `specification.xml` (`model.option.federationTarget.*`) and parsed into `Ruleset.FederationRegionTargets`; the convention counter deliberately stays a uniform 40% (a separate design Phase-3 number). New `Game.ReferendumTargetFor(regionKey)` oracle (base target − banked reduction, clamped 0–100; falls back to the uniform 50 when a region/ruleset authors no target). `CheckPutToReferendum` now requires every settled region at ≥ its own target — a deliberate **difficulty increase** (Tasmania/Victoria need 94%, not 50%). The three "target −N" Pioneer clauses were **flipped from the WS4.4 +support proxy to real target reductions**: Barton lowers NSW's target by 3, Griffith the hardest settled region's by 5, Mary Lee South Australia's by 5 (banked in the persisted `_federationTargetReductions` map; Angas/Parkes/Spence stay +support). The `FederationPanel`'s per-region gauge marker + readiness colour now read each region's own target (NSW greens at 57%, Tas/Vic at 94%). SaveGame **v73**: `FederationTargetReductions` slice, omit-when-default (classic + no-Pioneer Australia game byte-identical; `AddFederationSupportToHardestColony` removed as dead code). +6 L1/L2 (`FederationVictoryTests`: target parse, `ReferendumTargetFor` fallback, per-region gate blocks a 90%-but-target-94 region, target-reduction save round-trip; `AustralianContentTests`: Barton/Griffith/Mary Lee lower targets). Render-verified (`australia-federation.png` — per-region markers). Full L1/L2 2958 + determinism soak + L3 `FederationPanelTests` green. Locked defaults approved by Chris (2026-07-12): the six targets as-is, flip all three clauses. | (this commit) |
| 2026-07-12 | **Historical-Figure-attained celebration popup (WS2.7 UI polish, presentation-only)** — electing a Founding Father / Australian Pioneer was previously only a buried line in the History log. A small parchment modal (`PioneerAttainedPanel`, built in code like `FederationPanel`, added to the `UI` layer) now marks the moment: the figure's name and the body they join (the variant's `CongressName` — "Federation Convention" for Australia), their design category (Trade→"Industry & Commerce", Exploration→"Exploration & Infrastructure", Military→"Settlement, Administration & Defence", Political→"Democracy & Federation", Religious→"Social Reform & First Nations"), a lore line, and a plain-English perk summary (mirrors the Colopedia father-effect line; goods/unit names read through the variant `DisplayOverrides`). `GameController` detects a new `Congress` member each `RefreshView` (tracking `_lastCongressCount`) and opens it, **gated on `Ruleset.VictoryFederation`** so the classic game never shows it — classic UI untouched, no rules change (ADR-006). Centred over the viewport by absolute position off `GetViewportRect` (a code-built centre-anchored overlay corner-pins against its 0-size `CanvasLayer` parent) and sized to hug its content (autowrap labels carry a pinned width so their min-height isn't computed one-char-wide, which had ballooned the card to full height). +2 L3 (`PioneerAttainedPanelTests`: the panel renders name/body/category/perk and hugs its content; electing a figure through a live controller fires the celebration) — the content-hug assertion is a regression guard for the sizing bug. Render-verified (`docs/guide/img/australia-pioneer-attained.png`). This completes the WS2.7 UI-polish pass. | (this commit) |
| 2026-07-11 | **Colony-maturity tier in the colony header (WS2.7 UI polish, presentation-only)** — the existing `Game.SettlementMaturityOf` oracle (Outpost → Township → Colonial Town → Colonial Capital) was computed but surfaced nowhere; the Australian colony screen now shows the tier on the header info line (`ColonyPanel` `VBox/ColonyInfo`, a presentation `MaturityLabel` enum→name map). **Australia-only** (gated on `Ruleset.VictoryFederation`), so the classic colony header is unchanged. +1 L3 (`AustraliaReskinPanelTests`: a fresh Australia colony reads "Outpost" in its header). Render-verified (`docs/guide/img/australia-colony.png`). | (this commit) |
| 2026-07-11 | **Federation panel — designed treatment (WS2.7 UI polish)** — the campaign's central dashboard was lifted from plain labels + default grey `ProgressBar`s to a *designed* screen on the parchment/wood theme, **presentation-only** (no rules change; the panel still only reads oracles + forwards the two commands). Adds a five-step **phase tracker** (Maturity → Convention → Constitution → Referendum → Commonwealth, current lit gold, past filled wood, future dimmed), per-colony **styled support gauges** (recessed parchment trough + support-coloured fill — barn-red below `bar/2`, ochre approaching, federation-green at/over the referendum bar — with a gold **threshold marker** at the bar), a gold-edged **Convention Points** chip, `ColonyTitle`/`SectionHeader` title/header variations, and a gold call-to-action on the one enabled action. New **`Game.ReferendumSupportThreshold`** public read-only oracle (exposes the internal referendum bar for the marker + colour, ADR-006). All node names the L3 tests rely on are preserved; +1 L3 (`FederationPanelTests`: phase tracker + styled gauges + title variation) guards against a regression to the plain look. **Render-verified** via `DOCS_CAPTURE=1` (`docs/guide/img/australia-federation.png`; the capture now founds six colonies with a support spread). Australia-only (classic never shows the panel) → classic UI untouched; full L1/L2 2955 + determinism soak green. Other WS2.7 targets (event popup, HUD era indicator, colony-maturity badges, Pioneer-attained popup) remain as follow-ups. | (this commit) |
| 2026-07-09 | **Commonwealth victory screen (WS1.2)** — the Federation win is now *presented* correctly. Before this, `Game.Winner` returned the human at `FederationPhase.Commonwealth` but `VictoryPanel` was not Federation-aware, so it showed the classic *"Every rival European power has been swept from the New World"* reason and named the winner by nation-id tail ("English"). `VictoryPanel` now branches on `IsFederationVictory` (the variant supplies Commonwealth text **and** `VictoryFederation` is on — which is the exclusive win): it titles the screen **"The Commonwealth of Australia is proclaimed"**, shows the doc-19 proclamation, and — a BINDING requirement (docs 03/15/19) — the **historically-honest addendum** on who the 1901 settlement excluded (Aboriginal and Torres Strait Islander peoples and other communities). The text is variant-scoped (ADR-018): new nullable `GameVariant.CommonwealthVictoryTitle`/`CommonwealthProclamation`/`CommonwealthAddendum` (Australia set, classic null), threaded to `VictoryPanel.Open`. Classic byte-identical (nulls → the existing "{winner} is victorious!" path); the score/stats sections are unchanged. +1 L1 (`AustraliaReskinTests`: fields set for Australia, null for classic) +1 L3 (`VictoryPanelTests`: the Federation win shows the proclamation + addendum, not "swept"/"English") + a render capture (`docs/guide/img/australia-victory.png`). Full L1/L2 2936 + 5 soak green. **Deferred:** the referendum-failure screen (a failed referendum is mid-game — retry, not game-over) and the low-legitimacy-victory warning (needs the First Nations legitimacy system, WS5). | (this commit) |
| 2026-07-09 | **The Federation Referendum is now the SOLE win path** (Chris 2026-07-09). `Game.Winner` returns the Commonwealth win when `VictoryFederation` is on and **skips the classic conditions entirely** (defeat-REF / last-European / last-human) — the referendum replaces that whole model, and it prevents a degenerate turn-1 "last European standing" instant win under the variant's 0 rival powers. The Australia spec's `victoryDefeatREF`/`victoryDefeatEuropeans` set to `false` for consistency; new `GameVariant.ReferendumVictoryOnly` (Australia) drives the New-Game dialog to switch off + lock the classic victory checkboxes. Classic byte-identical (its `Winner` skips the Federation branch when the option is off). | (this commit) |
| 2026-07-09 | **Phase-4d.7 — Democracy & Federation Pioneers get real on-election Federation effects.** Parkes (`tenterfieldOration`) → broad +Support to all colonies; Barton (`conventionDrive`) → +Convention Points toward the convention; Griffith (`draftConstitution`) → +Convention Points toward the draft; Spence (`fairRepresentation`) → +Support to the small colonies (SA/Tas/WA); Quick (`corowaPlan`) → lowers the referendum pass threshold (+10, read live from the persisted Congress in `HoldReferendum`). New API in `Game.Federation.cs` (`AddConventionPoints`, `AddFederationSupportToAllColonies`, `AddFederationSupportToSmallColonies`, `ReferendumThresholdRelief`); handlers in `Game.AustralianEffects.cs`; five Australia-only abilities added to `australia/specification.xml`. All keyed on Australia-only abilities + `VictoryFederation`, so classic is byte-identical (soak green). Magnitudes are balance placeholders; several doc-11 clauses mapped to the closest supported lever (see §5). | (this commit) |
| 2026-07-09 | Initial implementation & documentation — Phase-4a core Federation victory loop: per-colony Federation Support, Convention Points, `FederationPhase` state machine, seeded referendum, Commonwealth win via `Game.Winner`, SaveGame v72 persistence (omit-when-default), Federation HUD panel. Imperial Pressure kept political only (no REF); five victory grades deferred. | (this commit) |
