# System: Combat

| | |
|---|---|
| **Status** | In development (Phase 5: slice 5b = the attack action, roles/equipment, brave defenders, outcomes, native alarm; 5c = native settlement assault; on-map combat UI; 1b = native raids; 1c-2 = foreign-power retaliation at war; 1c-3a–d = naval combat / damage+repair / loot / piracy / Drake; **1c-3e = colonial-colony assault + capture** — a land unit captures an ungarrisoned rival colony (human-initiated). Plunder, the colony defence bonus, and foreign-AI capture are later sub-slices.) |
| **Last verified** | 2026-06-15 @ Phase 5 slice 1c-3e (colonial-colony capture) |
| **Code** | `game/src/GameLogic/Combat/Combat.cs` (`CombatModel`); the attack + settlement-assault actions, roles/equipment + outcomes in `GameSession/Game.cs`; data on `Specification/UnitType.cs`, `TerrainType.cs`, `RoleType.cs`, `UnitChange.cs`, `NativeNationType.cs` (`SettlementType`/`SettlementPlunder`), parsed in `Ruleset.cs`; unit owner/role on `Units/Unit.cs` · UI: `presentation/GameController.cs` (`HandleTileClick` → `AttackUnitAt`/`AttackSettlementAt`) |
| **Tests** | `Combat/CombatModelTests.cs` (pure model), `Specification/RoleTests.cs` (role/unit-change data), `GameSession/CombatTests.cs` (attack action, outcomes, fathers, equip, braves, **settlement assault/plunder**), `Persistence/SaveGameTests.cs` (v19), `presentation/tests/InputTests.cs` (click-to-attack L3) |
| **FreeCol reference** | `freecol/src/.../common/model/SimpleCombatModel.java` (power + `resolveAttack`, settlement branch), `Unit.java` (role helpers), `Tension.java` (alarm deltas), `RandomRange.java` (`getAmount` → plunder); data in `freecol/data/rules/classic/specification.xml` (`<roles>`, `<unit-change-types>`, `<plunder>`) |
| **Related systems** | [ruleset-data](ruleset-data.md), [units-movement](units-movement.md), [natives](natives.md) (settlements, alarm), [founding-fathers](founding-fathers.md) (Washington/Revere/Cortés), [save-load](save-load.md) |

## 1. How it works (plain English)

When one unit attacks another, the game weighs the attacker's **offence** against the defender's **defence**, then rolls the dice. The chance of winning is simply the attacker's strength as a share of the total: a power-3 attacker against a power-1 defender wins three times out of four.

Strength is the unit plus its **equipment**. A bare colonist can't fight — it has no offence. Arm it with 50 muskets and it becomes a **soldier** (+2 offence, +1 defence); add 50 horses and it's a **dragoon** (+3/+2). Equipment comes from a colony's warehouse: standing in the colony, a colonist trades muskets and horses for a military role. Native **braves** are the same idea from the other side — a settlement's warriors, who can pick up captured muskets to become armed braves.

The defender is also helped by rough ground (a hill doubles defence, a forest adds half again), by digging in, and by sheltering in a settlement. The roll is **graded** — a clear win versus a narrow one — which decides how badly the loser comes off.

**What happens to the loser**, in order:
- A unit that is **doomed on any loss** (a brave, a scout) is **killed**.
- An **armed** loser is **disarmed** first: it loses one step of equipment (a dragoon drops to a soldier, a soldier to an unarmed colonist) and the *winner can capture that equipment* — a brave that beats your soldier walks away armed with his muskets. A disarmed unit usually survives.
- An **unarmed but capturable** colonist is **taken prisoner** (it switches sides).
- Some units **demote** instead — artillery that loses becomes damaged artillery, and damaged artillery that loses is destroyed.
- Otherwise the loser is **killed**.

**The winner** may be **promoted**: a free colonist soldier who wins decisively can become a veteran soldier, and a veteran a colonial regular. Normally only a *decisive* win promotes, and only by chance — but with **George Washington** in your Congress, *every* win promotes. **Paul Revere** is the defensive counterpart: if natives ever attack a colony whose last colonist is unarmed, that colonist grabs muskets from the warehouse to defend.

**Attacking natives makes them angry.** Striking at a brave raises its settlement's alarm; killing the brave raises it a lot more. Enough hostility and the settlement turns from wary to hateful.

**Ships fight too.** An armed warship (a frigate, privateer or man-o-war) can attack an adjacent enemy ship on the water. Naval battles play a touch differently: there's no terrain to hide behind on open water, and a ship riding low with a full hold fights worse (every cargo load is a handicap). A beaten ship usually **sinks** — and everything aboard, cargo and passengers, goes down with it — though a ship can also **evade** a weaker attacker and slip away unharmed. (For now every defeated ship sinks; limping home to a drydock for repairs comes later.) Foreign powers you're at war with will send their warships after your ships, and you'll be told when one is sunk.

**And now they strike back.** Once a tribe is angry enough (Displeased or worse) its braves come hunting — they march on your nearest colonist or soldier and attack it. A brave that beats an unarmed colonist kills it; one that beats an armed soldier knocks him back a step (and may take his muskets). When you're raided, the status bar tells you. Braves don't sack your colony buildings or take prisoners in this slice. (The full story of *who* the natives target and *when* lives in [natives](natives.md).)

**Sacking a settlement.** You can also attack a native settlement itself. It defends with its warriors *and* its walls — a camp adds half again to its defence, a capital doubles it — so you'll usually want artillery or massed soldiers. Win and you **sack it**: the settlement is destroyed and you carry off **plunder** (gold — a camp gives a few hundred, a rich village far more, and **Hernán Cortés** doubles the take and always finds treasure). The whole nation seethes: its *other* settlements' alarm jumps. Lose, and your attacker is beaten back — disarmed or, for artillery, smashed to damaged artillery — while that settlement turns on you. (You can't simply walk onto a native settlement; you attack it, or trade/talk from beside it.)

**What the player does:** with a unit selected, **click an adjacent enemy brave or native settlement to attack it** — the same click that would move you onto an empty tile attacks an enemy that holds it instead. The result is shown in the HUD ("won the battle" / "beaten back"; "the native settlement was sacked!" / "assault was repelled"), and attacking ends the unit's turn. (Arming colonists still happens via the colony equip action; richer combat feedback/animation comes later.)

**Worked example:**
> Your free colonist, armed as a soldier (offence 0 + 2 = 2), attacks a native brave (defence 1) in the open. The soldier's offence with the +50% attack bonus is 2 × 1.5 = 3; the brave's defence is 1. The win chance is 3 / (3 + 1) = 75%. If you win, the brave (doomed on any loss) is killed and the settlement's alarm jumps. If you lose, your soldier drops his muskets and walks home a plain colonist — and the brave picks the muskets up.

## 2. Detailed rules

**Base power** comes from the unit type (`UnitType.Offence`/`Defence`) plus the unit's **role** (`RoleType.Offence`/`Defence`): free colonist 0/1, brave 1/1, artillery 7/5, veteran soldier 0/1.5 (its punch is the role); roles — soldier +2/+1 (50 muskets), dragoon +3/+2 (50 muskets + 50 horses), scout +1/+1 (50 horses), armed brave +2/+1 (25 muskets, native-only), mounted brave +1/+1 (25 horses), native dragoon +3/+2.

**Attacker percentage modifiers** (FreeCol `GENERAL_COMBAT_INDEX`): attack bonus **+50%**; movement penalty **−33%** (2 movement points left) / **−66%** (1 left); amphibious **−75%**; artillery-in-the-open **−75%** (a `bombard` unit fighting outside a settlement).

**Defender percentage modifiers**: terrain defence bonus (plains 0, marsh +25%, most forest +50%, rainForest +75%, hills +100%); fortified **+50%**; settlement defence bonus (5c).

**Odds & resolution**: win probability = `attack / (attack + defence)`; a draw `r ∈ [0,1)` grades it — `r < 0.1·win` great win, `r < win` win, `r ≥ 0.1·win + 0.9` great loss, else loss.

**Loser outcome precedence** (FreeCol `SimpleCombatModel.resolveAttack`, land open-field path):
1. **dispose-on-combat-loss** (type or role: brave, scout) → **slaughter**.
2. **offensive role** → disarm: the winner captures the equipment (`Unit.canCaptureEquipment` — the winner has `captureEquipment` and a military `role-change` matching `from = winner role`, `capture = loser role`, available to its side) and arms itself into that role; the loser steps to `Role.Downgrade ?? default`. Then, if the loser is *dispose-on-all-equipment-lost* with no downgrade it is slaughtered, or if *demote-on-all-equipment-lost* it is type-demoted.
3. **canBeCaptured + winner captureUnits** (not amphibious) → **capture the unit**: it changes owner, taking the `capture` unit-change (veteran soldier → free colonist) if one exists, and is disarmed.
4. a **demotion** unit-change exists → **demote the type** (artillery → damaged artillery).
5. otherwise → **slaughter**.

**Winner promotion** (FreeCol): if the winner's type has a `promotion` unit-change and either it has `automaticPromotion` (Washington) **or** the result was a *great* win and `100·r ≤ probability`, it promotes (free colonist → veteran soldier → colonial regular). The promotion roll is a **second draw** from the same RNG, taken only when needed (`great && !automatic`).

**Native alarm from combat** (FreeCol `defenderTension`, applied across the defender nation's *every* settlement via `csModifyTension`): tension is driven by the **outcome**, not the act of attacking. A European **win** raises it; a **repelled** attack *lowers* it (the natives prevailed, their tension toward you eases). Open-field (5b): killing a brave adds **+500** (`UNIT_DESTROYED` +400 + a minor insult `TENSION_ADD_MINOR` +100); a loss subtracts **−100** (−`MINOR`), or **−300** if your unit is slain (−`MINOR` − `NORMAL`).

**Settlement assault (5c).** A player land unit attacks a native settlement *tile* (`CheckAttackSettlement`/`AttackSettlement`). The settlement defends with an **implicit garrison**: a brave's defence (1) times its **settlement defence bonus** (camp/village +50%, capital/city +100%, city capital +200%); the open-tile terrain bonus, ambush, and the REF-only bombard bonus do **not** apply in a settlement. One graded combat draw resolves it:
- **Attacker wins** → the settlement is **sacked**: the attacker may promote (as above), takes **plunder** (next paragraph), the nation's alarm shifts (below), and the settlement is **destroyed** (removed from the map).
- **Attacker loses** → the attacker runs the loser-outcome precedence (disarmed / artillery → damaged artillery / slaughtered); the settlement survives and the nation's alarm *eases*.

**Plunder** (FreeCol `RandomRange.getAmount`, `continuous=false`): the sacked settlement's `<plunder>` range pays `(rnd[0, max−min] + min) × factor` gold, gated by a `probability`% roll (100 = always). Each settlement type carries a **base** range and a richer **extra** range; the attacker uses the extra range iff it has `model.ability.plunderNatives` — granted by **Hernán Cortés**. Pinned (gold): camp 200–300 @20% (Cortés 300–600 always); village 300–800 @50% (Cortés 400–1200); camp capital 400–800 (Cortés 600–1200); village capital 600–1600 (Cortés 900–2400). Credited straight to gold (treasure-train logistics + Cortés's transport-fee discount are deferred).

**Settlement-assault tension** (FreeCol `defenderTension`, nation-wide): sacking a **non-capital** settlement adds **+900** = `SETTLEMENT_ATTACKED` (+500, the slain in-settlement defender) + `MAJOR` (+300, the destruction) + `MINOR` (+100), applied to the nation's **other** settlements (the sacked one is removed; a lone-settlement tribe's alarm dies with it). Burning a **capital** instead makes the nation **surrender**: every surviving settlement's alarm is *set* to `SURRENDERED` = **350** (`(Content 600 + Happy 100)/2`). A **repelled** assault *lowers* the nation's alarm by **−100** (or **−300** if your unit is slain), across all its settlements.

**Native-initiated combat (slice 1b).** Braves now attack too: the native AI (see [natives](natives.md)) drives a hostile brave to attack an adjacent **human unit** through the *same* `Attack` action — but via the internal `Attack(brave, target, RandomFor(nativePlayer))` overload, so combat draws from the **nation's own RNG stream**, never the human's stream 0 (ADR-009). A brave can only target **human-owned units** (the AI's target filter; the engine's `AreEnemies` owner-inequality alone would also allow rival powers/tribes); it cannot assault native settlements (the `CheckAttackSettlement` native gate stays closed). A brave attacking a human unit standing on a **colony tile** fights an open-field combat (terrain bonus only — *no* settlement-defence bonus), and that defender auto-equips via **Paul Revere** if it is the colony's last unarmed colonist. Because the defender is the human (not a native), the native-alarm path is skipped — a raid never changes the raider's own alarm. A brave **cannot capture units** (`captureUnits=false` in the classic spec), so a beaten unarmed colonist is **slaughtered**, not taken — and `CaptureUnit` is hardened so that even a hypothetical native captor never mis-assigns the captive to the human (a brave's `OwnerId` is 0 = the human's id; `OwnerNationId` is the authoritative native owner).

**Foreign-power-initiated combat (slice 1c-2).** Foreign powers now fight back: a power at `Stance.War` with the human (war starts when the human attacks it — see [diplomacy](diplomacy.md)) drives its **armed** units (offence > 0, e.g. its starting soldier) after the human's nearest unit via `RunForeignPowerTurn` → `AttackHumanUnit`, the foreign sibling of the native `RaidHumanUnit`. Identical seam to 1b: it targets **human units only** (`NearestHumanUnit`), attacks via the internal `Attack(unit, target, RandomFor(power))` overload so combat draws from the **power's own RNG stream** (never stream 0), and records a `CombatNotice` (attacker = the European nation id) surfaced in the HUD. It does **not** assault human *colonies* (1c-3) and does not yet *declare* war on its own. The war branch is dormant at peace, so the existing foreign-power determinism/goldens/soak stay byte-stable.

**Naval combat (slices 1c-3a / 1c-3b).** A ship attacks an adjacent enemy ship through the same `Attack`/`CheckAttack` spine, but resolution differs (`CombatModel.ResolveNaval`, FreeCol `SimpleCombatModel`): the win bands are the same (`r < 0.1·win` great win, `< win` win) but the **loss** range opens with an **evade** sub-band — `r < 0.8·win + 0.2` → the defender slips away unharmed (every ship has `evadeAttack`), then `r ≥ 0.1·win + 0.9` great loss, else loss. **Modifiers:** the +50% attack bonus and movement penalty still apply; **no terrain defence bonus** on open water; **no** fortify/amphibious/artillery-in-open; and a **cargo penalty** of **−12.5% per goods slot** (`CombatModel` `CargoPenalty`, applied to both offence and defence — a laden ship fights worse; goods slots only, via `GoodsSlotsUsed`, FreeCol `getGoodsSpaceTaken`). Ship offence/defence come from the unit type (frigate 16/16, privateer 8/8, man-o-war 24/24; transports like the caravel have **offence 0** so they can't initiate — the `OffenceBase ≤ 0` gate refuses them). Ships and land units **cannot attack each other** directly (cross-domain gate, FreeCol `Unit.canAttack`). An **evaded** attack changes nothing but spends the attacker's turn.

**Cargo loot (1c-3c, FreeCol `csLootShip`).** Before the beaten ship goes down, if the **winner** is a **naval raider** — it has `model.ability.captureGoods` (frigate, privateer, man-o-war) — and has **free hold space**, it **plunders** as much of the loser's cargo as fits (`LootShip`, by stable goods order; goods pack 100/slot, merging into any partial stack the winner already holds). Whatever doesn't fit is lost with the ship. A pure transport (caravel/galleon/merchantman, no `captureGoods`) takes nothing. The transfer is deterministic — we auto-take what fits (FreeCol's loot-cargo *chooser* is a single-player nicety with no AI equivalent). No save-format change — looted goods ride the existing `Cargo` serialization.

**Outcome — damage vs sink (1c-3b, FreeCol `resolveAttack`).** A defeated ship loses its **cargo and everyone aboard** (passengers drown either way — `RemoveAll(CarrierId == ship.Id)`), then:
- on a **great** loss (or with **no repair location**, or beached) the hull is **sunk** (`SinkShip`) — gone for good;
- otherwise it is **damaged** (`DamageShip`): the hull survives but limps off the map to its **repair location** (Europe in our model — see below), where it sits **under forced repair** for `MaxHitPoints − 1` turns (every classic ship is 6 HP → **5 turns**), recovering one per turn (`AdvanceRepairs` in `EndTurn`). While `RepairTurnsRemaining > 0` the ship is pinned to 0 movement and **cannot sail, take on cargo, or be boarded** (FreeCol `isReadyToTrade`/forced repair — guarded in `SailToNewWorld`, `CheckBuyEuropeGoods`, `CheckBoard`). When it reaches 0 it is back in service in Europe (sail it home as usual).

**Foreign warships** at war hunt the human's nearest **ship** and attack it (1c-3a′ — `RunForeignPowerTurn`, on the power's own RNG stream); AI targeting is **domain-matched** (`NearestHumanUnit` filters `IsNaval == hunter.IsNaval`, so ships hunt ships and land units hunt land). A naval raid on the human records a `CombatNotice` the HUD surfaces ("sank"/"evaded"). A foreign power's damaged ship limps to **its own** Europe and ticks down without acting, all off the human's stream 0.

**Colonial-colony assault + capture (1c-3e, FreeCol `csCaptureColony`).** A colonial **land** unit can assault a **rival colony tile** (`CheckAttackColony`/`AttackColony`). If a garrison unit stands on the tile you must defeat it first (the normal unit-attack path); once the colony is **ungarrisoned**, its last-resort defender is an **unarmed colonist** (the abstracted population, defence 1) and a win **captures** the colony (`CaptureColony`): its `OwnerId` flips to the attacker and its people, buildings and stores transfer intact — the new owner can now manage it (and enter it). A loss disarms/demotes the repelled attacker (the shared loser precedence). Assaulting a rival is an **act of war** (recorded both ways before the colony changes hands — this is the colonial-colony attack→war trigger deferred in FP-6a). *(Model note: our colony population is an abstract integer, so an ungarrisoned colony's only defender is the single transient unarmed colonist — there is no in-colony **armed** colonist that would fight first as in FreeCol. To defend a colony you station a real unit on its tile, which routes to ordinary unit combat.)* **Human-initiated** for now; **deferred** (documented): plunder gold (FreeCol `colony.getPlunder` — no fabricated formula yet), the colony/stockade **defence bonus** value (needs building modifiers — the last defender currently gets none), Revere auto-equip of the last defender, **foreign-AI-initiated** capture (needs AI target selection + a colony-loss notice channel), and ships caught in a falling colony.

**Privateer piracy (1c-3d-i, FreeCol `csCombat`).** A unit with `model.ability.piracy` (the privateer) can raid a rival colonial power **without declaring war**: when the attacker *or* the defender has piracy, `Game.Attack` skips the colonial War/tension hook (so neither side's stance changes — a privateer plunders deniably, and attacking *someone else's* privateer also stays peaceful). A privateer also **flies no flag** (FreeCol `isOwnerHidden` = the unit type has piracy): when a foreign privateer raids the human, the `CombatNotice`'s attacker is the sentinel `Game.UnknownEnemyNationId` (`model.nation.unknownEnemy`, a no-owner pseudo-nation) and the HUD names no nation — "A privateer sank your frigate." This is the *mechanic*; the human can use it now (raid rival shipping without war). **AI-initiated** piracy (a foreign power sending privateers at peace) is deferred — foreign fleets have no privateers yet, and the AI only attacks at war. The `attackedByPrivateers` player flag FreeCol sets is **not modelled** (no monarch/diplomacy consumer); the `CombatNotice` is the player's feedback.

**Deviations from original / FreeCol (deliberate, 5b/5c/1b/1c-2/1c-3 scope):**
- **Repair location is always Europe.** FreeCol first sends a damaged ship to the nearest **own colony with a drydock** (`model.ability.repairUnits`), falling back to Europe; we have no drydock building yet, so Europe — FreeCol's fallback — is the only repair location. The repair *duration* and "cannot act while repairing" rules are faithful; **drydock-colony repair** is a later sub-slice. (A ship with no repair location at all — e.g. a hypothetical native-owned ship, of which there are none in the classic ruleset — still sinks.) Still deferred: **AI-initiated piracy** (foreign privateer fleets raiding at peace), and **colonial-colony assault/capture** (1c-3e).
- **Francis Drake (1c-3d-ii)** makes privateers **+50% offence and +50% defence** (`model.modifier.offence`/`defence` scoped to `model.unit.privateer`) — the first **scoped** founding-father combat modifier; it folds the unit owner's Congress into the offence/defence base (see [founding-fathers](founding-fathers.md)). A privateer with Drake fights at 12/12 instead of 8/8.
- **Repair counts down once per round, not per owner's turn.** `AdvanceRepairs` runs once at the end of `EndTurn` (after every player has acted), exactly like `AdvanceSailing`. So a ship damaged *during* a round (e.g. by an at-war rival in the same round) loses one repair turn at that round's end — FreeCol instead heals at the start of the *owner's* next turn. The total is ~5 turns either way; the divergence is a one-tick edge case and is consistent with how high-seas sailing already advances.
- **No naval or foreign-European unit/colony combat yet**, and no native **colony** raiding/capture (`pillageUnprotectedColony`) or tribute demands — the foreign-powers slice / native-AI follow-ups (see [natives](natives.md)).
- **Implicit garrison settlement defence.** A settlement defends with one transient brave-strength defender (the settlement bonus applied), not the strongest of several in/on-tile braves; the adjacent garrison braves need not be cleared first, and a winning assault destroys the settlement in one resolution. A faithful multi-brave on-tile defence waits for in-settlement unit lists.
- **Plunder is credited straight to gold.** FreeCol spawns a treasure train the attacker must escort home; treasure-train logistics + Cortés's `treasureTransportFee −100%` are deferred. `CAPTURE_CONVERT`/`BURN_MISSIONS` are omitted (missionary-gated, no missionary system); `DESTROY_NATION`/atrocity-score bookkeeping is not modelled.
- **Native tension is stored per-settlement only.** We apply FreeCol's `defenderTension` (with `getSlaughterTension` location routing — open-field +400 vs in-settlement +500/+600) uniformly across the nation's settlements, but keep no separate nation-level `Player.tension` store for *natives*, and don't model the player's own tension *toward* natives (so a repelled attack's attacker-side tension is dropped). Native stance changes off these values are the native-AI slice.
- **Colonial-vs-colonial:** from FP-6a, attacking a *rival colonial player's* unit records mutual War + a tension spike on the player-level diplomacy model (see [diplomacy](diplomacy.md)) — this is recorded only, it does not change combat resolution or gate the attack. Native combat is untouched by it.
- **Paul Revere applies the defence bonus without consuming/persisting the 50 muskets** (FreeCol's `AUTOEQUIP_UNIT` permanence is deferred); native auto-equipment is dormant until native settlements stock goods.
- Modifier *values*, tension deltas, the promotion probability, the great-win band and the plunder ranges are FreeCol-pinned constants in code (transposability-migration items — see [game-modes](game-modes.md)).

## 3. Technical design

- **Data:** `RoleType` (offence/defence summed from index-30 additive modifiers, required-goods, downgrade, granted/required abilities, `role-change` capture rules) and `UnitChange` (from→to + probability) parse in `Ruleset.ParseRoles`/`ParseUnitChanges`. `UnitType` gains combat-ability flags (`DisposeOnCombatLoss`, `CanBeCaptured`, `CaptureUnits`, `CaptureEquipment`, `DisposeOnAllEquipmentLost`, `DemoteOnAllEquipmentLost`, `Bombard`). `Ruleset.GetUnitChange` and `Ruleset.CaptureRole` are the lookups combat uses.
- **Model:** `Combat.CombatModel` is unchanged and still pure — roles fold into the **scalar base** passed to `AttackPower`/`DefencePower` (`UnitType.Offence + RoleType.Offence`), landing at FreeCol's index 30, before the index-50 situational percentages.
- **State:** `Unit.OwnerNationId` (null = player) + `RoleId`/`RoleCount`. `Game.CheckAttack`/`Attack` follow the `MoveCheck` + `CheckX`/`X` pattern; `Attack` builds the contexts, resolves through `CombatModel`, then `ResolveLoserOutcome` + `ApplyWinnerPromotion` apply the precedence above and `ChangeNativeAlarm` raises tension. `EquipRole`/`CheckEquipRole` arm a colonist from a colony's stock (charging the role-goods delta). `EffectiveCombatRole` implements FreeCol `getAutomaticRole` (Revere). Braves spawn in `Game.New` via `SpawnUnit(type, pos, ownerNationId)` on `FreeAdjacentLand`, consuming no RNG draw.
- **Settlement assault (5c):** `Game.CheckAttackSettlement`/`AttackSettlement` mirror the attack pair but target a `NativeSettlementAt(target)`. The defender is a **transient** `Unit` of `BraveUnitTypeId` (id 0, never added to `_units`) on the settlement tile, fed a `DefenceContext(SettlementDefenceBonus: SettlementType.DefenceModifier)`. A win removes the settlement from `_nativeSettlements`, runs `ApplyWinnerPromotion`, then `ComputePlunder`; a loss runs `ResolveLoserOutcome(transientDefender, attacker)`. `ComputePlunder` re-implements FreeCol `RandomRange.getAmount`; `SettlementType.PlunderRange(hasPlunderAbility)` selects base vs extra by the `plunderNatives` ability (via `AbilityForOwner`). `CheckMove` rejects a non-native move onto a native settlement tile.
- **Naval outcome (1c-3b):** `ResolveLoserOutcome(winner, loser, greatLoss)` takes the result's `great` flag; for a naval loser it calls `SinkShip` when `greatLoss || !CanRepairAtEurope(loser)`, else `DamageShip`. `DamageShip` drops passengers + cargo (`ClearCargo`), sets `Location = InEurope`, and sets `RepairTurnsRemaining = RepairTurnsFor(type) = max(1, MaxHitPoints − 1)`. `CanRepairAtEurope` is true for any **colonial** owner (every colonial power has a Europe); a ship is always colonial-owned, so the no-repair-location sink is defensive. `AdvanceRepairs` (called from `EndTurn` after `AdvanceSailing`, before the movement reset) decrements `RepairTurnsRemaining`; the reset loop pins a still-repairing ship to 0 moves (`IsUnderRepair ? 0 : InitialMovement`). `UnitType.MaxHitPoints` parses spec `hit-points` (default 1; all ships 6). The action guards — `SailToNewWorld` throws, `CheckBuyEuropeGoods`/`CheckBoard` return `No` — reject a repairing ship.
- **Determinism (ADR-009):** the public `Attack`/`AttackSettlement` draw from the game's **main saved RNG** (`_random`); the `internal …(IGameRandom)` overloads let tests force an outcome and script plunder draws — **and let the native AI resolve a raid from the nation's own stream**: `RaidHumanUnit` calls `Attack(brave, target, RandomFor(nativePlayer))`, so no native combat ever touches stream 0 (the human's byte-stability — guarded by `NativeAiTests` + the soak). The damage/repair path is **fully deterministic** (no RNG draw): `DamageShip`/`AdvanceRepairs` consume nothing, iterate `_units` in stable order, and never perturb stream 0 when a non-human ship is damaged. The settlement-assault RNG order is **combat draw → (win) promotion draw → plunder probability draw (only if `<100`) → plunder range draw**. `UpgradeUnitType` preserves owner/role/roleCount. `CaptureUnit` keeps `OwnerNationId` as the authoritative owner and never copies a native winner's `OwnerId` (0 = the human) onto the captive. Native units are excluded from `CurrentlyVisible`/`IsVisible`.
- **Save:** format **v18** adds `SavedUnit.Owner`/`Role`/`RoleCount` (default role omitted → v17 player saves byte-identical); braves round-trip via `Owner`. **v19** is a marker for settlement assault — a destroyed settlement is simply absent from the saved list and plunder folds into gold, so **no new field** and older saves load unchanged. **v21** adds `SavedUnit.RepairTurns` (ship damage state) — additive and **omitted when 0/healthy**, so an undamaged fleet serializes byte-identically and pre-v21 saves load as healthy.

## 4. Verification

| Layer | Required? | Tests / goldens | Status |
|---|---|---|---|
| L1 Unit | Always | `CombatModelTests` (power/odds/resolution + role fold); `RoleTests` (roles, unit-changes, capture-roles, combat abilities pinned to spec); `CombatTests` (CheckAttack gating, slaughter/disarm/demote/promote outcomes, equip-capture chains, alarm deltas, Washington, Revere, EquipRole, brave garrison, fog exclusion; settlement: plunder parse + formula, Cortés extra range, destroy, tension 500/600 + sibling propagation, loss-disarm, CheckAttackSettlement/CheckMove guards; **native-initiated: a brave may attack a human unit but not assault a settlement; a brave that beats an unarmed colonist destroys it without re-owning it; a raid leaves the raider's own alarm unchanged**); **`NativeAiTests`** (native raids draw only from the nation's stream); **`NavalCombatTests`** (great-loss sinks; **a normal loss *damages* — the hull limps to Europe under repair, cargo + passengers lost; repairs one turn at a time then returns to service; cannot sail / load / be boarded while repairing; repair state survives a save round-trip; a healthy fleet is byte-identical (no `RepairTurns` token)**; evade leaves both ships but spends the turn; an unarmed ship can't initiate; ships ↔ land can't attack; the −12.5%/goods-slot cargo penalty; replay-stable; a foreign warship at war attacks the human's ship on its own stream; stream-0 byte-stable; loot: a `captureGoods` raider plunders a defeated ship's hold before it sinks/limps, limited by free space; a plain transport that wins doesn't loot; **piracy: a privateer raids a rival without declaring war (attacker- and defender-side), a non-pirate attack still wars, a foreign privateer raid hides its nation in the notice**); `CombatModelTests` (`ResolveNaval` band theory, the cargo penalty, ship `hit-points` pinned, `captureGoods` pinned, **`piracy` pinned to spec**) | ✅ |
| L2 Scenario | When stateful | `CombatTests` save round-trips (v18 owner/role/braves; **destroyed-settlement state + production main-RNG resume-determinism**); `NavalCombatTests` damaged-ship repair-state round-trip (**v21**); **`ColonyCaptureTests`** (capture an ungarrisoned rival colony / declares war / repelled doesn't capture / garrison + own-colony refused / captured-colony save round-trip); `SaveGameTests` pre-v18 default load | ✅ |
| L3 Interaction | Yes (attack click) | `InputTests.ClickingAnEnemy_WithSelectedUnit_Attacks` (select a unit, click an adjacent brave → the attack fires, not a move); `EuropePanelTests.ShipUnderRepair_ShowsNoSailButton` (a repairing ship in port offers no sail button). (Colony-capture click routing mirrors the unit-attack routing; capture logic is L1-covered by `ColonyCaptureTests`.) | ✅ |
| L4 Visual | No screen | — | — |

- **FreeCol cross-check:** role/unit-change values, the loser-outcome precedence, the tension deltas and the power numbers are pinned to `SimpleCombatModel`, `Unit.java`, `Tension.java` and the classic spec in the tests above.

## 5. Open issues / TODO

- [x] **Combat 5c — native settlement assault** (attack / plunder / destroy + Cortés): shipped. See §2/§3.
- [x] **`CheckMove` foreign-colony guard** (slice 1c-1): a unit can't walk onto a colony it doesn't own (`ColonyAt(target).OwnerId != unit.OwnerId`) — closes the on-map-attack-UI path of strolling onto an ungarrisoned foreign colony. A garrisoned tile still routes to "attack it" via `DefenderAt`.
- [x] **Foreign-power-initiated unit combat** (slice 1c-2): a power at war sends its armed units after the human's units (`RunForeignPowerTurn`/`AttackHumanUnit`, on the power's own RNG stream). See §2.
- [x] **Naval combat — sink-only** (1c-3a): ship-vs-ship attack (evade/sink, cargo+passengers down, naval modifiers), foreign warships attack at war (1c-3a′). See §2/§3.
- [x] **Ship damage + repair** (1c-3b, save v21): a beaten ship is damaged and limps to Europe to repair over 5 turns (cannot sail/load/board meanwhile), sinking only on a great loss / no repair location. See §2/§3.
- [x] **Naval cargo loot** (1c-3c): a winning `captureGoods` raider (frigate/privateer/man-o-war) plunders the beaten ship's hold (up to its free space) before it sinks/limps. Save-stable. See §2/§3.
- [x] **Privateer piracy** (1c-3d-i): a `model.ability.piracy` unit attacks a rival without declaring war + flies no flag (anonymous `CombatNotice`). Save-stable. See §2/§3.
- [x] **Francis Drake** (1c-3d-ii): +50% offence/defence for privateers — the first scoped founding-father combat modifier (`FatherCombatFactor` folds the owner's scoped Congress into `OffenceBase`/`DefenceBase`). Save-stable. See [founding-fathers](founding-fathers.md).
- [x] **Colonial-colony assault + capture** (1c-3e, human-initiated): a land unit captures an ungarrisoned rival colony (`CheckAttackColony`/`AttackColony`/`CaptureColony`) — ownership + people/buildings/stores transfer; act of war. Save-stable. See §2.
- [ ] **Colony-capture follow-ups:** **plunder gold** on capture (`colony.getPlunder`); the colony/stockade **defence bonus** (needs `BuildingType` defence modifiers); **Revere** auto-equip of a colony's last defender; **foreign-AI-initiated** capture (a power at war takes the human's undefended colony — needs a colony-loss notice channel); ships caught in a falling colony.
- [ ] **Naval follow-ups:** **drydock-colony repair** (`model.ability.repairUnits`); **AI-initiated piracy** + a naval-preferring AI target refinement; foreign-vs-foreign / foreign-vs-native AI combat and AI-*declared* war.
- [x] **Native-initiated attacks (native AI)** (slice 1b) — braves raid the human's units on the nation's own RNG stream; exercises the Paul Revere auto-equip path. (The capture-unit path stays dormant — braves have `captureUnits=false` — but `CaptureUnit` is hardened against the brave-OwnerId-0 trap.) See [natives](natives.md). Colony pillage/capture (`pillageUnprotectedColony`) remains deferred.
- [ ] Faithful multi-brave on-tile settlement defence (in-settlement unit lists); clearing the adjacent garrison before the settlement falls.
- [ ] Treasure trains for plunder + Cortés's `treasureTransportFee`; `CAPTURE_CONVERT`/`BURN_MISSIONS` with the missionary system; `DESTROY_NATION`/atrocity bookkeeping.
- [ ] Nation-level tension store + `getSlaughterTension` location routing (5c keeps per-settlement + sibling propagation).
- [ ] Persist Revere's auto-equipped muskets (`AUTOEQUIP_UNIT`); native auto-equipment once settlements stock goods.
- [x] Apply role movement bonuses (dragoon/scout +9) to unit movement (done — see [units-movement](units-movement.md)).
- [x] On-map combat UI — click an adjacent enemy/settlement to attack (done).
- [ ] Move the pinned modifier/tension/promotion constants to ruleset data (transposability, [game-modes](game-modes.md)).

## Changelog

| Date | Change | Commit |
|---|---|---|
| 2026-06-15 | Slice 1c-3e: **colonial-colony assault + capture** (human-initiated) — `Colony.OwnerId` made settable; `Game.CheckAttackColony`/`AttackColony`/`CaptureColony`: a land unit assaults an ungarrisoned rival colony (transient unarmed-colonist defender, no colony bonus yet), a win flips `OwnerId` (people/buildings/stores transfer), a loss disarms/demotes the attacker; act of war recorded before the flip. `GameController.HandleTileClick` routes an ungarrisoned-rival-colony click to `AttackColonyAt`. Save-stable (OwnerId already serialized). Plunder / colony defence bonus / Revere / foreign-AI capture deferred | Phase 5 slice 1c-3e |
| 2026-06-15 | Slice 1c-3d-ii: **Francis Drake** — the first scoped father combat modifier. `FatherModifier` gains `ScopeUnitTypes`/`AppliesTo` (`ParseModifier` reads `<scope>`); `Game.FatherCombatFactor` folds the owner's scoped `model.modifier.offence`/`defence` into `OffenceBase`/`DefenceBase` — Drake = +50%/+50% for privateers (8/8 → 12/12). Per-owner, deterministic, no save change (rides the Congress); a power multiplier that commutes with the index-50 situational percentages. See [founding-fathers](founding-fathers.md) | Phase 5 slice 1c-3d-ii |
| 2026-06-15 | Slice 1c-3d-i: **privateer piracy** — `UnitType.Piracy` (parses `model.ability.piracy`); `Game.Attack` skips the colonial War/tension hook when the attacker or defender has piracy (raid/be-raided without war); a foreign privateer's `CombatNotice` records the `Game.UnknownEnemyNationId` sentinel and `FormatCombatNotice` names no nation ("A privateer sank your …"). Deterministic; no save change. `attackedByPrivateers` flag not modelled (no consumer); AI-initiated piracy + Drake deferred | Phase 5 slice 1c-3d-i |
| 2026-06-15 | Slice 1c-3c: naval **cargo loot** — `UnitType.CaptureGoods` (parses `model.ability.captureGoods`); `Game.LootShip(winner, loser)` runs at the top of the naval loser branch (before sink/damage): a raider with free hold takes the loser's cargo up to its capacity (100/slot, merging partial stacks). Deterministic (auto-take what fits); no save-format change (rides the `Cargo` dict). +loot/no-loot/space-cap tests + a `captureGoods` spec-pin | Phase 5 slice 1c-3c |
| 2026-06-15 | Slice 1c-3b: ship **damage + repair** (save **v21**) — `ResolveLoserOutcome` now branches damage-vs-sink on the great-loss flag; `DamageShip` drops cargo+passengers and limps the hull to Europe with `RepairTurnsRemaining = MaxHitPoints − 1` (5); `AdvanceRepairs` heals one/turn in `EndTurn`; a repairing ship is pinned to 0 moves and can't sail/load/board (`SailToNewWorld`/`CheckBuyEuropeGoods`/`CheckBoard` guards + Europe-panel UI); `UnitType.MaxHitPoints` parses spec `hit-points`. Deterministic (no RNG); `RepairTurns` additive (omitted when 0). Drydock-colony repair still deferred | Phase 5 slice 1c-3b |
| 2026-06-15 | On-map combat UI: `GameController.HandleTileClick` routes a click on an adjacent enemy unit / native settlement to `Attack`/`AttackSettlement` (else move), with a HUD outcome notice and selection cleared after; L3-tested (`InputTests.ClickingAnEnemy_WithSelectedUnit_Attacks`). Presentation only (ADR-006) | combat UI |
| 2026-06-15 | Slice 1c-3a: naval combat (sink-only) — `CombatModel.ResolveNaval` (evade band) + a −12.5%/slot cargo penalty + no terrain on water; `Game.Attack` branches on a naval defender; `SinkShip` removes a beaten ship and everyone aboard (fixes a latent passenger-orphan); foreign warships attack at war (`RunForeignPowerTurn` naval skip narrowed; `NearestHumanUnit` domain-matched); HUD "sank/evaded" phrasing. No save-format change (v20). Damage/repair, loot, privateers/Drake, colony capture deferred (1c-3b…e) | Phase 5 slice 1c-3a |
| 2026-06-15 | Slice 1c-2: foreign-power retaliation — `RunForeignPowerTurn` sends a power's armed units after the human's nearest unit when at `War` (via `AttackHumanUnit` → internal `Attack(…, RandomFor(power))`, the foreign sibling of `RaidHumanUnit`); human-units-only target, `CombatNotice` surfaced; dormant at peace (byte-stable). No save/RNG-stream-0 surface. See [diplomacy](diplomacy.md) | Phase 5 slice 1c-2 |
| 2026-06-15 | Slice 1c-1: `CheckMove` now blocks moving onto a colony you don't own (`ColonyAt(target).OwnerId != unit.OwnerId`) — closes the foreign-colony walk-in the on-map attack UI exposed; same-owner (garrison/join) moves and "attack a garrisoned tile" via `DefenderAt` unchanged. Logic-only, no save/RNG. (Rival rendering is presentation — see [presentation.md](../modules/presentation.md).) | Phase 5 slice 1c-1 |
| 2026-06-15 | Native-initiated raids (slice 1b): the native AI attacks the human's units via the internal `Attack(brave, target, RandomFor(nativePlayer))` overload — combat on the nation's own RNG stream (human stream 0 byte-stable), human-units-only targets, settlement gate kept closed; `CaptureUnit` hardened against the brave-`OwnerId`-0 capture trap; raids surfaced via transient `CombatNotice`s. No save-format change. AI in [natives](natives.md) | Phase 5 slice 1b |
| 2026-06-14 | Combat 5c: native settlement assault (`CheckAttackSettlement`/`AttackSettlement`) — implicit-garrison defence with the settlement bonus, destroy-on-win, `<plunder>` gold (`SettlementPlunder`, `ComputePlunder` ≈ FreeCol `RandomRange`), Hernán Cortés (`plunderNatives` → extra range), loss-disarm; can't move onto a settlement; save v19. Reworked combat tension to FreeCol `defenderTension` (nation-wide; win raises, repelled attack lowers; non-capital sack +900, capital burn → surrender 350) — supersedes 5b's flat +200/+400 | Phase 5 slice 5c |
| 2026-06-14 | Combat 5b: unit ownership + roles/equipment (`RoleType`, `UnitChange`, `EquipRole`), brave defenders, the attack action (`CheckAttack`/`Attack`) with the FreeCol loser/winner outcome precedence (slaughter/disarm/equipment-capture/demote/promote), native alarm on attack, Washington & Revere; save v18 | Phase 5 slice 5b |
| 2026-06-14 | Combat foundation: parse unit `offence`/`defence` and terrain defence bonus; pure `CombatModel` (power, odds, graded resolution) | Phase 5 slice 5a |
